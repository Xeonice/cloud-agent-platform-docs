# 文档一致性门禁（权威定义：docs/shared/09-工程化规范.md §2.4）
#
# 这道门跑在**文档主仓**：
#   A 类检查只读 docs/**，永远能跑；
#   B 类检查要读 submodule：B1/B2 读 api（openapi.json + MCP tool 注册），
#   B3/B4 读 api 与 web 两侧（openapi 跨仓逐字节、WS_PROTOCOL_CANONICAL 跨仓对账）。
# submodule 拉不到时脚本会「大声 skip」对应的 B 项并以 0 退出 —— 不让 A 类跟着瘫痪，
# 但会在 job summary 里留一条 warning，避免被静默吞掉。
#
# 零依赖：scripts/docs-check.mjs 只用 Node 内置模块，**不需要 install**。
name: docs-check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# 同一分支上新的 push 取消上一轮
concurrency:
  group: docs-check-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  docs-check:
    name: 文档一致性门禁
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout 主仓
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # .gitmodules 里 api/web 是 SSH remote 且指向另外两个仓库，默认 GITHUB_TOKEN
      # 读不到；配了 SUBMODULE_TOKEN（有 repo 读权限的 PAT）才拉得动私有子仓。
      # **拉不到不阻断**：下面的 docs:check 会把 B 类判成 skip。
      # ⚠️ 两个都要拉：B3/B4 是【跨仓】门禁（openapi 逐字节、WS canonical 对账），
      # 只拉 api 会让它们静默 skip —— 而「没人把关」正是这两条门禁要防的那件事。
      - name: 拉取 api / web submodule（可失败 → B 类走 skip）
        id: submodule
        continue-on-error: true
        env:
          SUBMODULE_TOKEN: ${{ secrets.SUBMODULE_TOKEN }}
          GIT_TERMINAL_PROMPT: '0'
        run: |
          set -euo pipefail
          if [ -n "${SUBMODULE_TOKEN:-}" ]; then
            git config --global url."https://x-access-token:${SUBMODULE_TOKEN}@github.com/".insteadOf "git@github.com:"
          else
            git config --global url."https://github.com/".insteadOf "git@github.com:"
          fi
          git submodule update --init --depth 1 api web

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      # 等价于 `pnpm docs:check`；零依赖所以不装包，checkout 完直接跑。
      - name: docs:check
        run: node scripts/docs-check.mjs

      - name: 提示 B 类被跳过
        if: ${{ steps.submodule.outcome != 'success' }}
        run: echo "::warning title=B 类检查本轮未执行::api/web submodule 没拉到（未配 SUBMODULE_TOKEN？），端点覆盖、MCP 对账、以及 openapi 与 WS 协议的【跨仓一致】本轮全部无人把关。"
