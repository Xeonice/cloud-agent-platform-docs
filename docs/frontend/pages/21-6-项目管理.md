# F21-6 · 项目管理（任务树 + 指示器 + 弹层，无独立路由）前端设计

> 状态：✅ 可评审 · 对应产品页：[P21-6 项目管理](../../product/pages/21-6-项目管理.md)
> 上级：[前端页面设计总纲](./README.md) · 架构依据：[07 §7.3/§8.1](../07-前端目录结构与视图逻辑分离.md) · [15 §3.1.1/§5](../15-前端状态管理.md) · [12](../12-前端测试体系.md)

## 1. 页面职责

Project = 工作区（git repo 或空目录）+ Task 集合的容器。管理形态**轻量化、无独立页面**：浏览/切换由左侧任务树承担，创建走树底与向导确认步两个入口，管理走组头「⋯」侧弹层。**本文只描述"项目"这一横切能力的前端落点**——它的宿主是工作台（[F21-1](./21-1-工作台.md)）与向导（[F21-2](./21-2-发起任务向导.md)）。

## 2. 路由与骨架落点

| 项 | 落点 |
|---|---|
| 路由 | **无独立路由**（07 §7.3）；项目上下文体现在 `uiSlice.selectedProjectId` 与深链 `/?taskId=` 的反向定位 |
| 宿主 | 工作台 `app/page.tsx`（树、指示器、树底入口、组头菜单）+ 向导 modal（确认步项目行、「＋ 新建项目…」） |
| 弹层 | `CreateProjectModal`（`currentModal='createProject'`）、`ProjectMenuPanel`（`currentModal='projectMenu'`）；两者不同时存在（modal 不堆叠） |
| 两条回程 | **`wizardReturn`**：向导 →创建项目 → 回确认步；**`pendingProjectCreate`**：创建弹层 → Git 凭证页 → 回创建弹层**重试克隆**（携 `projectId`，回程调 retry-clone 而非重新创建）。二者**语义分离、状态位不复用**（15 §3.1.1 纪律 3） |
| 创建语义 | `POST /api/projects` **先落库再异步 clone**（24 §3）——**项目在 clone 开始前就已经存在于列表**，clone 失败时它以 `cloneStatus='failed'` 持久化，而不是"什么都没发生"。这决定了所有重试都走 `retry-clone`（§5），**任何路径都不得重新 POST create**，否则产生重复项目 |

> ⚠️ **本节这棵树大半没建**（2026-08-31 核实）：`ProjectMenuPanel` / `ProjectGroupMenu` /
> `CurrentProjectIndicator` 在代码里**一个都不存在**（全仓零命中），今天唯一已落地的
> 项目级落点是 `ProjectInfoBar.view`（只读条，§9）。
>
> ⇒ §8 那句「已保留卷/自动化 **MVP 在 `ProjectMenuPanel` 中渲染为禁用态 + 版本角标**」、
> 以及 §7.2 给 `ProjectMenuPanel.view` 列的那些 Storybook variant，描述的都是**文档的
> 事实、不是代码的事实** —— 它们指着一个不存在的组件。⚠️ 这条曾经误导过一次判断：
> 「保留卷今天是禁用态、所以归 v1.1」这个推论的前提本身就不成立。
>
> **已保留卷（本轮落地）因此挂在 `ProjectInfoBar` 上**（`[🎁 已保留卷]` → overlay），
> 是当下唯一可行的落点；`RetainedVolumesContainer` 与入口 prop 的注释里都标了 ⏳：
> `ProjectMenuPanel` 一旦落地，整段搬过去。

## 3. 组件树

```
（宿主：工作台）
├── CurrentProjectIndicatorContainer                 [F21-1 渲染，本页定义语义]
│   └── CurrentProjectIndicator.view                     只读；点击 → 树内定位展开
├── ProjectTaskTreeContainer                          [F21-1 渲染，项目相关分支在本页定义]
│   ├── ProjectGroupHeader.view                          组头 = 项目的浏览/切换/管理入口
│   │   ├── variant="normal"                             📁 ProjectName (N) ⋯
│   │   └── variant="cloneFailed"                        🔴 📁 ProjectName ⚠️ 克隆失败 ⋯（§6，产品 P21-6 §9）
│   ├── ProjectGroupMenu.view                            组头「⋯」菜单；failed 态三出口：
│   │                                                    [重试克隆] / [改为空项目] / [删除]
│   └── TreeFooterCreateButton.view                      树底 [＋ 新建项目]
├── CreateProjectModal.view                           [本页私有]
│   ├── ProjectNameField.view                            1–40 字符、禁止重名、项目上限 50
│   ├── ProjectSourceRadio.view                          ◉ 从 Git 仓库克隆 / ○ 空项目
│   ├── GitUrlField.view                                 URL + "私有仓库需先配置 Git 凭证" 提示
│   ├── CloneProgress.view                               "🔄 正在克隆仓库…（12MB/45MB）"
│   └── CloneErrorPanel.view                             网络/URL 类 vs 权限类两分支；两类的 [重试*] 都调 retry-clone（§5）
└── ProjectMenuPanel.view                             [本页私有] 侧弹层
    ├── ProjectMetaSection.view                          名称/状态/任务数/创建时间（**不展示「来源」行**，§6）
    ├── ProjectActions.view                              [重命名(v1.1)][归档(v1.1)][删除]
    ├── AutomationEntry.view（v1.1）                     ⚙️ [自动化规则] → F21-7 侧弹层
    ├── RetainedVolumesEntry.view（v1.1）                🎁 [已保留卷] → `GET /api/retained-volumes?projectId=`（审计 P2-5）
    └── DeleteProjectConfirm.view                        级联后果文案 + 运行中任务警示
```

**复用关系**：`CreateProjectModal` 被两处调起（树底、向导确认步），**同一实现、靠 `wizardReturn` 区分回程目标**；`DeleteProjectConfirm` 与 F21-1 的 `DestroyTaskDialog` 是不同组件（前者级联、后者含保留卷勾选），但共用二次确认骨架。

## 4. 状态与数据接入

| 类别 | 具体 | 来源 |
|---|---|---|
| Query | `projectKeys.list()` → `GET /api/projects`（含各项目 Task 数聚合） | 15 §2.1 |
| Mutation | `createProject` → `POST /api/projects` —— **先落库再异步 clone**（后端 24 §3）：立刻返回 **202 + `{ id, cloneStatus:'cloning' }`**，项目此刻**已经在列表里**；进度经 WS | 24 §3 |
| Mutation | **`retryClone`** → **`POST /api/projects/:id/retry-clone`** —— 对**已存在的 `cloneStatus='failed'` 项目**重试克隆（27 §10.4） | 02 §5.1 |
| Mutation | **`convertToEmpty`** → **`POST /api/projects/:id/convert-to-empty`** —— 对 failed 项目：`source→empty`、工作区清空、`cloneStatus→ready`，**保留项目 id / 名称 / 已有 Task**；非 failed 态调用返 **409** | 02 §5.1 |
| Mutation | `deleteProject` → `DELETE /api/projects/:id`（级联） | P21-6 §7 |
| Zustand（persist） | `selectedProjectId`、`taskListFolds` | 15 §3.1.1 |
| Zustand（不 persist） | `selectedProjectForMenu`、`currentModal`、`wizardReturn`、`pendingProjectCreate` | 15 §3.1.1 |
| 派生 | `selectProjectTaskTree(projects, tasks, folds)`（分组树） | 15 §5 |
| WS | **`project.clone_progress`** → patch `projectKeys.list()` 驱动进度；**终态走 invalidate** | 15 §2.3 |

**`cloneStatus` 只有三个合法值**（后端 13 §2.2 的 CHECK 约束）：

| 值 | 含义 | 前端表现 |
|---|---|---|
| `cloning` | 克隆进行中 | 弹层进度态；项目已在树中 |
| **`ready`** | **工作区就绪可用**——克隆成功、`convert-to-empty` 成功、以及**空项目自建起就恒为此值** | 组头 `variant="normal"`，可选为当前项目 |
| `failed` | 克隆失败 | 组头 `variant="cloneFailed"`，不可选为当前项目（§6） |

⚠️ **没有 `success` 这个值**——正常态一律写 `ready`。类型从 contract 生成的 union 取，不手写字面量；`lib/projectGroupState.ts` 的入参类型即这三值的 union，写错第四个值会在 tsc 阶段就红（§7.1）。

## 5. 交互实现要点（产品交互表逐行映射）

| 产品交互（P21-6 §6/§9） | container + handler | 调用 |
|---|---|---|
| 切换项目（树内点击组头/任务） | `ProjectTaskTreeContainer.handleSelectTask/handleSelectGroup` | `uiSlice.expandProject(projectId)`（同时切上下文 + 展开）→ 右侧终端切到所点任务；`selectedProjectId` persist |
| 顶部指示器点击 | `handleLocate()` | 只做树内定位展开，**不打开下拉、不承载创建/管理入口** |
| 树底 [＋ 新建项目] | `handleOpenCreateProject()` | `currentModal='createProject'`，`wizardReturn=false` |
| 确认步「＋ 新建项目…」 | 由 [F21-2](./21-2-发起任务向导.md) 触发 | `wizardReturn=true` + `wizardData` 全量暂存 → 同一个 `CreateProjectModal` |
| 创建（空项目） | `handleCreate({name, source:'empty'})` | `project.service.create()` → 即时成功 → invalidate + 自动选中 + toast |
| 创建（git clone） | `handleCreate({name, source:'git', url})` | `POST /api/projects` → 弹层转进度态，进度由 `project.clone_progress` 驱动；>10min 提示「仓库较大或网络缓慢 [继续等待]/[取消]」；**30min 硬超时**转失败 |
| 创建成功回程 | `onCreated(id)` | `wizardReturn` 真 → 回向导确认步并选中新项目；否 → 关弹层 + `expandProject(id)` |
| clone 失败·网络/URL | `CloneErrorPanel variant="network"` | 就地红字「仓库不可访问：检查 URL 或网络」+ [重试] / [改为空项目]。**[重试] 调 `project.service.retryClone(projectId)` → `POST /api/projects/:id/retry-clone`**，**不是**重新 `POST /api/projects` |
| clone 失败·权限 401/403 | `CloneErrorPanel variant="permission"` | 就地 [配置 Git 凭证] → 置 `pendingProjectCreate={ **projectId**, name, source, url }` → 路由跳 `/settings/credentials` → 配置完成回本弹层 → **[重试克隆] 同样调 `retryClone(projectId)`** |
| **[改为空项目]**（创建弹层内 + 组头菜单两处） | `handleFallbackToEmpty(projectId)` | `project.service.convertToEmpty(id)` → **`POST /api/projects/:id/convert-to-empty`**。**与 retry-clone 是两回事**：这条是**放弃克隆**、把该项目就地转为可用的空项目（保留 id/名称/已有 Task），既不重试也不新建。成功后 `cloneStatus→ready` → 组头红标消失、转 `variant="normal"`；用 `setQueryData(projectKeys.list())` patch 该项即时更新，再 `invalidateQueries` 兜底（15 §2.3 的 patch 优先、复杂则降级 invalidate 原则） |
| 组头「⋯」（failed 项目） | `handleOpenGroupMenu(projectId)` | 打开 `ProjectGroupMenu`，**三出口**：[重试克隆] → `retryClone` · [改为空项目] → `convertToEmpty` · [删除] → 走删除确认。三者共用同一个 `projectId`，**不经创建弹层** |
| 点击 failed 项目的组头/任务 | `handleSelectGroup` / `handleSelectTask` 内前置判定 | **failed 项目不可选为当前项目**：点组头**仅展开/折叠**、不改 `selectedProjectId`；点其下任务（若有）无效。这是否定性行为，§7/§9 各有断言 |
| 组头「⋯」→ 侧弹层 | `handleOpenProjectMenu(projectId)` | 置 `selectedProjectForMenu`（**不改 `selectedProjectId`**）+ `currentModal='projectMenu'` |
| 删除项目 | `handleDeleteProject(id)` | 二次确认，文案含"将删除该项目下 N 个 Task 及其数据卷（保留的成果卷除外），不可逆"；**存在运行中 Task 时追加「含 N 个运行中任务将被强制停止」** → `DELETE /api/projects/:id` → invalidate projects + sandboxes |
| 欢迎态 [开空项目] | `handleQuickCreateEmpty()` | **跳过弹层**，自动命名「未命名项目 N」（`lib/nextUntitledName.ts`）→ 创建 → 自动选中 → 引导发起第一个任务 |
| 归档项目（v1.1） | `handleArchive(id)` | 归档后组在树底淡显；选中后工作台只读（发起入口置灰） |

## 6. 状态矩阵落地

| 产品状态（P21-6 §4） | 渲染分支 |
|---|---|
| 无任何项目（冷启动） | `projects.data.length===0` → 工作台 `WorkspaceEmptyState variant="no-project"`（[📦 用我的代码库] / [📄 开一个空项目]）；指示器显"未选择项目" |
| 正常 | 指示器显当前项目名；树渲染全部项目组 |
| 克隆中 | `cloneStatus==='cloning'` → 弹层进度态（字节进度）；**此时项目已在树中**（先落库语义，§2） |
| 克隆失败·网络/URL | `cloneStatus==='failed'` 且错误码为网络/URL 类 → `CloneErrorPanel variant="network"`（[重试] → retry-clone） |
| 克隆失败·权限 | `cloneStatus==='failed'` 且 401/403 → `CloneErrorPanel variant="permission"` + [配置 Git 凭证] |
| **克隆失败·树内形态**（产品 P21-6 §9 已定） | 项目以 `cloneStatus='failed'` **持久留在树中**（先落库的直接后果）→ 组头渲染 `variant="cloneFailed"`：**`🔴 📁 ProjectName ⚠️ 克隆失败 ⋯`**；「⋯」菜单三出口 [重试克隆]/[改为空项目]/[删除]；**计入项目上限 50**；**无法选为当前项目**（点组头仅展开、点任务无效） |
| 转空项目成功 | `convertToEmpty` 成功 → `cloneStatus='ready'` + `source='empty'` → 组头回到 `variant="normal"`、红标消失；**项目 id / 名称 / 已有 Task 均不变** |
| **项目菜单不展示「来源」** | 产品已定：`ProjectMetaSection` **不渲染「来源」行**——无论 git 克隆、空项目还是 failed 转空，一律不显示。因此"转空后来源显示什么"这个问题在 UI 上不存在；`source` 字段仍在数据里（后端语义需要），只是**前端不呈现** |
| 归档项目（v1.1） | `status==='archived'` → 组在树底淡显；选中后发起入口置灰 |

## 7. 本页测试设计

### 7.1 unit（bun test：lib / hooks / stores）

| 被测 | 用例（断言级） |
|---|---|
| `lib/validateProjectName.ts` | 0 拒绝 / 1 通过 / 40 通过 / 41 拒绝；与现有项目重名 → 拒绝；项目数已 50 → 创建入口返回 `atLimit:true` |
| `lib/nextUntitledName.ts` | **按产品定的「历史最大 N + 1、不复用已删号」**：① 已有 1 → 返回 2；② **存在 1、3（有空洞）→ 返回 4**，**不填空洞的 2**；③ 无同名 → 返回 1；④ 曾创建并删除过 5 号（历史最大仍为 5）→ 返回 6；⑤ 已被用户改名的项目不参与最大值计算 |
| `lib/classifyCloneError.ts` | 401/403/`permission denied` → `permission`；DNS/超时/404 → `network`；未知码 → `unknown`（走 P22 兜底文案） |
| `lib/selectProjectTaskTree.ts` | 见 [F21-1 §7.1](./21-1-工作台.md)（同一纯函数，本页补测：删除项目后该组消失且其 Task 不残留） |
| `hooks/useProjects.ts` | ① 创建成功（202）→ invalidate `projectKeys.list()`，**且把返回的 `id` 交给调用方**（后续 retry-clone 要用）；② 删除成功 → **同时** invalidate projects 与 sandboxes；③ mutation `retry:0`；④ clone 进度事件 patch 到列表项而非新建缓存条目；⑤ **`retryClone(id)` 拼 `POST /api/projects/:id/retry-clone`，且 hook 层不存在任何"重试时重新调 create"的分支**（结构性断言，防重复项目） |
| `services/project.service.ts` | ① `create` 拼 `POST /api/projects` 并返回 `{id, cloneStatus}`；② **`retryClone(id)` 拼 `POST /api/projects/:id/retry-clone`**；③ **`convertToEmpty(id)` 拼 `POST /api/projects/:id/convert-to-empty`**；④ 三个方法**路径两两不同、不可互换**——带 id 的两个方法无 id 时应抛错而非降级去 create；⑤ `convertToEmpty` 收到 **409**（非 failed 态）时抛出可识别错误，由 hook 映射为"该项目无需转换"的人话而非裸 409 |
| `lib/projectGroupState.ts` | ① 由 `cloneStatus` 派生组头 variant 与可选性：`cloning→normal(进度)`、`failed→cloneFailed 且 selectable:false`、**`ready→normal 且 selectable:true`**；② **failed 项目仍计入项目上限 50**（`countsTowardLimit:true`）；③ **三值全覆盖断言**——入参类型是 contract 生成的 `cloneStatus` union，逐值断言有归宿（后端若加第四值，本用例先红）。**断言里不得出现 `'success'`**：该值不存在于 CHECK 枚举（§4） |
| `stores/createUiSlice`（项目片段） | ① `expandProject(p)` 同时置 `selectedProjectId` 与展开；② `setSelectedProjectForMenu` 不改 `selectedProjectId`；③ `wizardReturn` 与 `pendingProjectCreate` 互不影响（分别置位后各自读回正确）；④ 二者均不在 `partialize` 白名单 |

### 7.2 Storybook

| Story | 必备 variant / play 断言 |
|---|---|
| `CreateProjectModal.view` | 空表单 · git 源（URL 字段 + 私有仓提示）· 空项目源（URL 字段隐藏）· 名称超限红字 · 克隆进度态 · 网络类失败 · 权限类失败；**play**：切到"空项目" → 断言 URL 字段从 DOM 移除；名称为空时 [创建] disabled |
| `CloneProgress.view` | 0% · 27%（12MB/45MB）· >10min 提示 [继续等待]/[取消] |
| `CloneErrorPanel.view` | network（[重试]/[改为空项目]）· permission（[配置 Git 凭证]）· unknown（兜底文案 + [重试]） |
| `ProjectMenuPanel.view` | 常规项目 · 含运行中任务 · **git 来源项目**（用于验证仍不显示来源）· v1.1 项（自动化/已保留卷）置灰或隐藏；**play（否定性）**：所有 variant 断言面板内**不存在「来源」行**，且 DOM 全文不含 mock 的 git URL |
| `DeleteProjectConfirm.view` | 0 任务 · 5 任务 · **含 2 个运行中任务（追加强制停止文案）**；**play**：断言确认按钮在勾选/输入确认前的可用性符合规格 |
| `CurrentProjectIndicator.view` | 见 [F21-1 §7.2](./21-1-工作台.md)（只读回归断言在那里） |
| `ProjectGroupHeader.view` | 见 F21-1（本页补两个 variant）：**`cloneFailed` 态 `🔴 📁 ProjectName ⚠️ 克隆失败 ⋯`** · 归档项目淡显态；**play**：failed 态点组头 → 只触发 `onToggleFold`，**`onSelectProject` 未被调用**（否定性断言） |
| `ProjectGroupMenu.view` | failed 项目（三出口 [重试克隆]/[改为空项目]/[删除]）· 正常项目（无前两项）；**play**：点 [改为空项目] → `onConvertToEmpty` 携正确 projectId |

### 7.3 集成（container story + play，msw 拦截网络层）

| 交互流 | 断言链 |
|---|---|
| 空项目创建 | 树底 [＋ 新建项目] → 填名 → 选空项目 → [创建] → 弹层关闭 → 树中出现该组且已展开 → 指示器切到新项目 |
| 克隆进度 | 提交 git 源 → 注入两次 `project.clone_progress` → 断言进度文案递增 → 注入成功终态 → 断言触发 invalidate 且弹层关闭 |
| 权限失败回程 | mock 403 → 断言 permission 分支 → 点 [配置 Git 凭证] → 断言 `pendingProjectCreate` **含 `projectId`** 且发生路由跳转；模拟返回 → 断言弹层以原表单值重开并显示 [重试克隆] → 点击 → 断言发出的是 **`POST /api/projects/:id/retry-clone`**，且 **msw 未收到第二次 `POST /api/projects`**，列表中该项目**仍只有一条** |
| 网络类 [重试] | mock 网络类失败 → 点 [重试] → 断言同样走 `retry-clone`（不是 re-create），项目数不变 |
| **[改为空项目]（弹层内）** | mock clone 失败 → 点 [改为空项目] → 断言发出 **`POST /api/projects/:id/convert-to-empty`**（**不是** create、不是 retry-clone）→ mock 成功 → 断言该项目转正常态、**id 与名称不变**、组头红标消失、项目总数不变 |
| **[改为空项目]（组头菜单）** | 关闭弹层后从 failed 组头「⋯」→ [改为空项目] → 断言走同一端点、同一结果（两个入口一条链路） |
| **failed 项目不可选为当前项目** | 当前项目为 A，点 failed 项目 B 的组头 → 断言 B 组**展开了**但 `selectedProjectId` **仍是 A**；点 B 组内任务（若有）→ 断言无切换、无终端打开 |
| **convert-to-empty 409** | mock 对非 failed 项目调用返 409 → 断言显示"该项目无需转换"类人话提示，**不裸抛 409**，且项目状态未被本地改动 |
| 向导回程（跨页） | 从向导确认步打开创建弹层（`wizardReturn=true`）→ 创建成功 → 断言**回到确认步**而非工作台，且新项目已选中（与 [F21-2 §7.3](./21-2-发起任务向导.md) 同一断言链的项目侧视角） |
| 组头菜单不改上下文 | 当前项目为 A，点 B 组头「⋯」→ 断言 `selectedProjectForMenu==='B'` 且 `selectedProjectId` 仍为 A |
| 删除级联 | 删除含 2 个运行中任务的项目 → 断言确认文案含「含 2 个运行中任务将被强制停止」→ 确认 → 树中该组消失且其 Task 不残留 |

### 7.4 e2e（Playwright）

- **参与 12 §4.2 用例组**：A（分组树跨项目切换）、C 第 2 步（确认步「＋ 新建项目…」往返保值）、G 第 ②③ 步（建项目 → 分组树）。
- **本页独有补充场景**：
  1. 冷启动无项目 → 断言欢迎态两个 CTA → [开空项目] 一键路径 → 断言**跳过弹层**且自动命名。
  2. 私有仓库 clone 403 → 配置 Git 凭证 → 回程 [重试克隆] 成功 → 断言项目就绪并自动选中，且**项目列表中该项目只有一条**（跨页回程的真实链路 + 防重复创建）。
  3. 深链 `/?taskId=` 指向 B 项目的 Task → 断言上下文自动切到 B（项目上下文一致性，P22 §4.14）。
  4. 删除当前选中项目 → 断言上下文自动落到另一个项目而非空白页。

## 9. ★ 项目只读条与基线同步（本轮新增）

### 9.1 为什么补这一块

此前平台上**看不到任何 git 远端情况**：项目在界面上只是树里一行（名字 + 任务数 +
克隆徽标）。而 `projects` 表里 `repo_url` / `repo_branch` / `baseline_size_bytes` /
`updated_at` **一直都在**，只是没进 DTO —— 不是没做，是数据卡在持久化层。

10 §7 里此前那条 *"「来源」字段不对外展示（产品定案）——repoUrl 不入 DTO"* 由本轮
推翻：那条定案的场景是"用户不需要知道代码从哪来"，而完整克隆（03 §7.2★）之后，
**基线体积**与**最后同步时间**都成了用户必须看得见的信息。

### 9.2 只读条（不是详情页）

选中项目时，工作台主区**顶部**一条只读信息 —— 不新开页面：工作台主区在"没选任务"
时本来就是空的，为四个字段开一个页面不成比例。

| 字段 | 来源 | 空项目 |
|---|---|---|
| 远端地址 | `ProjectDto.repoUrl` | 不显示（整条降级为"空项目"） |
| 分支 | `ProjectDto.repoBranch` | — |
| 基线体积 | `ProjectDto.baselineSizeBytes` | — |
| 最后同步 | `ProjectDto.updatedAt` | 显示创建时间 |

**只读**：改远端、切默认分支、重新 clone 都**不在这条上**。它回答的是"我在拿什么
代码干活"，不是项目管理。

### 9.3 [重新同步]

唯一的动作：`POST /api/projects/:id/sync`（仅 `ready` 态）。

⚠️ **一个刻意不做呈现的语义**：同步只更新基线，**已有 Task 的工作区一律不动**
（它们是当时的写时复制副本）。于是同一项目下的两个 Task 可能跑在不同代码上，而界面
上看不出来。做这个呈现属于"完整"那一档；本轮只用「最后同步」这一格让"我的基线是
什么时候的"可见 —— 有意识地留下的缺口，不是遗漏。

### 9.4 新建项目：改成真弹窗，并补分支输入

⚠️ **今天它不是弹窗。** `currentModal==='createProject'` 这个名字是假的——
`WorkbenchContainer` 把它 **return 成 `mainContent`**，是主区换页。改成走仓内既有 overlay
（`ConfirmDialog.view` 的 `fixed inset-0 z-50 … bg-black/60`），与「新建任务」弹窗**形态对称**
——两个新建动作长一个样，这正是本轮要解决的"两个描述都不清晰"（详见 [F21-2](./21-2-发起任务向导.md) §N.0）。

字段：名称 / 来源（Git 仓库 · 空项目）/ 仓库地址 / **分支**。
`CreateProjectRequest.repoBranch` **契约里一直有**，表单从来没接——本轮补上（留空 = 远端默认分支）。

改形态时**顺手把 `currentModal` 的名字兑现**，并清掉 `'registerImage'` / `'wizard'` 两个死值
（全仓无人 set、无人读）。

## 8. 依赖与缺口

- 依赖后端：`GET/POST/DELETE /api/projects`、**`POST /api/projects/:id/retry-clone`**、异步 clone 与 `project.clone_progress` 事件、错误码**必须区分权限类与网络类**（P22 §2 依赖）。
- ✅ **创建语义已定案（24 §3）：先落库再异步 clone**。`POST /api/projects` 返回 202 + `{id, cloneStatus:'cloning'}`，项目立刻进列表；clone 失败则以 `cloneStatus='failed'` 持久化。**由此所有重试路径一律走 `POST /api/projects/:id/retry-clone`（27 §10.4），前端任何分支都不得重新 POST create**——否则每次重试都多出一个同名项目。`project.service.ts` 的 `create` 与 `retryClone` 是两个不可互换的方法（§7.1 有结构性断言）。
- ✅ **[改为空项目] 端点已锁定**：**`POST /api/projects/:id/convert-to-empty`**——对 failed 项目 `source→empty`、工作区清空、`cloneStatus→ready`，**保留项目 id / 名称 / 已有 Task**；非 failed 态调用返 **409**。语义上它与 retry-clone 平级（都作用于已存在的 failed 项目），与 create 无关。两个入口（创建弹层内、组头「⋯」菜单）走**同一条链路**。
- ✅ **失败项目的树内形态已定**（产品 P21-6 §9）：组头 `🔴 📁 ProjectName ⚠️ 克隆失败 ⋯`；「⋯」三出口 [重试克隆]/[改为空项目]/[删除]；**计入项目上限 50**；**无法选为当前项目**（点组头仅展开、点任务无效）。三条行为由 `lib/projectGroupState.ts` 纯函数固化（§7.1）。
- 重命名/归档/已保留卷/自动化为 v1.1，MVP 在 `ProjectMenuPanel` 中渲染为禁用态 + 版本角标。
- ✅ **保留卷端点统一在一个资源前缀下**（审计 P2-5）：`GET /api/retained-volumes?projectId=` · `DELETE /api/retained-volumes/:id`——原先"一个嵌套（`/projects/:id/volumes`）一个平铺（`/volumes/:id`）"的双形态已废弃。`RetainedVolumesEntry` 与 [F21-5](./21-5-系统状态.md) 的磁盘治理横幅 [去清理] 消费同一端点，前者按项目过滤、后者看全局总量。

## 9. 验证计划（验收视角）

> **与 §7 的边界**：§7 是测试实现，本节是验收。功能清单从 **P21-6 的交互表（§6）、状态矩阵（§4）、首次引导（§5）、边界规则（§9）** 逐行取。

### 9.1 功能验收清单

| # | 功能/交互（产品出处） | 验收标准（可观察） | 验证方式 | 覆盖 |
|---|---|---|---|---|
| 1 | 树内点击切换项目（§6） | `currentProjectId` 切换、该组展开、右侧切到所点任务、顶部指示器同步；persist | [F21-1 §9](./21-1-工作台.md) VS-1 步骤 4 | ✅ |
| 2 | 顶部指示器**不提供下拉**（§1/§6） | 点击只做树内定位，无下拉菜单 | [F21-1 §7.2](./21-1-工作台.md) play 否定断言 | ✅ |
| 3 | 树底 [＋ 新建项目]（§2） | 打开创建弹层，`wizardReturn=false` | §7.3 空项目创建 | ✅ |
| 4 | 确认步「＋ 新建项目…」（§2） | 打开同一弹层，`wizardReturn=true`，创建后回确认步 | §7.3 向导回程 + [F21-2 §9](./21-2-发起任务向导.md) VS-3 | ✅ |
| 5 | 项目名校验与上限（§3.2/§9） | 1–40 字符、**禁止重名**、项目上限 50。**触达上限的 UI 产品已定**：`[＋ 新建项目]` **置灰** + tooltip「已达 50 个项目上限，请先删除部分项目」；**failed 项目也占名额** | §7.1 `validateProjectName` + `projectGroupState`（`countsTowardLimit`） | ⚠️ 标准已明确，**置灰 + tooltip 的 story/集成未覆盖** |
| 6 | 空项目创建（§6） | 即时成功 → 自动选中 + toast | §7.3 空项目创建 | ✅ |
| 7 | git clone 创建（§6） | 弹层转进度态"🔄 正在克隆仓库…（12MB/45MB）" | §7.2 `CloneProgress` variant + §7.3 克隆进度 | ✅ |
| 8 | clone 进度 WS 驱动（§7） | `project.clone_progress` patch 列表 → 进度文案递增；终态 invalidate | §7.1 useProjects + §7.3 | ✅ |
| 9 | clone >10min 提示（§6） | 「仓库较大或网络缓慢」+ [继续等待]/[取消] | §7.2 variant | ⚠️ **10min 阈值无假时钟用例** |
| 10 | clone 30min 硬超时（§6） | 转失败态 | — | ❌ 无用例 |
| 11 | clone 失败·网络/URL 类（§4） | 就地红字「仓库不可访问：检查 URL 或网络」+ [重试]/[改为空项目]；**[重试] 走 `retry-clone`，不产生第二个项目** | §7.1 `classifyCloneError` + §7.3 网络类 [重试] | ✅ |
| 12 | clone 失败·权限类 401/403（§4） | 就地 [配置 Git 凭证] → 跳凭证页 → 回弹层 [重试克隆]；**同样走 `retry-clone`，项目仍只有一条** | §7.1 + §7.3 权限失败回程 + §7.4 补充场景 2 | ✅ |
| 12b | **[改为空项目]**（§4/§5） | 放弃克隆、该项目就地转为可用空项目——**不是重试、也不是新建**；成功后 `cloneStatus→ready`、组头红标消失、**id/名称/已有 Task 不变**、项目总数不变 | §7.1 service ③ + §7.3 弹层内 & 组头菜单两条 | ✅ |
| 12c | **失败项目的树内形态**（P21-6 §9） | 组头 `🔴 📁 ProjectName ⚠️ 克隆失败 ⋯`；「⋯」三出口 [重试克隆]/[改为空项目]/[删除] | §7.1 `projectGroupState` + §7.2 `ProjectGroupHeader`/`ProjectGroupMenu` variant | ✅ |
| 12d | **失败项目计入上限 50**（P21-6 §9） | 有 failed 项目时，项目总数按含它计算 | §7.1 `projectGroupState`（`countsTowardLimit:true`） | ✅ |
| 12e | **失败项目不可选为当前项目**（P21-6 §9，否定性） | 点其组头**仅展开/折叠**、`selectedProjectId` 不变；点其下任务无效 | §7.2 play 否定断言 + §7.3 failed 不可选 | ✅ |
| 12f | **convert-to-empty 对非 failed 返 409**（§8） | 前端给人话提示，**不裸抛 409**，本地状态不被改动 | §7.1 service ⑤ + §7.3 409 分支 | ✅ |
| 13 | 两条回程语义分离（15 §3.1.1） | `wizardReturn`（回确认步）与 `pendingProjectCreate`（回创建弹层、**携 projectId**）互不复用 | §7.1 uiSlice 项目片段 | ✅ |
| 14 | 组头「⋯」项目菜单（§3.3） | 侧弹层显示名称/状态/任务数/创建时间；**不展示「来源」行**（产品已定，见 §6）；**不改当前工作项目** | §7.2 `ProjectMenuPanel` variant（含"无来源行"否定断言）+ §7.3 组头菜单不改上下文 | ✅ |
| 15 | 删除项目级联（§6/§9） | 二次确认列出「将删除该项目下 N 个 Task 及其数据卷（保留的成果卷除外），不可逆」 | §7.2 三 variant + §7.3 删除级联 | ✅ |
| 16 | 删除含运行中 Task（§6） | 确认文案追加「含 N 个运行中任务将被强制停止」 | §7.2 variant + §7.3 | ✅ |
| 17 | 删除后上下文回落（§7.4） | 当前项目被删 → 自动落到另一个项目，不留空白页 | §7.4 补充场景 4 | ✅ |
| 18 | 首次进入建项目引导（§5） | 「第一步：为你的代码建一个项目」+ 两个 CTA | [F21-1 §7.2](./21-1-工作台.md) + §7.4 补充场景 1 | ✅ |
| 19 | [开空项目] 一键路径（§9） | **跳过弹层**，自动命名「未命名项目 N」；**编号规则产品已定**：取**历史最大 N + 1**、**不复用已删除的号**（避免与已删项目的保留卷/成果 diff/历史日志旧引用混淆）；创建后可改名覆盖 | §7.1 `nextUntitledName`（断言已按新规则改写）+ §7.4 补充场景 1 | ✅ |
| 20 | 深链项目上下文一致（P22 §4.14） | `/?taskId=` 命中其他项目 → 自动切到其所属项目 | §7.4 补充场景 3 | ✅ |
| 21 | 已保留卷入口（§3.3，v1.1） | `GET /api/retained-volumes?projectId=`（审计 P2-5） | — | 🕐 v1.1（但端点已定，见 §9.3） |
| 22 | 重命名（§3.3，v1.1） | 组头「⋯」→ [重命名] | — | 🕐 v1.1 |
| 23 | 归档（§4/§9，v1.1） | 组在树底淡显；**只读范围产品已定**——**不能**：发起新 Task、改项目配置（重命名 v1.1、自动化规则）；**可查看/回放**：已有 Task 终端历史只读回放、成果卷查看、运行历史；**可恢复**：[解档] 回复原状态 | §7.2 归档淡显 variant | 🕐 v1.1（范围已就绪，实现时按此验收） |
| 24 | 凭证/镜像与项目无关（§9） | 项目菜单**不出现**任何凭证/镜像配置入口 | — | ❌ 否定性验收无用例 |
| 25 | 工作区隔离：每 Task 独立副本（§9） | 发起时克隆/复制项目工作区到专属卷 | — | 非前端可验——**标为后端验收项**（前端只在进度卡呈现「准备工作区」阶段） |

### 9.2 关键功能端到端验证脚本

#### VS-1 · 私有仓库 clone 失败到成功的完整回程（跨三页）

| 步 | 操作 | 期望 | 判定通过 |
|---|---|---|---|
| 1 | 树底 [＋ 新建项目] → 填名 + 私有 git URL → [创建] | 后端 **202 返回 `{id, cloneStatus:'cloning'}`**；弹层转克隆进度态；**树中已出现该项目** | 先落库语义（24 §3），不是"成功了才出现" |
| 2 | mock clone 阶段返 403 | 就地显示「克隆失败：无权访问该仓库」+ **[配置 Git 凭证]**；该项目在树中转 `failed` 态 | 权限类与网络类**分支不同**（`classifyCloneError`） |
| 3 | 点 [配置 Git 凭证] | 跳 `/settings/credentials` 的 Git 分区；`pendingProjectCreate` 已置位**且含步骤 1 返回的 `projectId`** | 表单值 + 项目 id 都被暂存 |
| 4 | 配置 HTTPS Token → [测试连接] ✅ → [保存] | 保存成功，只显尾号 | 明文不回显 |
| 5 | 返回 | **创建弹层以原表单值重开**，出现 [重试克隆] | 名称与 URL 未丢 |
| 6 | [重试克隆] → mock 成功 | 发出 **`POST /api/projects/:id/retry-clone`**；项目就绪 + 自动选中 + toast | 回程闭环 |
| 7 | 检查项目列表 | **该项目仍只有一条**（名称/URL 相同的重复项目数 = 0） | **本脚本的防回归核心**：重试若走 re-create 就会出现两条 |
| 8 | 检查网络记录 | 全程 `POST /api/projects` **只发生过 1 次**（步骤 1） | 同上，从请求侧再确认一次 |
| **失败路径** | 步骤 2 改为网络类错误 | 显示「仓库不可访问：检查 URL 或网络」+ [重试]/[改为空项目] | **不出现** [配置 Git 凭证]；[重试] 同样走 retry-clone |
| **失败路径** | 步骤 5 时用户直接关闭弹层 | `pendingProjectCreate` 清空；**但该项目仍以 `failed` 态留在树中**（先落库），不会凭空消失 | 暂存不残留 ≠ 项目不残留，两者别混 |
| **失败路径** | retry-clone 再次失败 | 仍是同一个项目转回 `failed`，可继续重试 | 重试次数不产生新项目 |

#### VS-2 · 删除项目的级联确认

| 步 | 操作 | 期望 | 判定通过 |
|---|---|---|---|
| 1 | 组头「⋯」→ 侧弹层 | 显示项目详情；**当前工作项目未变** | `selectedProjectForMenu ≠ selectedProjectId` 的语义分离 |
| 2 | [删除]（该项目有 5 个 Task，其中 2 个运行中） | 确认文案含「将删除该项目下 5 个 Task 及其数据卷（保留的成果卷除外）」**且**「含 2 个运行中任务将被强制停止」 | 两句都在 |
| 3 | 确认 | 树中该组消失；其 Task 不残留在任何组 | `selectProjectTaskTree` 无孤儿 |
| 4 | 若删的是当前项目 | 上下文自动落到另一项目 | 不出现空白页 |
| **失败路径** | 删除请求 500 | 组仍在 + 错误 toast | 无乐观删除 |

### 9.3 覆盖缺口汇总（本页 5 项）

> 上一轮的两个 ❌（#12b [改为空项目] 端点未定、#12c 失败项目树内形态未定）**已随 convert-to-empty 三层补充闭合**，转 ✅。

| 缺口 | 影响 | 建议补法 |
|---|---|---|
| **#10 clone 30min 硬超时** | 超时不处理 → 弹层永远转圈 | 假时钟集成用例：推进 30min → 转失败 + [重试]（重试仍走 retry-clone） |
| **#5 项目上限 50 的 UI** | 纯函数返回 `atLimit` 了，但界面表现无人验（也未定义，见 §9.4） | 待产品定 UI 后补 story |
| **#9 clone >10min 提示** | 只有静态 variant | 假时钟集成用例 |
| **#24 凭证/镜像与项目无关** | 否定性验收：项目菜单里多个"项目级镜像配置"就破了全局语义 | story play 否定断言 |
| **#21 已保留卷（v1.1）** | 端点已随审计 P2-5 定案，但 UI 未实现 | v1.1 实现时按 §9.1 第 21 行补 |

### 9.4 产品侧待补验收标准（本页 0 项）

> **本页六条已全部闭合**（分两轮）：
> **第一轮**：失败项目的树内形态、[改为空项目] 语义、转空后的来源呈现（定为**不展示「来源」行**）。
> **第二轮**：① **项目上限 50 触达 UI**——P21-6 §9：`[＋ 新建项目]` 置灰 + tooltip「已达 50 个项目上限，请先删除部分项目」，failed 项目也占名额；② **「未命名项目 N」编号**——P21-6 §9：取**历史最大 N+1、不复用已删号**（理由：避免与已删项目的保留卷/成果 diff/历史日志旧引用混淆）；③ **归档只读范围**——P21-6 §9：不能发 Task/改配置，可只读回放终端历史与成果卷，可 [解档] 复原。
> 三条已分别落进 §9.1 #5 / #19 / #23，并连带改写了 §7.1 `nextUntitledName` 里原先「取 2 还是 4 需与实现一致」的模糊断言。
