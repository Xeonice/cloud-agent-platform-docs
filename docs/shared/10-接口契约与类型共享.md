# 10 - 前后端接口契约与类型共享（独立仓库前提）

> 状态：✅ 可评审（后端侧确认 @nestjs/swagger 11.4.x 暴露 /openapi.json，双侧 CI 均做 openapi.json diff 校验，见文档 02 §8 与 09 §2.3；**§6 端点全量清单**与 §3 WS 事件已按产品定稿 P20/P21/P22 同步）
> 关联文档：[02 MCP 与 OpenAPI 双协议](../backend/02-MCP与OpenAPI双协议.md) · [07 前端目录结构](../frontend/07-前端目录结构与视图逻辑分离.md) · [03 调度中心](../backend/03-Sandbox调度中心.md) · [05 鉴权流转](../backend/05-Runtime鉴权流转.md) · [06 TTY 链路](../backend/06-TTY终端链路.md)

## 1. 结论先行

| 契约类型 | 方案 | 同步方式 |
|---|---|---|
| REST | `openapi-typescript`（生成纯类型）+ `openapi-fetch`（类型安全 client） | **生成后提交** + CI 漂移检测门禁 |
| WebSocket | 手写 `ws-protocol.ts` 双端同步 + **运行时 zod/valibot 校验兜底** | 手动同步 + CI hash 比对感知变更（文档 14 §2.5 方案 B）；改动频繁时升级为独立协议小包（方案 A） |
| 不采用 | 发布 npm 类型包 | 作为规模扩大后的平滑演进路径预留 |

## 2. REST 契约链路

```
NestJS (@nestjs/swagger)
      │  暴露 /openapi.json
      ▼
前端仓库: pnpm generate:api
      │  openapi-typescript https://backend/openapi.json
      │    -o src/types/generated/openapi.d.ts
      ▼
services/api/client.ts
      │  openapi-fetch 基于该类型创建强类型 client
      ▼
业务代码: client.GET('/api/sandboxes/{id}')  ← 天然类型推导，无需手写 DTO
          （后端设 setGlobalPrefix('api')，openapi paths 含 /api 前缀，见文档 02 §8）
```

选型理由（2026-08 现状）：

- `openapi-typescript`：约 11k star / 50 万周下载，生成**纯类型**（`.d.ts`），零运行时开销；被 TanStack Query 官方文档列为推荐范式之一。
- `openapi-fetch`：与上者配套，无代码生成的运行时薄封装。
- 备选 **Orval**（约 7k star / 35 万周下载）：可直接生成 React Query hooks + MSW mock。需要"开箱即用 hooks + mock"时再评估切换；当前推荐 openapi-fetch 保持数据层显式。

### 2.1 CI 契约漂移检测（关键门禁）

防止"后端契约变化被前端悄悄吃掉"：

```yaml
# static-checks job 内的一步（PR 硬门禁——与文档 09 §1.3、16 §6 的流水线一致）
- run: pnpm generate:api              # 重新从后端 /openapi.json 生成
- run: git diff --exit-code src/types/generated/
  # 有差异 → CI fail → 人工确认并提交更新
```

契约变更因此会出现在 PR diff 中被 review，比 npm 包版本号更新更透明。

### 2.2 为什么不发 npm 类型包

- 双仓库物理隔离，发包意味着版本号对齐、私有 registry、联调时 `npm link`/`file:` hack，对"简易管理入口"级项目工程成本偏重。
- "生成后提交"同样强类型，且改动可 review。
- **演进路径**：若未来多个前端消费同一后端契约，再升级为发布 `@org/api-types`（仅含生成 `.d.ts`，无运行时代码），与现方案不冲突。

## 3. WebSocket 契约

WS 没有 OpenAPI 级别的成熟工具链（AsyncAPI 生态尚不无缝），采用**最低工程成本 + 运行时兜底**：

1. **帧/事件类型的唯一 canonical 定义在 [§7.4](#74-ws-帧类型)**（`TerminalClientFrame`/`TerminalServerFrame` + `SandboxEvent`）——本节此前重复给过一份已删除（交叉评审 P0-1：与 §7.4 冲突过，判别键/字段名/base64/exit/clone phase 都不一致）。后端仓库以 discriminated union 定义、双端各持一份 `ws-protocol.ts`。
2. 双端文件顶部加 `// SYNC WITH shared/10 §7.4 — 修改需双端确认` 注释。
3. **前端对收到的每帧做 zod/valibot `parse`**——类型意外漂移时开发环境立刻抛错，而不是 UI 静默 undefined。
4. **变更感知**：后端 CI 对协议文件算 hash 写入 release asset，前端定时 job 比对，不一致即失败提醒人工同步（文档 14 §2.5 方案 B）；协议改动趋于频繁时升级为独立 npm 协议小包 + Renovate 自动开 PR（方案 A，仅含类型与 zod schema、无运行时代码——与本文档"REST 不发包"的结论不冲突：REST 有 codegen 这条更优路径，WS 没有）。

### 3.1 事件语义表（前端 patch 行为的权威）

| 事件 | 触发时机 | 前端处置 | 后端落点 |
|---|---|---|---|
| `sandbox.created` | 创建请求受理、pending 记录落库后 | 列表插入（或对齐乐观项） | 03 §4 |
| `sandbox.status_changed` | **每一次**状态机转移（含新增的 `preparing-workspace`） | patch 列表项状态 + 驱动阶段进度卡；**`status:'failed'` 时读 `errorCode` 出 P22 §1 人话**，无码才退化成兜底文案 | 03 §4，事务内双写后经 Outbox 投递（13 §3） |
| `sandbox.removed` | destroyed 终态 | 移除列表项 | 03 §4 |
| **`sandbox.waiting_input`** | `running` 子态**翻转时**（不周期性重发） | 列表项切 🔵 等待输入 图标；**仅展示，不改主状态** | 06 §8 网关内存态；判定与误报纪律见 03 §4.1 |
| **`project.clone_progress`** | 异步 clone 期间节流 **1s** 一帧；`phase:'slow'` 在超 10min 时补发一帧。带 `stage`/对象数/速率（03 §7.2★） | 创建弹层进度态；`slow` 出提示、`failed` 就地红字按 `errorCode` 分支引导 | 03 §7.2 |
| `runtime-auth.status_changed` | 凭证过期/吊销/重授权成功 | invalidate `['runtime-auth']` + 横幅 | 05 §5 |
| **`runtime.install_progress`** | sandbox 的 `starting` 段里装 runtime CLI 时，`runtime_installations.status` **每次变更**（`not_installed → installing → installed \| failed`） | 进度卡「启动实例」格子下补一行子文案（「正在安装 claude-code…」）；`failed` 时不单独处置——紧随其后的 `sandbox.status_changed → failed` 才是权威 | 03 §4.3 ③；源事件 `RuntimeInstallationStateChanged`（23 §12），走 Outbox |

两条纪律：

- **`runtime.install_progress` 为什么要单开一条、不复用 `sandbox.status_changed`（S5，[TASK-LAUNCH-DECISIONS](../TASK-LAUNCH-DECISIONS.md) T-3）**：装 CLI 期间 `sandbox.status` 恒为 `starting`，而实测在 AIO 默认镜像上现装 `claude-code` 要 **753 秒（12.5 分钟）**（04 §3 ★1）——没有这条事件，前端只能盯着「启动实例」格子干等 12 分钟。复用 `sandbox.status_changed` 则会推出一串「状态没变的状态变更事件」，破坏本表第 2 行「**每一次**状态机转移」的语义与前端的 patch 行为。它**进 Outbox**（源事件本就是 ✅ Outbox，23 §7.5），因为漏一帧会让进度卡永久停在旧文案。
- **异步失败的错误码有且只有两条出口，两条都必须留（S5 前端反馈）**：provision 在 202 之后才可能失败，**没有同步响应可承载错误码**（04 §4）。① **即时**：`sandbox.status_changed` 在 `status:'failed'` 时带 `errorCode`；② **刷新后可恢复**：`SandboxDto.failureCode` / `failureMessage`（§7.3，源 `sandboxes.failure_code`，13 §2.1.1）。**缺 ① 前端要等下一次轮询才知道原因，缺 ② 用户刷新一次原因就永久消失**。⚠️ 不要以为 `runtime.install_progress` 能兜底——它只覆盖装 CLI 那一段，**`IMAGE_CONTRACT_VIOLATION`（镜像缺 tmux）根本不经过它**，没有 ①② 就只剩兜底人话。`errorCode` 是**码不是句子**：人话由前端按 P22 §1 查表出。
- **`sandbox.waiting_input` 与 `project.clone_progress` 都是"可丢失"事件**——前者丢了下次翻转会重发（且有 REST 派生字段 `waitingInput` 兜底），后者丢了不影响最终 `clone_status`（REST 可查）。因此**都不进 domain_events Outbox**，直接由网关/编排器推送；进 Outbox 的只有状态类事件（需要可靠投递与回放）。
- 高频事件必须节流：clone 进度 1s、waiting_input 只在翻转推——WS 通道与终端数据流共用连接预算，事件洪水会挤压终端体验。

## 4. 与 sandbox 列表更新的配合

- WS 事件通道推送 `sandbox.*` 事件 → 前端 `queryClient.setQueryData` patch 缓存（推送为主）。
- TanStack Query 保留 30–60s 低频轮询兜底（WS 断线期间防陈旧）。

## 5. 风险与备选

| 风险 | 等级 | 缓解 |
|---|---|---|
| 生成后提交模式下前端未及时重新生成 | 中 | CI 漂移检测作为强制门禁（§2.1），不依赖自觉 |
| WS 手动同步漂移 | 中 | 运行时 schema 校验兜底 + artifact 拉取增强 |
| 后端 OpenAPI 注解不全导致生成类型缺失 | 中 | 后端引入 `eslint-plugin-nestjs-typed` 校验 @ApiProperty 与字段类型对齐（文档 14 §8） |

## 6. REST 端点全量清单（前后端唯一权威副本）

> 生成物（`openapi.d.ts`）是类型的权威，本表是**路径与语义**的权威——前端写 service 方法、后端建 controller、Playwright 建 mock（12 §4.2）都以此对表。定义细节见各后端文档，此处只做全量索引。所有路径含全局前缀 `/api`。

> ## ⏳ 的含义：**设计已定、代码里还没有**（2026-08-31 全表核实）
>
> 本节表格里带 ⏳ 的路径，是**对 `api/openapi.json` 逐条核实后**确认尚未实现的。此前它们
> 与已落地端点**混排且无任何标记**，代价是真的付出去了：`02 §5.2` 据此写下「`start_sandbox`
> / `stop_sandbox` —— REST 已有，MCP 壳待补」，而那两个端点**从来就不存在**，于是一条本该
> 是「先做端点」的工作被登记成了「补个壳」。
>
> ⚠️ **`docs:check` 抓不到这类漂移**，这是**口径决定的**，不是 bug：B1 的判据是
> 「`10 §6` ⊇ `openapi.json`」——**单向**。文档里多躺一条代码没有的，门禁放行（就是为了
> 允许写规划中的端点）；A4 只比对 `27 §2–§9` 与本节两份**文档之间**是否相等，两边一起
> 错它同样是绿的。⇒ **⏳ 是这条边界上唯一的人工信号，加端点时请顺手维护**。
> 对照：A5 对错误码做的是**双向**对账，所以「表里躺着一个不存在的码」会被当场拦下。
>
> | ⏳ 分组 | 端点 | 归属 |
> |---|---|---|
> | ~~沙箱生命周期~~ | ~~`POST /sandboxes/:id/start` · `/stop` · `/exec`~~ | **✅ 已落地**（本迭代，连同 `start_sandbox`/`stop_sandbox`/`get_sandbox`/`exec_in_sandbox` 四个 MCP tool） |
> | ~~访问口令**设置**~~ | ~~`PUT /system/access-passcode`~~ | **✅ 已落地**（本迭代。口令从此存在 `system_settings.access_passcode_hash`；`ACCESS_PASSCODE` env 仍然优先，且它非空时本端点整个 409） |
> | ~~保留卷~~ | ~~`GET /retained-volumes` · `DELETE /retained-volumes/:id` · `GET /retained-volumes/:id/archive`~~ | **✅ 已落地（本轮，v1.1 提前做）** —— 上一版这里写的是「缺的**不是端点是整条切片**」，那份清单现在逐条结清：`retained_volumes` 表（migration `0017`）、`RetainedVolume` 聚合（I-RV-1/2/3）、`RetainedVolumeRepository`、`RegisterRetainedVolumeCommand`（经 `ProjectFacade`，由 `destroy(keepVolume:true)` 调用 —— 此前它**只写文件系统标记、一条记录都不登记**）、`VolumeReaper`（到期删卷 + 置 `deletedAt`）。⛔ **裁掉的那处设计按 23 §6.2 定**：`DELETE`/下载的 `:id` 是聚合自己的 `RetainedVolumeId`（uuid v7），**不是** `sandboxId`；`workspacePath` 留在库里做 UNIQUE（I-RV-3），**不进 DTO**。⛔ **「恢复」本轮仍不做** —— 语义没裁（P20 §6） |
> | ~~自动化（7 行 / 11 个端点）~~ | ~~`/projects/:id/automations`、`/automations/*`~~ | **✅ 后端整块已落地（F21-7，2026-08-31）**：`@platform/automation` 模块（四层）、`automations` + `automation_runs` 两张表（migration `0018`）、`AutomationScheduler`。⚠️ **调度器不是 `@Cron`**：`setInterval` + `unref()`，与 `VolumeReaper` 同款。⏳ **前端路由仍未做** —— 端点这一侧的 ⏳ 已摘，前端那一半不在本条的判据里（B6 只看 openapi） |
> | v1.5 占位 | `/system/backup` · `/system/version` | 原就标注为占位 |
>
> ##### 保留卷的打包口径（2026-08-31 定案）
>
> | 决定 | 为什么 |
> |---|---|
> | **只给整包，不做 diff** | agent 会自己 `git commit`（跑的就是 Claude Code / Codex），那时 `git diff` 是空的 ⇒ 用户下到 0 字节的「成果」；空项目连基线都没有。见 P20 §6 |
> | **按 git 口径挑内容**：已跟踪 + 未跟踪且未被 ignore，`.gitignore` 命中的一律不打包；**`.git` 保留** | 实测本仓 web 工作区 **1.0 GB → 14 MB（砍掉 98.6%）**，`node_modules` 725 MB + `.next` 302 MB 全是可再生的。⚠️ 真正的体积杀手是这条排除，**不是压缩**。`.git` 保留是因为没它就丢了全部历史，「拿走继续用」这个意图不成立 |
> | **tar，不压缩** | gzip 后的大小**只有真压完才知道**（内容熵决定），而响应头必须先发 ⇒ 边压边传就给不出 `Content-Length` ⇒ 浏览器进度条显示"未知大小"。tar 的大小是**确定的算术**（文件内容 + 512 字节块头 + padding），可精确预计算 ⇒ `Content-Length` 给得出，**浏览器原生进度条直接可用、前端零代码**。代价实测只有 9 MB（14 MB vs 4.8 MB），局域网内不值得为它换掉进度 |
> | **两个大小都存都显示** | 同一个卷磁盘占 1.0 GB、下载包 14 MB，**差 70 倍**。只显示一个必然误导：显示 14 MB 会让人以为清理只能拿回 14 MB（实际 1 GB），显示 1.0 GB 又与下载量对不上。⇒ `RetainedVolumeDto` 给 `diskBytes` + `downloadBytes` 两个字段 |
> | **与 P21-8 §4 的备份共用同一套打包机制** | 那边是「SQLite + 可选卷压缩 → 下载」，同样要流式、同样要判磁盘水位。各写一套会有两个打包路径、两处水位判断，以后修一个漏一个 |
>
> ⚠️ **打包前必须判磁盘**，判据与 `WorkspacePreparer.assertDiskSpace()` 同类——磁盘是本项目的真实瓶颈（03 §1）。
> ⛔ **三者不可兼得**：流式直出 + 压缩 + 精确进度，没有任何实现能同时做到（压缩后大小是信息论意义上压完才知道的）。文档里不要写成那样。
>
> ⚠️ **`sandbox.health` 分层探针不在这张表里**（它是能力不是端点），但同属「设计已定、代码里还没有」，
> **✅ 已落地（本轮，v1.1 提前做）。** 上一版记的两道硬门槛，一道**不成立**、一道**已验证**：
>
> ① ~~「要加第 13 个状态」~~ —— **误判，已在 `03 §7.8` 修正**。契约里 `lifecycleState`
>    （生命周期）与 `health`（健康度）**从一开始就是分开的**
>    （`packages/contracts/src/sandbox-provider.contract.ts` 的 `HealthState` /
>    `HealthStatus` / `SandboxRuntimeStatus.health`），翻转的是 `health.state`，
>    `SANDBOX_STATUSES` 那 12 个取值**一个都没动**，也没有任何跨仓枚举变更。
>    对外新增的只是 `SandboxDto.health?`（**可选**）：老客户端读不到它时行为与今天完全一致。
>    依据 K8s [#7856](https://github.com/kubernetes/kubernetes/issues/7856)「Enums aren't
>    extensible」+ 本节自己那句「`unhealthy` ⇏ agent 不可用」——一个连「不可用」都推不出来的
>    信号没资格决定生命周期。
>
> ② **「只能对着活的微 VM / 容器验证」这一条被当真了。** boxlite 侧有一条**真微 VM** 的
>    e2e（`apps/api/test/e2e/boxlite-provider.e2e-spec.ts`「inspect() 在真微 VM 上填出
>    health」）：跑着的 box 有 `health`、`metrics().execErrorsTotal` 真的读到、`stop()`
>    之后零成本层就答得出 `unhealthy`，全程不进沙箱。⚠️ **实测顺带纠正了一个设计假设**：
>    `agent-infra/sandbox:latest` 的 `healthStatus.state === 'None'`（这张镜像根本没配
>    health check），所以零成本层的正面证据只有「VM 在跑」+「`execErrorsTotal` 没涨」
>    ⇒ 平台**不把「没有异常迹象」写成 `healthy`**，那样是替沙箱担保一件没人问过的事；
>    没有正面信号时是 `unknown`，`healthy` 要么由 provider 明确报出、要么由数据面确认挣来。
>
> ⚠️ **aio 那一侧本机验证不了**（AIO 镜像不在本机，相关 e2e 全部 skip）：它的判据
>    （8080 探活 + `State.Health` 仅作诊断详情）目前只有纯函数单测
>    （`provider-health-mapping.spec.ts`）覆盖映射逻辑，**「那扇门确实是 8080」这件事没有
>    活容器背书**。⛔ 别把它当成已验证。
>
> ⚠️ **本轮没有新增 WS 帧**：翻转经审计流（`sandbox.health`）与 `SandboxDto.health` 出线。
>    加一个 `sandbox.health` WS 事件要动 `WS_PROTOCOL_CANONICAL`（B4 跨仓对账）与前端的
>    那一份手抄，属于独立的一次跨仓变更。

### 6.1 sandbox（Task）

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | `/api/sandboxes?projectId=&status=` | 响应含派生字段 `waitingInput`（取数经 terminal 只读端口，P1-12） | 02 §5.1 |
| GET | `/api/sandboxes/:id` | | 02 §5.1 |
| POST | `/api/sandboxes` | **202** + pending 记录；`timeoutMinutes` 缺省=120（headless）/ 传了即 400（交互式），P1-13；可选 **`require`** 能力静态校验，不满足 → **409 `UNSUPPORTED_CAPABILITY`**（§7.3 / 04 §5） | 02 §5.1 |
| POST | `/api/sandboxes/:id/start` · `/stop` | **200** + `SandboxDto`。`start` 只接受 `stopped`（I-SBX-1 里进入 `starting` 的唯一静息边），答的是**已受理**：DTO 回 `starting`，其后的进度走 WS。`stop` 只接受 `running`/`idle`，**保留实例与工作区**（否则 `start` 无物可启）。两者均非法态 → **409 `INVALID_STATE`** | 02 §5.1 |
| DELETE | `/api/sandboxes/:id` | **`{ keepVolume?: boolean }`**（body 或 `?keepVolume=`，等价） | 02 §5.1 / 03 §7.7 |
| POST | `/api/sandboxes/:id/exec` | `{ command }` → **200** `{ stdout, stderr, exitCode }`。**非零退出是结果不是错误**（否则 `exitCode` 只会是 0）。仅 `running`/`idle`（I-SBX-3）→ 否则 409 `INVALID_STATE`；**60s 平台侧硬期限** → 504 `TIMEOUT`。⚠️ `stderr` 今天恒为空串：`ProcessStream` 是单条已解复用字节流（04 §2.4），拆不回来，要分流请在命令里 `2>&1` | 02 §5.2 |
| POST | `/api/sandboxes/:id/runtimes/:rt/tasks` | 无头任务。**202** + `AgentTaskDto`（`prompt` ≤8000、`timeoutMinutes` ∈ {30,60,120,240}、`resumeFrom?` 续接上一轮、`extraArgs?` **白名单枚举**）。provider 不支持 `headlessTask` → **409 `UNSUPPORTED_CAPABILITY`**；输出流走 WS `/tasks`（§6.7 / §7.4） | 02 §5.2 / 27 §2 |
| GET | `/api/sandboxes/:id/tasks` | 该沙箱的历史任务，按 `startedAt` **倒序**。刷新后前端靠它拿回「刚才在跟哪条流」——本地存的 taskId 是猜测，这里才是权威 | 27 §2 |
| GET | `/api/sandboxes/:id/tasks/:taskId` | `AgentTaskDto`（含 `timeoutMinutes`，倒计时用）。⚠️ `lastSeq` 是**体检上界**，**不是** `fromSeq`（后者排他，填它会一条都回放不到，§7.4） | 27 §2 |
| GET | `/api/sandboxes/:id/tasks/:taskId/artifacts/:name` | 产物流式下载（`application/octet-stream`）。`:name` 是**产物目录内的相对路径**（含 `/` 时 percent-encode）；绝对路径与 `..` 段 → **400 `INVALID_ARTIFACT_NAME`** | 27 §2 |
| POST | `/api/sandboxes/:id/tasks/:taskId/cancel` | 终止在跑的任务，**202**（两阶段 SIGTERM→5s→SIGKILL）。终态 `killed` 随 WS `/tasks` 的 `exit` 帧到达，不在响应里；已终态 → **409** | 27 §2 / 03 §8.3 |
| GET | **`/api/providers`** | **能力发现**：返回 `ProviderDto[]`（**扁平数组**，非信封）——`{ name, capabilities(7 位全量), isDefault }`。**registry 驱动**：第三方经 04 §8 方式一 `register()` 注册后自动出现，无需改任何代码。前端据此渲染 provider 选项与按能力显隐控件（**不再有前端闭集**）。**不进 MCP**（27 §2） | 04 §5 |

> ⚠️ **`GET /api/providers` 与 §6.6 的 `GET /api/system/providers` 是两个端点，别混用**：前者是 **sandbox 上下文**的能力发现（已落地，驱动"建 Task 选 provider"这条业务链路，只出 `name/capabilities/isDefault`）；后者是**平台运维看板**（尚未落地，范围更宽——provider **+ runtime + imageSpec** + 健康/失败率 + 最近 testkit 结果，供 P21-5 系统状态页）。两者查询 key 也不同（前端 15 §2.1：`['providers','list']` vs `['system','providers']`）。

### 6.2 project

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | `/api/projects` | 含各项目 Task 数聚合 + `cloneStatus` | 02 §5.1 |
| POST | `/api/projects` | git **202 异步**，进度经 WS | 03 §7.2 |
| GET | `/api/projects/:id` | 含 clone 失败的 `errorCode` | 03 §7.2 |
| POST | **`/api/projects/:id/retry-clone`** | 重试克隆（`failed`→`cloning` 显式重置）；非 failed 态 → 409 | 02 §5.1 / 03 §7.2 |
| POST | **`/api/projects/:id/convert-to-empty`** | 改为空项目（`failed`→`ready`，转 `sourceType='empty'`、删半成品基线目录、**保留 id/名称/已关联 Task**）；非 failed 态 → 409 | 02 §5.1 / 03 §7.2 |
| POST | **`/api/projects/:id/cancel-clone`** | **只取消克隆、不删项目**：中止在跑的 clone（排队中的直接出队），`cloning`→`failed` + `errorCode:'INTERRUPTED'`，项目 id/名称/已关联 Task 全部保留，用户可继续 [重试克隆] 或 [改为空项目]。非 cloning 态是 no-op（返回当前 `ProjectDto`，不报错）。**与 `DELETE` 的区别**：DELETE 也会中止克隆，但连项目一起删 | 03 §7.2 |
| GET | **`/api/projects/:id/branches`** | 列出基线里的分支（**读本地引用，不碰网络、不需要凭证**——完整克隆的直接红利，03 §7.2★）。空项目 / `cloning` / `failed` 态 → 空数组（failed 的半成品基线目录已被 `rm -rf`） | 03 §7.2 |
| POST | **`/api/projects/:id/sync`** | 同步基线：`git fetch --all` + 刷新 `baselineSizeBytes`/`updatedAt`。**已有 Task 的工作区一律不动**（它们是当时的副本）。非 `ready` 态 → 409 | 03 §7.2 |
| DELETE | `/api/projects/:id` | 级联；cloning 态=先取消克隆再删（**要"取消但保留项目"用上一行的 `cancel-clone`**） | 03 §7.2 |
| GET | `/api/retained-volumes?projectId=` · DELETE `/api/retained-volumes/:id` | 已保留卷（统一资源前缀，P2-5）。列表**不含已清理的**；DELETE 先删目录再置 `deletedAt`，记录留档（I-RV-2） | 03 §7.7 |
| GET | `/api/retained-volumes/:id/archive` | 打包下载整个工作区卷 —— **tar 流** + 精确 `Content-Length`，口径见下 | 03 §7.7 |

### 6.3 runtime / 凭证（**全部无 sandbox 维度**——05 §2 决策 A）

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | **`/api/runtimes`** | 列表 + 卡片元数据 + **凭证状态聚合** | 02 §5.1 |
| GET | `/api/runtimes/:rt/credentials/status` | 单 runtime 状态（永不回明文） | 05 §3 |
| POST | `/api/runtimes/:rt/auth/begin` | 在 auth helper 内起登录 | 05 §3 |
| GET | `/api/runtimes/:rt/auth/status?challengeRef=` | device-code 轮询 | 05 §3 |
| POST | `/api/runtimes/:rt/auth/complete` | setup-token 粘贴 | 05 §3 |
| POST | `/api/runtimes/:rt/credentials/secret` | API key 直存 | 05 §3.1 |
| PUT | `/api/runtimes/:rt/auth-mode` | 模式二选一；目标未配置 → 409 | 05 §4 |
| DELETE | `/api/runtimes/:rt/credentials/:credentialId` | 吊销 | 05 §4 |
| GET | **`/api/credentials?kind=git`** | Git 凭证掩码列表（SSH 只回指纹 / token 只回尾号）。**`kind` 必填，MVP 只接受 `git`**——runtime 凭证的读取归 `GET /api/runtimes`，避免两条可达路径 | 02 §5.1 |
| POST | `/api/credentials/git` | `{ type, secret, platform?, allowedHosts }`；`https-token` 必带 host 白名单（≥1，C 裁决）；同协议已有则按"更换"语义（同一 `UnitOfWork`，I4）；passphrase 私钥拒绝 | 02 §5.1 / 05 §3.2 |
| POST | `/api/credentials/git/test` | `ls-remote` 测试（15s），**不回 ref 名**；body 判别联合 `{source:'inline',...}`（存前测未入库密钥）\| `{source:'stored',credentialId,...}`（卡片测） | 03 §7.4 |
| DELETE | `/api/credentials/git/:id` | 吊销；**无联动清除**（Git 凭证不注入 sandbox） | 05 §3.2 |

> ⚠️ **历史路径 `POST /api/sandboxes/:id/runtimes/:rt/auth/*` 已废弃**，任何仍按"选一个 running sandbox 作登录宿主"实现的前端代码需一并改（决策 A）。

### 6.4 image

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET / POST | `/api/images` | 列表 / 注册 | 04 §7 |
| POST | **`/api/images/validate`** | **注册前预检**，不落库（前端在用的就是它，P1-3） | 02 §5.1 |
| POST | `/api/images/:id/validate` | 已注册镜像重新验证，写回 `validationStatus` | 04 §7 |
| DELETE | **`/api/images/:id`** | 硬删除；有引用或预置镜像 → 409（P1-6） | 02 §5.1 |
| PATCH | **`/api/images/:id { isActive?, imageConfig? }`** | 修改 manifest 可变字段的**唯一入口**：禁用 + 运行参数 env/cmdOverride；env 违规一律 400 + 逐项 `path`（`ENV_NAME_INVALID` / `ENV_NAME_RESERVED` / `ENV_LIMIT_EXCEEDED` / `ENV_DUPLICATE_KEY`）。⚠️ **`isActive` 只收 `false`**，`true` → 400 指向 `activate`（理由见下） | 02 §5.1 · 校验权威 05 §4.1 |
| POST | **`/api/images/:id/activate`** | **把这一行设为该 tag 的当前版本**——同时承担 [更新到新版本] 与 [回滚到旧版本]，一个事务里停掉同 tag 的现任、启用这一行 | 13 §2.4.2 ★ |
| POST | **`/api/images/:id/check-update`** | 重解 tag 比对 digest，**不落库**；digest 形态的 ref 不适用 → 409 | pages/21-4 §6 |


> ★ **为什么「启用」不能走 PATCH，而「禁用」可以（2026-08）**
>
> 判据是**要不要换指针**，不是对称性：
>
> - **禁用**是单行操作——把这一行标成不可选，不影响任何别的行。PATCH 天然合适。
> - **启用**在 `unique(image_id, version) WHERE is_active` 之下**必然要停掉同 tag 的现任**。
>   那是一个两行的事务，语义是「换」不是「加」。用 PATCH 表达它，读起来像在改一个字段，
>   实际会让另一行悄悄下线——**副作用大于字面**。
>
> 所以 `PATCH {isActive:true}` 直接 400，`message` 指向 `POST /api/images/:id/activate`。
> ⚠️ 一个动作只有一个入口：`activate` 同时是「更新到新版本」和「回滚到旧版本」，
> 两者在实现上本来就是同一件事（换当前指针），只是方向不同。分成两个端点会让
> 「回滚」看起来是个特殊操作，而它不是。

> **✅ 本小节八个端点全部已实现**（2026-08 镜像管理切片；本块此前写的是「一个都还没有实现」，那句话现在是反的）：
> `api/openapi.json` 里是 **6 条 path / 8 个 operation**，与上表逐条对得上，
> B1（`openapi 的 image path ⊆ 本表`）因此是**真在比**而不是空过。
> 后端有 `packages/modules/image/`，`images` / `image_manifests` 两张表建于 `drizzle/0010`。
> 表里那四个 `ENV_*` 码仍是 `details[].code`、顶层码仍是 `VALIDATION_FAILED`——
> 它们连同另外几个镜像码**已从「没有产出方」小节搬进 §6.8 主表**，现在参与 A5 对账（60 = 60）。
>
> ⚠️ **一条落地时才出现的口径差**：`POST /api/images/:id/validate` 的响应比上表写的宽，
> 是 `ValidationOutcome` **加** `currentDigest` / `upstreamDigest` / `digestChanged` 三个字段
> （`RevalidateOutcomeSchema`）——「digest 变了要说出来」这条要求需要它们才落得下（27 §6）。
> **落地状态的权威在 04 §7 / 13 §2.4 / 27 §6。**

### 6.5 automation（v1.1）

> **✅ 11 个端点整块落地（F21-7，2026-08-31）。** 后端有了 `@platform/automation`
> 模块（四层齐全）、两张表（migration `0018`）、`AutomationScheduler`（每分钟扫、
> `async-mutex` 串行、先推进后执行、outcome-pending 补扫）。⛔ **全部不进 MCP**（27 §11.3）。
>
> ⚠️ **没有用 `@nestjs/schedule`/`@Cron`**（上一版这里预期它）：`setInterval` +
> `timer.unref()` 与仓里另外两个后台循环（`VolumeReaper` / `CredentialRefreshScanner`）
> 同款，为一个每分钟一次的循环引入一个新的调度框架不划算。

| 方法 | 路径 | 定义处 |
|---|---|---|
| GET / POST | `/api/projects/:id/automations` | P21-7 §8 |
| GET / PUT / DELETE | `/api/automations/:id` | P21-7 §8 |
| POST | `/api/automations/:id/enable` · `/disable` | 03 §8.4 |
| GET | `/api/automations/:id/runs`（分页）· `/api/automations/runs/:runId` | 03 §8 |
| GET | `/api/automations/runs/:runId/logs?offset=&limit=` | 03 §8.6 |
| POST | `/api/automations/webhook-test { url }` | 03 §8.5 |

⚠️ **`PUT /api/automations/:id` 的 `timezone` 缺席 = 原样保留**（I-AUT-9）。前端**只在
用户显式改时区时**才传这个字段；顺手把当前浏览器时区再传一遍，会让一条「每天凌晨 3 点」
的规则在用户换台机器之后挪走 8 小时（03 §8.1「最难排查的一类 bug」）。

⚠️ **`GET .../runs/:runId/logs` 回的是 `text/plain` 原始字节**，分页游标走响应头
`x-log-offset` / `x-log-total` / `x-log-eof`；`offset` 缺席即回**末尾 `limit` 字节**
（默认 64KB，上限 1MB）。

⚠️ **`POST /api/automations/webhook-test` 恒 200**，成功与失败都在 body 的 `ok` 里 ——
它的调用方是一个想知道「地址通不通」的表单，把对端的 502 翻译成本接口的 502 只会让
前端去解释一个不是它的失败。`errorCode` 复用既有四码（`VALIDATION_FAILED` /
`HOST_NOT_ALLOWED` / `TIMEOUT` / `UPSTREAM_UNAVAILABLE`），不新造。

### 6.6 system

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | **`/api/system/init-status`** | 冷启动首屏据此决定是否进初始化向导；附上次出网检测结果 | 02 §5.1 |
| POST | **`/api/system/init`** | `{ proxyConfig?, acknowledgeOffline? }` → 写 `initialized=true`；**一次性操作，已初始化返 409 `ALREADY_INITIALIZED`**（不是幂等，P2-4）；**模型 API 全挂且未带 `acknowledgeOffline` 返 409 `OFFLINE_NOT_ACKNOWLEDGED`**（⚠️ 两种 409 **两个码**，处置相反，见 §6.8）。出网检测复用 `/diagnose` | 02 §5.1 / P21-8 §2 |
| GET / PUT | `/api/system/settings` | 运行期读改代理等；**永不回显口令 hash** | 13 §2 / 11 §3.1 |
| PUT | **`/api/system/access-passcode`**（**MVP**，P0-3） | `{ action: enable\|regenerate\|disable }` → **200** `{ enabled, passcode? }`。`enable`/`regenerate` **一次性返回 16 位明文**（`passcode` 键仅这两种动作出现，`disable` 时整个缺席），此后只存 hash；**不影响已通过 session**。⚠️ **三种 409 `INVALID_STATE`**：① 已启用再 `enable`（要换口令用 `regenerate`——`enable` 幂等化会让一次误发静默轮换掉正在用的口令）；② 未启用就 `regenerate`；③ **部署用 `ACCESS_PASSCODE` env 固定了口令**（平台改不动别人的部署配置，见下 ★）。`disable` 幂等。⚠️ **`enable` 会把发起这次调用的人自己挡在门外**——调用前没有门、也就没有 session；明文已在响应体里，前端拿它走一次 `POST /api/access/unlock` 即可 | 02 §5.1 / 11 §3.1 |
| POST | **`/api/system/diagnose`** | **SSE `text/event-stream`**，单项超时 5s | 02 §5.3 |
| GET | `/api/system/resources` | 系统状态页看板（含**磁盘水位**与保留卷占用，P1-9） | P21-5 |
| GET | **`/api/system/providers`** | 运维看板视角：已注册 provider/runtime/imageSpec + capabilities + 健康/失败率（统一名，P1-6）。**✅ 已落地**（2026-08-28 系统状态切片）；**与 §6.1 的 `GET /api/providers` 不是同一个端点**（那条只服务 sandbox 创建链路，不含 runtime/imageSpec 与健康）。⏳ 「最近 testkit 结果」**没有产出方**：contract-testkit 跑在 CI 里，运行期平台手上一份结果都没有——编一个恒 `null` 的字段只会在看板上多一格永远空着的卡（落地要先有「把 testkit 结论写进库」那一步，04 §10.1） | 02 §5.1 · 04 §10.1 |
| POST | **`/api/access/unlock`**（**MVP**，P0-3） | 提交访问口令换 session：`{ passcode }` → 成功 `Set-Cookie: ap_session`（签名、`HttpOnly` + `SameSite=Lax`、7 天，11 §3.1）；**未启用口令时直接放行**返回 `{ unlocked: true }`。口令错 → 401 `PASSCODE_INVALID`；连续 5 次错 → 429 `PASSCODE_LOCKED` + `retryAfterSec`（与 Guard 共用同一把锁）。前端由 `PasscodeGate` 消费（F21-8 §3） | 11 §3.1 |
| GET | **`/api/health`** | 存活探针；与 `/api/access/unlock` 同为**豁免访问口令 Guard 的两个端点**（一个是探针、一个是提交点），其余一律要 session | 11 §3.1 |
| GET | **`/api/system/audit`** | 平台级审计流，**按 `seq` 双向游标**（`?since=&before=&from=&to=&category=&severity=&subjectId=&limit=`，`since`/`before` 互斥）。⚠️ 不用时间戳翻页——同毫秒多条会漏。响应恒按 `seq` **降序** + `hasMore`，语义见 §6.6.1。`subjectId` 用于沙箱详情时间线。⚠️ **`severity` 是逗号分隔的多值**（`warn,error` = 「仅告警」），服务端 `IN (...)`，见 §6.6.1 | 13 §2.8.2 / P21-5 §10.2 |
| GET | **`/api/system/audit/export`** | 打包导出 `tar.gz`：`audit.jsonl` + `runtime.log` + `diagnose.json` + **`export-range.json`**（范围说明单独成份，理由见 P21-5 §10.3）；取「最近 24h」与「50MB」先到者。⚠️ `runtime.log` 缺失有三种成因（设施未启用 / 范围内无内容 / 读取失败），各带自己的 `omittedReason`，**任一种都不报错** | P21-5 §10.3 |
| POST / GET | `/api/system/backup` · `/api/system/version` ⏳ | 备份导出 · 版本检查 | **v1.5 占位** |

> **✅ 「系统状态与初始化」六个端点已落地**（2026-08-28 切片）：`init-status` / `init` /
> `settings`(GET+PUT) / `diagnose` / `resources` / `providers`。后端落点
> `api/apps/api/src/platform/system/`（01 §2），`system_settings` 单行表建于 `drizzle/0015`。
>
> ⚠️ **两条边界在落地时被钉死，它们此前只是文字**：
> ① **`PUT /settings` 只存配置、不放行** —— 放行是 `/init` 的事，而且是一次性的（已初始化 409）。
>    把「保存了代理」顺手当成「初始化完成」，运行期改一次代理就会悄悄重放一次初始化语义。
> ② **`/init` 的离线门** —— 模型 API 全部不可达且未带 `acknowledgeOffline` ⇒ **409**
>    （零副作用）。不设这道门，离线部署会一路走完向导，直到用户建完项目、配完凭证、
>    点下 [发起] 才发现 Agent 从一开始就不可用（P21-8 §1 的物理约束）。
>
> ★ **`PUT /api/system/access-passcode` 已落地，随之钉死了三条**：
> ① **口令有两个来源，env 永远优先**：`ACCESS_PASSCODE` 是运维在部署配置里写下的，
>    `system_settings.access_passcode_hash` 是有人在界面上设的。env 非空时本端点**整个
>    返回 409**，而不是「让库盖过 env」（平台悄悄推翻运维显式写下的一行）或「写库成功但
>    env 继续生效」（用户点了[禁用]、被告知成功、门还锁着）。如实说「这台实例的口令由部署
>    配置固定」是唯一不撒谎的答案。
> ② **`enabled` 不再是构造期快照**：它现在跟着库走，所以 `enable` 当场生效、不用重启
>    ——否则这个端点会「报告成功、什么都没变，直到下次部署」。
> ③ **cookie 签名密钥与口令解耦**（新列 `access_passcode_session_secret`，`drizzle/0016`）：
>    此前密钥退化到口令本身，那会让每一次 `regenerate` = 全员当场掉线，正好违背 11 §3.1
>    白纸黑字的「已通过的会话不受口令重新生成影响」。密钥落库而不是每进程现生成，
>    否则 cookie 承诺的 7 天会被每一次重启悄悄砍成「到下次部署为止」。
>
> ⚠️ **hash 用的是 scrypt 而不是 11 §3.1 写的 Argon2id/bcrypt**：后两者都是原生插件，
> 而本仓今天一个都没有；scrypt 是同一类（memory-hard）且内置于 Node。这条偏离记在
> 11 §3.1 与 `passcode-hash.ts`，不留给谁去 lockfile 里发现。口令是 16 位 × 58 字符集
> ≈ 93 bit，KDF 的强弱在这个熵量级上不改变结论——hash 真正买到的是「库被拖走也不等于
> 口令明文被拖走」，三者都做得到。

#### 6.6.1 `/api/system/audit` 的双向游标（前端设计驱动，F21-5 §3A）

13 §2.8.2 原写「与 `SandboxJobs` 的游标同语义」，但那套是 **read forward** 的单向 tail（契约 `readJobOutput`：「Read forward from `cursor`，省略则从头读」）——它只覆盖「拉更新的」。**审计面板要两个方向**：增量刷新拉更新的，向下滚动拉更老的。一个 `cursor` 参数表达不了，故拆开：

| 参数 | 方向 | 语义 | 谁用 |
|---|---|---|---|
| `since=<seq>` | 向新 | 返回 `seq > since` | 增量刷新（与 `SandboxJobs` 的 forward 同源） |
| `before=<seq>` | 向老 | 返回 `seq < before` | 历史滚动（本平台此前**没有**这个方向） |
| 都不传 | — | 从最新开始 | 首屏 |

- **互斥**：同时传两个 → 400 `VALIDATION_FAILED`。
- **`from` / `to`（ISO 时间）与游标正交**：游标管**翻页位置**，时间范围管**过滤条件**，两者可同时传。⚠️ 别用 `to` 代替 `before` 翻页——13 §2.8.2 已定「不用时间戳翻页，同毫秒多条会漏」；也别把时间范围折算成 seq，`at` 与 `seq` 只是**近似**同序（并发插入下可微小乱序），折算会在边界上悄悄吞掉记录。走 `idx(at)`，实测 20 万条下各类筛选均 < 0.5ms。
- **响应恒按 `seq` 降序**（最新在前），与方向无关——UI 渲染顺序因此统一，`since` 的结果 prepend、`before` 的结果 append，两边都不需要再排。
- `limit` 默认 **200**（= P21-5 §10.2 的「最近 200 条」），上限 500。
- **`severity` 是逗号分隔的多值**（`info` / `warn` / `error` 的任意子集），服务端按 `WHERE severity IN (...)` 过滤：`severity=error` 单值照旧、`severity=warn,error` 即产品要的「**仅告警**」；重复值去重；含非法值 → 400 `VALIDATION_FAILED`（与互斥那条同一套信封，带 `sideEffectFree: true`）。⚠️ 因此它在 `openapi.json` 里**不是 enum，而是一个带说明的 string**——query enum 描述不了「子集」，声明成 enum 会让 codegen 出的签名拒掉唯一能表达「仅告警」的取值。

⚠️ **「仅告警」必须在服务端过滤，客户端裁剪是一条死路。** 等值过滤时前端只能不带 `severity` 拉回一页（最多 500 条）再自己裁，而那一页是**最近的** N 条：平台平稳跑一周、最近 200 条全是 info，昨天那次 provision 失败落在更老的位置 ⇒ 用户勾「仅告警」得到空列表 + `hasMore:false`，读出来的结论是**「平台从没告警过」**，而告警就在第 201 条。服务端 `IN (...)` 之后 `ORDER BY seq DESC LIMIT n+1` 扫的是**匹配行**的最新一页，「空 + `hasMore:false`」才真的等于「全表没有这一档」；客户端裁剪那套（以及它带出的「裁完为空但还有更老的」翻页入口缺失）也随之消失。


⚠️ **`since` 不是「顺序读取」游标，它是「最新 n 条 + 下界过滤」。** 因为响应恒按 `seq` 降序再 `LIMIT`，`since=X` 返回的是**比 X 新的那些里最新的 n 条**，而不是紧接着 X 往上数的 n 条。这一条决定了两件事，它们其实是同一件事的两面：

1. 拉满 `limit` 就说明中间漏了（下面这段）；
2. **填补断层不能用 `since`**——`since: gap.afterSeq` 取回的正是顶上已有的那一批，洞一条都不会少。填洞要用 `before: gap.beforeSeq`（向老翻一页，逐步收窄）。⚠️ 这个错误的代价是「按钮点了、请求发了、列表看着刷新了，洞还在」——F21-5 §5 此前正是这么写的，已订正。

⚠️ **`since` 拉满 `limit` 意味着有断层，这一点必须显性传达。** 异常风暴时 30s 内产生 >200 条事件——那恰恰是最需要看清的时刻——一次拉不完。若前端只 prepend 一页就停，列表中间会出现**看不见的空洞**：UI 上是连续的，实际漏了。故响应带 `hasMore`，且**前端不得假装连续**（消费纪律见 F21-5 §3A）。

⚠️ **`seq` 是公开列，不是 opaque token**：前端要按它排序、比较、判断断层，所以直接用数字。这与 `JobCursor` 的「stored, never parsed」是**刻意的不一致**——那边游标是 stdout/stderr 双偏移的内部结构，这边就是一列主键。

### 6.7 WS 通道

| 通道 | 用途 |
|---|---|
| `/events` | `SandboxWsEvent` 推送（§3） |
| `/terminal?socketSessionKey=` | PTY 帧（§7.4 `TerminalClientFrame`/`TerminalServerFrame`）。**query 参数 camelCase**（P1-5），对应 DB 列 `socket_session_key`，映射在 gateway 层；**由服务端生成**（P2-9），前端只负责存回带（06 §6）。tmux re-attach 由后端负责 |
| `/tasks` | 无头 Task 输出流（§7.4 `TaskClientFrame`/`TaskServerFrame`，S6）。双向事件名统一 `frame`（与 `/terminal` 同款）；握手 query 带 `xSchemaHash: 'sb-tasks-v1'`——**与 `/terminal` 的 hash 分开**，任务帧改版不该把所有开着的终端一起打断。**不需要应用层 ping 定时器**：socket.io 自带 engine 级心跳，服务端也不靠 ping 做空闲回收（`ping`/`pong` 帧保留但可不用）。**为什么是第三个命名空间而不是再多一条 `/events` 事件**：任务输出是高频字节派生流，一次长任务能推出几千帧，压进走 Outbox 的投影通道等于纯写放大，还会淹掉整个 UI 依赖的那条通道——与当初把 `/terminal` 拆出去是同一条理由 |

**SSE 与 codegen 的边界**：`openapi-typescript` 只能生成 `/api/system/diagnose` 的响应 content-type 声明，**流帧的类型必须手写**并与 WS 协议文件同放（同一份 `// SYNC WITH ...` 纪律，§3）——诊断帧结构见 02 §5.3。

> **✅ 已落地（2026-08-28）**：两仓各持一份，与 `ws-protocol.ts` 同目录：
> `api/packages/contracts/src/sse-protocol.ts` ↔ `web/src/types/sse-protocol.ts`，
> 两边导出的 `SSE_PROTOCOL_CANONICAL` **逐字节相同**，由主仓 `scripts/docs-check.mjs` 的
> **B5** 对账（照 B4 的实现，09 §2.4）。
>
> ⚠️ **为什么门禁与代码同一轮落地**：手抄 + 零守卫 = 下一个 `TRIGGERED_BY`（本仓刚在那上面
> 发现「三份手抄、零守卫」，代价是中文界面上漏一个英文标识符而全量测试一条都不红）。
> canonical 里连**八项检查的 id 与顺序**、**status 五取值**、**预制镜像五步名**、**四个
> `PRESET_IMAGE_*` 码**一起钉住 —— 这四样都是前端会照着写渲染分支的东西。
>
> `openapi.json` 里这个 operation 现在**真的有 content-type 那一节**了
> （`content: {"text/event-stream": {schema: {type: "string"}}}`）。⚠️ 只写 `@ApiProduces`
> 是不够的：实测那样产出的是 `"200": {"description": ""}`，**连 content-type 都没有**。

### 6.8 统一错误响应 envelope（审计 P1-4）

**所有非 2xx 响应一律是这个形状**，无例外。**codegen 可得性**：`ErrorEnvelope` 必须作为具名 component 进 `openapi.json` 且被所有 error response 引用（`@nestjs/swagger` + nestjs-zod `patchNestJsSwagger()` 才能把 zod 的 enum/discriminatedUnion 如实反射）——否则前端 `components['schemas']['ErrorEnvelope']` 取不到、退化成手写（交叉评审 P1-4）。

```ts
interface ErrorEnvelope {
  code: string;          // 稳定错误码（02 §6.1；未映射的一律 'INTERNAL'，绝不缺省）
  message: string;       // 人类可读，可直接展示；已脱敏（不含明文/URL userinfo）
  retryable: boolean;    // 前端据此决定渲染不渲染 [重试]——不要用 HTTP 状态码猜
  sideEffectFree?: boolean;
                         // true = 这次请求在产生任何副作用之前就被拒（不进调度、不落库、
                         // 不调 provider.create，04 §5）。缺席 = 未表态 ⇒ 按「可能有副作用」读
  traceId?: string;      // 服务端日志关联，用户报障时报这个。**REST 上一定有**（ErrorEnvelopeFilter 统一注入，10A §3）；
                         // 类型仍是 optional 因为本形状与 MCP 共用，而 MCP tool 不经那个 filter——标 required 会对 MCP 撒谎
  details?: Array<{ path?: string; code?: string; message: string }>;
                         // 逐项错误：env 校验（ENV_NAME_RESERVED 等）、镜像 validate 的 errors[]、DTO 字段校验
                         // ⚠️ 只放「路径 + 规则 + 期望」，绝不放用户提交的值（见下 §「DTO 校验」）
}
```

| 规则 | 说明 |
|---|---|
| **`code` 永不缺省** | 兜底也要给 `'INTERNAL'`——裸 500 会让前端连「操作失败（错误码 XXX）」都显示不出来（02 §6.2） |
| **`retryable` 是一等字段** | 它来自 `SandboxProviderError.retryable`（04 §4），不是从状态码推断的 |
| **`sideEffectFree` 同样是一等字段** | **「被拒」与「失败」不是同一件事**：零副作用的拒绝没有 id、列表里不留记录、没有任何东西可供 [重试] 作用，正确呈现是**就地改请求**；而「已受理、中途失败」才该出失败卡 + [重试]。前端曾用 `httpStatus === 409` 当代理来区分，**而四条门口拒绝里它只覆盖到一条**（见下表）——状态码是传输事实，「平台有没有改过状态」是平台事实，只有平台能表态 |
| **`sideEffectFree` 缺席 = 保守读法** | `undefined` 不是 `false`，是**未表态**，消费方一律按「可能有副作用」处理（= 本字段存在之前的行为）。**它是 optional 正因如此**：改成必填会逼几十条异常路径去表态一件它们不知道的事，猜错的那条会告诉用户「什么都没发生」——而漏标只是退化回现状。**少报是降级，多报是撒谎** |
| **`details` 只在有逐项信息时出现** | 空数组不如不给；`path` 用点号路径（`env[3].key`） |
| **`details` 不回显用户提交的值** | 只放**路径 + 规则 + 期望**。zod 的 issue 原样透出会带 `received`（枚举/字面量/联合），而校验失败的字段最可能是 prompt 正文或明文凭证；信封会进渲染、进日志、进用户截图。例外只有类型名（`expected`/`received` 是 `string`/`number` 这类**类型**，不是值）——详见 [backend/04 §4.2](../backend/04-Contract与Registry扩展体系.md) |
| **DTO 校验失败也是信封** | `VALIDATION_FAILED` / 400 / `retryable:false` / `sideEffectFree:true`（04 §4.2）。此前它出线的是 `nestjs-zod` 默认的 `{statusCode, message:'Validation failed', errors}`——不是信封，于是**每个端点的每一次字段校验失败**都被前端压成「请求失败（HTTP 400）」 |
| **MCP 侧同构** | tool 错误的文本载荷里带同一个 JSON，字段一致，便于 LLM 调用方解析（02 §6.1） |
| **CI 断言** | 25 每个错误码用例除断言 `code` 外，还断言 envelope **必填**字段完整性；`sideEffectFree` 是可选字段，只在门口拒绝上断言（见下表与守卫用例） |
| **★ 由 filter 结构性保证，不靠逐处自觉** | 「所有非 2xx 一律此形状」此前**不成立**：实测同时存在四种形状，其中**裸 Nest 异常 26 处**是最多的一类，前端 `isErrorEnvelope` 认不出就整条替换成「请求失败（HTTP 404）」——后端那句指名道姓的话就此丢掉。现由全局 `ErrorEnvelopeFilter` 在出线前统一归一（与 `sideEffectFree` 由**位置**挣得、`atDoor` 出口盖章是同一手法，只是用在传输层）。**逐个改 26 处 throw 是 26 次可以漏的机会，而且下一个人写 `throw new NotFoundException` 时不会想起这条约定** |
| **★ filter 不给 `sideEffectFree` 兜底** | 传输层**不知道**平台有没有改过状态，只有位置知道。猜 `true` 会让本该给 [重试] 的失败变成「就地改配置」，半成品沙箱就没人回收——与上面「缺席 = 保守读法」是同一条纪律的两半 |
| **★ 已是完整信封的原样放行** | 门口拒绝与 zod 管道自己就知道 `sideEffectFree`/`details`，filter 不插手 |

#### 错误的四类（按**用户该做什么**分，不按 HTTP 状态分）

`retryable` / `sideEffectFree` 两个字段真正要表达的就是这四类：

| 类 | 含义 | `retryable` | `sideEffectFree` | 前端呈现 |
|---|---|---|---|---|
| **A 门口拒绝** | 请求本身不被接受，平台没动过任何状态 | `false` | `true` | 就地提示改配置，不出失败卡、不给 [重试] |
| **B 已受理后失败** | 平台改过状态，中途失败 | 视情况 | 不带 | 失败卡 + [重试]（仅可重试时） |
| **C 前置条件不满足** | 状态机不允许（如非 `ready` 态 sync） | `false` | `true` | 就地提示 **+ 指出什么时候可以** |
| **D 基础设施故障** | 网络/磁盘/超时 | 多为 `true` | 视位置 | 人话 + [重试]/[诊断] |

**A 与 C 的区别值得写明**：A 是「请求内容不对」（分支不存在、runtime 未注册），
C 是「请求没错但此刻不行」（项目还在克隆）。两者都零副作用，但 **C 要多说一句「什么时候可以」**。
C 类此前全是裸异常（`sync` 非 ready、`convert-to-empty` 非 failed、`retry-clone` 非 failed），
现由 filter 归成 `INVALID_STATE`（409），且 **message 原样透传**——那句话往往是唯一说清「到底哪儿不对」的东西。

#### ★ 错误码全量表（A5 门禁对账基准）

> **这张表是权威**：`docs:check` 的 **A5** 从 api 源码里扫出全部错误码，与本表逐条对账，
> 缺一即红。加码必须同时改这里——**否则 CI 不让过**。
>
> 为什么要这道门禁：`BRANCH_NOT_FOUND` 就是这么漏的——它进了下面的门口拒绝表，
> 却没进前端的文案表，**两侧各自"完整"，29 条测试全绿**。而错误码天生跨仓、跨层，
> 靠人记得同步三处（后端产出 / 本表 / 前端文案）是靠不住的。
>
> `类` 列见上方「错误的四类」。`retryable` 与 `sideEffectFree` 是**一等字段**，
> 不从 HTTP 状态反推。

| 码 | 类 | HTTP | `retryable` | `sideEffectFree` | 何时出现 |
|---|---|---|---|---|---|
| `VALIDATION_FAILED` | A | 400 | ❌ | ✅ | 请求体/查询参数不合 zod（04 §4.2）；`details` 带逐项路径 |
| `UNKNOWN_PROVIDER` | A | 400 | ❌ | ✅ | provider 不在注册表 |
| `UNKNOWN_RUNTIME` | A | 400 | ❌ | ✅ | runtime 不在注册表 |
| `INVALID_IMAGE_REFERENCE` | A | 400 | ❌ | ✅ | 镜像引用含空白/控制字符 |
| `IMAGE_NOT_REGISTERED` | A | 400 | ❌ | ✅ | 地址合法，但平台没有一张**活的**镜像在这个坐标上（尚未注册，或所有版本都已停用）。⭐ **全新部署会看到的第一条错误**——门口只接受注册过、解析出 digest 的镜像（04 §7 时刻③），而新库是空的。⚠️ **与上一行是两个码，刻意不合并**：那条的出路是改地址，这条是去镜像管理；合并之后什么都没填的新用户会被告知「你的地址里有控制字符」 |
| `IMAGE_PROVIDER_MISMATCH` | A | 400 | ❌ | ✅ | 地址合法、镜像也是活的，但它**跑不在这一档上**（ADR 决策 C：两档的预制镜像不再是同一张——aio 档跑自带沙箱内 HTTP API 的 AIO 镜像，boxlite 档跑精简镜像）。判据是**血统**：这张镜像派生自哪一张锚点，而锚点属于哪一档是平台自己的配置（`SANDBOX_<PROVIDER>_IMAGE`）。⚠️ **与上两行都不合并**：改地址没用、去镜像管理注册也没用，出路是**换一档，或换一张这一档的镜像**。⚠️ **单档部署下它永远不会出现**（两档指向同一张锚点） |
| `UNSUPPORTED_CAPABILITY` | A | 409 | ❌ | ✅ | `require.*` 与 provider 能力位不匹配 |
| `PROJECT_NOT_FOUND` | A | 404 | ❌ | ✅ | 建 Task 时项目不存在 |
| `PROJECT_NOT_READY` | A | 409 | ❌ | ✅ | 项目基线未就绪（仍在克隆/克隆失败） |
| `BRANCH_NOT_FOUND` | A | 400 | ❌ | ✅ | 请求的分支不在基线里（03 §7.2★） |
| `INVALID_ARTIFACT_NAME` | A | 400 | ❌ | — | 产物名非法（路径穿越防线） |
| `INVALID_STATE` | **C** | 409 | ❌ | — | 状态机不允许：sync 非 `ready`、convert-to-empty 非 `failed`、retry-clone 非 `failed`。**由 filter 归一**，message 原样透传 |
| `ALREADY_INITIALIZED` | **C** | 409 | ❌ | ✅ | `POST /api/system/init` 重复调用（多标签页并发 / 刷新后再点一次）。**一次性操作，刻意不做成幂等**（审计 P2-4）——幂等会让 `initializedAt` 被每一次误调重写，而「这台机器什么时候开出来的」正是日后排查里问得到的那个问题。⭐ **调用方拿到它应当放行**：目标状态（`initialized=true`）已经达成，把用户卡在向导里既没道理也没出路 |
| `OFFLINE_NOT_ACKNOWLEDGED` | **C** | 409 | ❌ | ✅ | `POST /api/system/init`：**模型 API 全部不可达**而请求里没有 `acknowledgeOffline: true`（P21-8 §1 的物理约束）。⭐ **调用方拿到它绝不能放行**——平台一个字都没写，`initialized` 仍是 `false`。message 已写清两条出路（配代理后重测 / 带 `acknowledgeOffline: true` 以离线模式继续），原样上 UI 即可。⚠️ **镜像仓库不可达不进这道门**：那只是拉不到新镜像，不是 Agent 不可用 |
| `NOT_FOUND` | **C** | 404 | ❌ | — | 资源不存在（26 处裸 `NotFoundException` 归一而来）。**由 filter 归一** |
| `BAD_REQUEST` | A | 400 | ❌ | — | 未分类的 400（裸 `BadRequestException` 归一）。**由 filter 归一** |
| `UNAUTHORIZED` | A | 401 | ❌ | — | 未通过口令门/凭证无效。**由 filter 归一** |
| `SCHEMA_MISMATCH` | A | —（**WS 握手**） | ❌ | ✅ | 客户端 `X-Schema-Hash` 与服务端不符（协议漂移）。走 socket.io `next(err)`，`err.data.code` 带码。**重连无用**——同一个客户端还是同一个 hash，要换的是客户端 |
| `SANDBOX_REQUIRED` | A | —（**WS 握手**） | ❌ | ✅ | 握手缺 `sandboxId`：**寻址**缺失，不是鉴权失败。⚠️ 刻意不写成 `UNAUTHORIZED`——那会把用户送去解锁弹窗，而弹窗补不了一个客户端自己没发的查询参数 |
| `FORBIDDEN` | A | 403 | ❌ | — | 有身份但无权限。**由 filter 归一** |
| `PAYLOAD_TOO_LARGE` | A | 413 | ❌ | — | 请求体超限。**由 filter 归一** |
| `PASSCODE_REQUIRED` | A | 401 | ❌ | ✅ | 口令门：未解锁（11 §3.1） |
| `PASSCODE_INVALID` | A | 401 | ❌ | ✅ | 口令门：口令错误 |
| `PASSCODE_LOCKED` | A | 429 | ✅ | ✅ | 口令门：连续失败被锁，**稍后可再试** |
| `AUTH_REJECTED` | A | 401 | ❌ | ✅ | runtime 凭证被上游拒（api key 无效等，05 §4） |
| `NO_CREDENTIAL` | **C** | 409 | ❌ | ✅ | 该 runtime 没有生效凭证 —— 出路是**先去配**，不是重试 |
| `IMAGE_PULL_FAILED` | **B** | — | ✅ | — | 拉镜像失败（沙箱状态机，非 REST 响应；走 WS `status_changed.errorCode`） |
| `INSTALL_FAILED` | **B** | — | ✅ | — | 沙箱内装 runtime CLI 失败（03 §4.3 ③） |
| `BINARY_NOT_FOUND` | **C** | —（见下 ⚠️） | ❌ | — | `AdapterAuthErrorCode` 闭集成员：沙箱里找不到 runtime CLI 可执行文件 |
| `PARSE_ERROR` | **D** | —（见下 ⚠️） | ✅ | — | `AdapterAuthErrorCode` 闭集成员：adapter 解析 CLI 输出失败 |
| `RESOURCE_EXHAUSTED` | **B** | **429** | ✅ | — | 资源不足，调度拒绝（03 §3 互斥登记；`POST /api/sandboxes` 的同步拒绝）。⚠️ **本行原写 507，与代码不符，2026-08-31 按实现改正为 429**：04 §4 那张唯一的 contract→HTTP 映射表（`provider-error.http.ts`）从建表起就是 `TOO_MANY_REQUESTS`，`create-door.spec.ts` 也一直断言 429 —— 507 从未被任何代码产出过。语义上 429 也更准：这条的出路是**等一会儿再来**（`retryable:true`，automation 决策表行 3 那 5 次排队重试就建立在这一位上），而 507「Insufficient Storage」是 `DISK_INSUFFICIENT` 那条「要用户去清理、重试无用」的语义 |
| `WORKSPACE_PREPARE_FAILED` | **B** | — | ✅ | — | 工作区准备失败（`preparing-workspace` 段，03 §7.6）。**2026-08 之前这个码有五处文档承诺、零个产出方**——实际出线的是 Node 的 errno（`ENOSPC`/`EACCES`），前端按码查文案查不到 |
| `TASK_FAILED` | **B** | — | ✅ | — | 无头任务非零退出（终态，非 REST 响应） |
| `TASK_KILLED` | **B** | — | ✅ | — | 任务被信号杀死（终态） |
| `TASK_TIMED_OUT` | **B** | — | ✅ | — | 任务超硬超时（30min/1h/2h/4h 可配）被强制终止 |
| `SANDBOX_GONE` | **C** | — | ❌ | — | 恢复路径：沙箱已不在 —— 重试无用，只能重新发起 |
| `RESUME_FAILED` | **B** | — | ✅ | — | 恢复路径：接回已有会话失败 |
| `REPLAY_FAILED` | **B** | — | ✅ | — | 任务事件回放失败 |
| `IMAGE_CONTRACT_VIOLATION` | **B** | — | ❌ | — | 镜像不满足契约（如无 tmux，04 §7）—— 换镜像，不是重试 |
| `REF_NOT_FOUND` | A | 404 | ❌ | ✅ | `ImageSpecProvider.resolve()` —— ref 在 registry 里不存在（注册 / 预检 / 重验证三个入口，04 §10.4 IS-02） |
| `REGISTRY_UNREACHABLE` | **D** | 502 | ✅ | ✅ | `resolve()` 出网失败。⚠️ **本组唯一 `retryable:true` 的码，也正因如此 `resolve` 不许在建 Task 的门口调用**——门口拒绝一律 `retryable:false` 是门的性质，出网一次就破了它（04 §7） |
| `MANIFEST_INVALID` | A | 422 | ❌ | ✅ | `POST /api/images` **注册被拒**，manifest **不落库**；逐条结论在 `details[]`。⚠️ 它覆盖注册期的**两种**判定——镜像规格（`validate()`，如 `IMAGE_ENTRYPOINT_INVALID`）与平台准入策略（`IMAGE_BASE_REQUIRED` / 根镜像的 `IMAGE_TMUX_MISSING`）：两者用户要做的事相同（改镜像重来），具体原因已经在 `details[]` 里 |
| `IMAGE_BASE_REQUIRED` | A | —（**随 `MANIFEST_INVALID` 出线**） | ❌ | ✅ | **注册期血统校验**的 `errors[].code`，**不是顶层码**（04 §7 ★血统）：镜像的 `rootfs.diff_ids` 不是任何一张平台预制镜像的前缀扩展 ⇒ 它不是从平台预制镜像构建的。message 必须点名可照抄的那张（「改成 `FROM <ref>` 重新构建」）。⚠️ **它不是「平台一张预制镜像都没有」那种情况的码**——那是平台没准备好而不是用户镜像不对，走 `INVALID_STATE`(409) |
| `IMAGE_TMUX_MISSING` | A | —（**随 `MANIFEST_INVALID` 出线**） | ❌ | ✅ | `errors[].code`，**不是顶层码**：**仅根镜像**（`SANDBOX_DEFAULT_IMAGE`，`ImageSeeder` 以 `builtin:true` 注册的那张）没有声明 `platform.tmux=true`（04 §7 ★血统 ③）。⚠️ **2026-08 范围收窄**：此前它是 `validate()` 对**每张**镜像的判定，被血统验证制取代——标签会被派生镜像继承，在派生场景里既证明不了什么又会主动说谎。⚠️ 与运行期的 `IMAGE_CONTRACT_VIOLATION` 仍是两个时刻的两个码——注册期抓「运维方指错了镜像」，运行期抓「谎报」 |
| `IMAGE_ENTRYPOINT_INVALID` | A | —（同上，`details[].code`） | ❌ | ✅ | `validate()` 的入口约定一半：镜像既无 `Entrypoint` 也无 `Cmd`，或没有 `WorkingDir`。**一个码配不同 `path`**（testkit IS-03 要的是可定位，不是一字段一码） |
| `RUNTIME_NOT_PREINSTALLED` | A | —（**`ValidationOutcome.warnings[].code`**） | ❌ | ✅ | 镜像没在 `platform.supportedRuntimes` 里声明预装某个平台已注册的 runtime（IS-05 ②）。**warning 不是 error**：现装能用，只是按分钟而不是秒计（实测 753s）。⚠️ **也不影响可选性**（04 §7 ★血统 ⑤）：血统保证了 node 在 ⇒ 任何合规镜像都装得上任何 runtime，用「没预装」去否决可选性等于否认一个已被保证的能力，还会让这条 warning 对应的那张卡永远选不到（实测：诚实标注后 `?runtimeId=claude-code` 返回 0 张） |
| `IMAGE_DIGEST_GONE` | **B** | 409（并走 WS `status_changed.errorCode`） | ❌ | — | `provider.create()` 按 `ref@digest` 拉取，而上游已删除/GC 掉那个 digest（tag 仍可解析）。**钉住 digest 换来的新失败模式**；与 `IMAGE_PULL_FAILED` 必须分开——那边是查地址查网络，这里地址完全正确，出路是 [检查更新] 换到新 digest |
| `ENV_NAME_INVALID` | A | 400（`details[].code`，顶层 `VALIDATION_FAILED`） | ❌ | ✅ | `EnvVarSet.create()` 构造期校验（23 §9.3，I-IMG-1）：名不合 `^[A-Za-z_][A-Za-z0-9_]*$` |
| `ENV_NAME_RESERVED` | A | 400（同上） | ❌ | ✅ | 同上：命中保留名（含 `CODEX_*` / `GIT_*` 前缀整体拦截） |
| `ENV_LIMIT_EXCEEDED` | A | 400（同上） | ❌ | ✅ | 同上：>50 条 / 名 >64 字符 / 值 >4096 **字节**（UTF-8 计） |
| `ENV_DUPLICATE_KEY` | A | 400（同上） | ❌ | ✅ | 同上：同一 manifest 内 key 重复（大小写敏感） |
| `CLONE_FAILED_PERMISSION` | **D** | 403 | ❌ | — | git 无权限 ⇒ 前端给 [配置 Git 凭证] |
| `CLONE_FAILED_NETWORK` | **D** | 502 | ✅ | — | git 网络失败 |
| `TIMEOUT` | **D** | 504 | ✅ | — | clone/sync 超时（clone 10/30min、sync 5min） |
| `DISK_INSUFFICIENT` | **D** | 507 | ❌ | — | 磁盘不足 —— 要用户清理，重试无用 |
| `INTERRUPTED` | **D** | 502 | ✅ | — | clone 被中断（进程退出/取消） |
| `UPSTREAM_UNAVAILABLE` | **D** | 502 | ✅ | — | 上游不可达。**由 filter 归一** |
| `UNSUPPORTED_METHOD` | A | 405 | ❌ | ✅ | MCP/REST 方法不支持 |
| `HOST_NOT_ALLOWED` | A | 403 | ❌ | ✅ | git 凭证的 `allowedHosts` 不含目标 host（SSRF 防线，23 §8） |
| `AUTH_CHALLENGE_EXPIRED` | **C** | 410 | ❌ | ✅ | device-code 挑战超时（15min）—— 出路是**重新获取**，不是重试同一个 code |
| `ALREADY_EXISTS` | A | 409 | ❌ | ✅ | provider 侧资源已存在（`SandboxProviderError` 闭集，04 §4） |
| `PERMISSION_DENIED` | **D** | 403 | ❌ | — | provider 侧权限不足（docker socket、boxlite 目录等） |
| `PROVIDER_UNAVAILABLE` | **D** | 502 | ✅ | — | provider 本身不可达（docker daemon 没起、boxlite 锁被占） |
| `INTERNAL` | **D** | 500 | ✅ | — | 兜底。**不透传内部细节**（可能含路径/SQL/栈），用户凭 `traceId` 报障 |
| `UNKNOWN` | — | — | ❌ | — | **仅前端**：`toApiError` 认不出信封时的降级值。后端**永不产出**——它出现在界面上就意味着信封链路破了 |
| `PRESET_IMAGE_NOT_CONFIGURED` | **B** | —（**SSE 诊断帧**，非 REST 响应） | ❌ | — | 诊断第 ⑧ 项第 1 步（P21-5 §9A）：`SANDBOX_DEFAULT_IMAGE` 没配，平台回落到兜底坐标 —— 那是上游镜像，没有沙箱 API / tmux / 常驻进程，**建任务必失败**。出路是**改配置** |
| `PRESET_IMAGE_NOT_IN_REGISTRY` | **B** | —（同上） | ❌ | — | 第 2 步：配了，但 registry 里解析不到（不存在 / 不可达 / 需凭证）。出路是**推镜像**或改地址 |
| `PRESET_IMAGE_NOT_PLATFORM_BUILT` | **B** | —（同上） | ❌ | — | 第 3 步：解析到了，但它没声明 `platform.tmux` ⇒ 不是平台自建的那张。⚠️ 文案必须说清「**手动注册同样会被血统检查拒**」，否则用户会以为只是少做了一步注册，照着去注册再撞一次墙。出路是**换成自建的那张**（04 §7 ★血统） |
| `PRESET_IMAGE_NOT_SEEDED` | **B** | —（同上） | ❌ | — | 第 4 步：是对的那张，但平台里没有 `valid` 且启用的注册行（开机播种没成 / 停用了 / invalid）。出路是**重启平台**等播种，或去镜像管理启用一版 |
| `AUTOMATION_LIMIT_REACHED` | **C** | 409 | ❌ | ✅ | 每项目自动化规则数已达 **20**（I-AUT-7）。⚠️ **与 `VALIDATION_FAILED` 刻意分开**：那条的出路是改表单，这条是**先删一条旧规则** —— 合并之后用户会对着一张挑不出毛病的表单反复重填 |
| `PREVIOUS_RUNNING` | — | —（**`AutomationRunDto.errorCode`**，非 REST 响应） | ❌ | — | 03 §8.2 决策表行 1：上次触发的 Task 仍在非终态 ⇒ 本次 `skipped`。**不计入 `consecutiveFailures`**（I-AUT-1：不是规则的错） |
| `AUTH_EXPIRED` | — | —（同上） | ❌ | — | 决策表行 2：该 runtime 无生效凭证 / 已过期 / 已吊销 ⇒ 本次 `skipped` + 横幅 + webhook。同样**不计入**失败计数。⚠️ 凭证 `expiring`（剩 <7 天）**不触发它** —— 那是预警不是拦截 |

⚠️ **`ALREADY_INITIALIZED` / `OFFLINE_NOT_ACKNOWLEDGED` 必须是两个码，⛔ 不许合并回 `INVALID_STATE`（2026-08 真机验证补）。**
它们**曾经共用 `INVALID_STATE`**：同一个端点、同一个 409、同一个码，而调用方要做的事**恰好相反** ——
一个「放行进工作台」，一个「留在向导里把那句话说给用户看」。
用一个码表达两件需要不同处理的事，等于逼**每个**调用方在拿到 409 之后再打一次 `GET /api/system/init-status`
去二次探测自己拿到的是哪一种；而不做那次探测、照着「409 = 已初始化 ⇒ 放行」写的那一版（F21-8 §8 约束 3 的原文），
会把一台**根本没初始化**的机器放进工作台 —— 用户看到工作台、下次刷新又被弹回向导，**中间一句错误都没有**。
加码的代价（本表一行 + A5 对账）是**一次性**的，二次探测的代价是**每个调用方每一次**。
这与上面四个 `PRESET_IMAGE_*` 不许合成一条「镜像不可用」是同一条纪律：**闭集的价值在于分得开，不在于短**。

⚠️ **`BINARY_NOT_FOUND` / `PARSE_ERROR` 今天在源码里「声明了、但一处都没抛」。**
它们是 `AdapterAuthErrorCode`（`runtime/src/domain/errors/adapter-auth.error.ts`）这个**六成员闭集**的一部分，
所以本表收录它们——A5 对账的是**源码里声明的码集合**，一个闭集成员即使还没有 throw 处也已经是契约的一部分。
但要如实记两笔：① 全仓没有 `throw new AdapterAuthError('BINARY_NOT_FOUND', …)`；
② `runtime-application.service.ts` 的 `mapAdapterError` 只给六个成员里的**四个**写了 case，
这两个落在 `default: return e` ⇒ 真抛出来会逃过 HTTP 映射，被 `ErrorEnvelopeFilter` 兜成 `INTERNAL`，
**用户拿不到这两个码本来要说的那句话**。补 throw 处时必须同时补 `mapAdapterError` 的 case。

⚠️ **本表 2026-08 因 A5 扫描器修复新增四行**（`SCHEMA_MISMATCH` / `SANDBOX_REQUIRED` / `BINARY_NOT_FOUND` / `PARSE_ERROR`）。
它们**一直**在源码里，是门禁没看见：`ERROR_CODE_SOURCES` 曾是一份手写路径清单（漏了 `runtime/src/domain`），
而联合类型的上下文判据曾要求类型名里含 `Code`（`WsHandshakeRejection` 不含）。
⚠️ 这次的失效方式是**静默**的——门禁照常报「集合相等」，而 `AdapterAuthErrorCode` 那个六成员闭集**被扫了三分之二**。
病因与 `GIT_TRACE` 那份四字面量清单同源：**枚举本身就是病因**（03 §7.3 G）。现已改为全 `src` 目录扫描 + 形状判据。

⚠️ **`—` 表示「不带该字段」，不是 `false`**。`sideEffectFree` 缺席 = 未表态，按「可能有副作用」读（见上表）。

⚠️ **B 类多数不是 REST 响应**：它们走沙箱状态机与 WS `sandbox.status_changed.errorCode`（04 §4 闭集）。
放进同一张表是因为**前端的文案表按码查，不分传输**——同一个码在两条路上出现，用户看到的该是同一句话。

⚠️ **四个 `PRESET_IMAGE_*` 走的是第三条传输：`POST /api/system/diagnose` 的 SSE `check` 帧**
（`DiagnoseCheckFrame.errorCode`，帧契约在两仓的 `sse-protocol.ts`，B5 对账）。它们进本表的理由与上一段完全相同——
**码表按码查、不分传输**；而它们**必须是四个码而不是一个**，是 P21-5 §9A 的硬性要求：
五步的下一步动作完全不同（改配置 / 推镜像 / 换成自建那张 / 重启平台 / 只是等一会），
合成一条「镜像不可用」等于把诊断退化成一个红灯。

⚠️ **诊断的其余七项刻意不发码**，所以本表里没有它们的行。那七项的结论天然带着这一次实测出来的具体数字
（哪个端口、被谁占、还剩多少 GB），按码查一句固定文案反而更差；为它们凭空造一批只在这一个端点出现、
前端也不会分支的码，只会让本表变长而没有消费方——那正是下面那一节存在的理由。
⚠️ 02 §5.3 的示例帧里写着 `"errorCode":"NETWORK"`，**那是示意而不是契约**：`NETWORK` 从来不在本表里，
本轮实现也不产出它（已在 02 §5.3 就地标注）。

⚠️ **第 5 步（未 staged）没有码，因为它不是失败**：它是 `status:'info'`「首个任务需要数分钟准备镜像」
（实测 13GB 镜像冷启动 190 秒）。给它一个错误码就等于承认它是错误，而渲染成 ⚠️/❌ 会让用户去修一个不需要修的东西。

#### ⏳ 已定案、但今天没有产出方的错误码（**刻意不进上表**，A5 不对账）

> **本节现在是空的，而这是它该有的样子** —— 2026-08 镜像切片落地，原先登记在这里的
> **九个镜像码全部搬进了上面的主表**（`REF_NOT_FOUND` / `REGISTRY_UNREACHABLE` /
> `MANIFEST_INVALID` / `IMAGE_DIGEST_GONE` / `IMAGE_TMUX_MISSING` 与四个 `ENV_*`），
> 另有实现时新增的三个（`IMAGE_ENTRYPOINT_INVALID` / `RUNTIME_NOT_PREINSTALLED` /
> `IMAGE_BASE_REQUIRED`——最后一个是 2026-08「血统验证制」带来的，04 §7 ★血统）
> 直接进主表。**小节保留，因为它是一套机制而不是一张清单** —— 下一个「设计已定、代码
> 未有」的码仍然要落在这里，而不是落进主表把 A5 弄红。
>
> **为什么单开一节，而不是往上表补行**：上表是 **A5 门禁的对账基准** ——
> 它与 api 源码扫出来的错误码集合**逐条全等比较**。往里加一行没有产出方的码，A5 当场红，
> 而且报出来的措辞是「文档表有、源码无」，**看起来像是这张表写错了**。
> A5 只读 `#### ★ 错误码全量表` 那一节（到下一个 `####` 为止），所以本节对它不可见。
>
> **这条规则做过变异验证，不是读代码读出来的**（原文的两句实测存档，数字是当时的）：
> ① 把 `REF_NOT_FOUND` 塞进当时的主表 ⇒ A5 立刻红（「表 46 行 / 源码 45 个码，差集 1 条：
> 文档表有、源码无」）；② 它留在本节 ⇒ A5 仍是「45 行，集合相等」。
> **「它红过吗」是这个仓对任何绿灯的最低要求**（[LIVE-RUN-FINDINGS](../LIVE-RUN-FINDINGS.md) 共性 2）。
> 本轮的反向验证同样跑过：九个码的产出方一写出来，A5 立刻报「源码 60 个码，表 49 行，
> 差集 11 条：源码有、文档表无」——**门禁不是在挑刺，它是在说这张表该更新了**。
>
> **但「不进主表」不等于「不用管」。** 这正是 `BRANCH_NOT_FOUND` 那次事故的另一半形状：
> 一个码在某一侧被当成已定案，另一侧压根不知道它存在，两边各自完整、合起来漏一条。
> 所以它们记在这里，带齐落地时要填进主表的四个字段，**不留在别人脑子里**。

| 码 | 类 | 预期 HTTP | `retryable` | `sideEffectFree` | 定案处 | 落地后由谁产出 |
|---|---|---|---|---|---|---|
| —（本节当前为空） | | | | | | |

**两条定案随九个码一起搬进主表，理由保留在这里**（此前 02 §5.1 / 本文 §6.4 / 13 §2.4.3 / 26 §7 / 27 §6 / 后端 28 §11 **六处都只写「400 + 逐项 `path`」**，**没有一处说过顶层 `code` 是什么**——而前端 `describeSandboxError` 是拿 `envelope.code` 去 `COPY_TABLE` 里查的，顶层码没定，这几个 `details[].code` 再齐也换不来一句人话。这个洞与 `WORKSPACE_PREPARE_FAILED` 是同一种）：

1. **四个 `ENV_*` 码住在 `details[].code`，顶层 `code` 是 `VALIDATION_FAILED`**（400 / `retryable:false` / `sideEffectFree:true`，04 §4.2）。
   理由：`VALIDATION_FAILED` 已经在上表里、已经有产出方、语义与前端呈现（就地标红、不出失败卡）**完全一致**，
   再造一个 `ENV_VALIDATION_FAILED` 只是给前端多一条要记的分支。
   ⚠️ 走的**不是**同一条路——zod 管道拒的是 DTO 形状，`EnvVarSet` 拒的是领域规则（构造即校验）——**但落到同一个信封形状**。
   实现落点：`M/image/interface/http/image-error.http.ts` 一张表，`EnvValidationError → VALIDATION_FAILED + details[]`。
2. **`IMAGE_TMUX_MISSING` 同理住在 `details[].code`**，顶层是 `MANIFEST_INVALID`(422)。
   ⚠️ 它与主表已有的 **`IMAGE_CONTRACT_VIOLATION` 不是同一个码，也不是同一个时刻**：
   前者是**注册期静态判定**（这张镜像不合格，不许进库），后者是**运行期实测失败**
   （`bootstrapAgentSession` 起会话前 `command -v tmux` 未命中 ⇒ 实例转 `failed`，走 WS 而非 REST）。
   04 §7 明说「注册期判定不免除运行期实测」，所以这两个码**必须并存**，谁也不能替代谁。

⚠️ **`MANIFEST_INVALID` 曾经是「前端有、后端没有」的那一半，现在后端补齐了。**
`web/src/lib/sandbox/sandboxErrorCopy.ts` 的 `COPY_TABLE` 里一直有它的文案行
（「❌ 镜像不满足平台约定」），而源码里没有任何产出方——与 `BRANCH_NOT_FOUND` 同形状、
反方向，两次都不会让任何测试变红。⚠️ **还欠一笔前端的账**：那条文案写的是
「缺少 tmux 等必须项」，描述的其实是 `IMAGE_CONTRACT_VIOLATION`（运行期）而不是注册期的 422；
**新进主表的十一个码也需要各自的前端文案**（落地纪律第 2 条）。

**落地纪律（三条，缺一条这张表就会变成新的谎话）**：

1. **码一旦有了产出方，必须从本表移进上面的主表**，并补齐「何时出现」一列。本表只登记「设计已定、代码未有」的码，**不是第二张常驻码表**。
2. **同时把前端文案表补齐**（`sandboxErrorCopy.ts` 的 `COPY_TABLE`）。后端产出 / 本表 / 前端文案，三处齐了才算落地——这是 A5 存在的全部理由。
3. ⚠️ **扫描范围必须能看见新模块**。原文这里要求「落地时同步改 `scripts/docs-check.mjs` 的 `ERROR_CODE_SOURCES`」，因为那曾是一份**硬编码路径清单**、里面没有 image 模块——不加就扫不到产出方，于是**A5 会一直是绿的，而绿的原因是它根本没在看**。
   **这条已经不需要做了，但不是因为它不重要**：`ERROR_CODE_SOURCES` 本身在 2026-08 被删掉，换成 `errorCodeSourceRoots()` 遍历 `apps/api/src` + 每个 `packages/*/src` 与 `packages/modules/*/src`——**枚举本身就是病因**（同 `GIT_TRACE` 那份四字面量清单，03 §7.3 G）。本轮 `packages/modules/image/**` 一建出来就被扫到了，无需任何门禁改动，A5 当场从「集合相等」变成「差集 11 条」。
   这正是 [LIVE-RUN-FINDINGS](../LIVE-RUN-FINDINGS.md) 共性 2（「证据本身可能是假的，绿灯不等于覆盖」）在门禁自己身上的版本：**门禁的扫描范围写窄了，它就会指着一张不完整的表说它是对的**（第一版 A5 把范围只写到 `errors.ts`，结果 10 个码全部误报，是同一个坑的另一面）。

#### 「门口拒绝」（零副作用）全量清单 — `POST /api/sandboxes` 创建门（04 §5）

| 拒绝 | 码 | HTTP | `retryable` | `sideEffectFree` |
|---|---|---|---|---|
| provider 不在注册表 | `UNKNOWN_PROVIDER` | 400 | ❌ | ✅ |
| runtime 不在注册表 | `UNKNOWN_RUNTIME` | 400 | ❌ | ✅ |
| 镜像引用含控制字符 | `INVALID_IMAGE_REFERENCE` | 400 | ❌ | ✅ |
| 能力静态校验不通过 | `UNSUPPORTED_CAPABILITY` | 409 | ❌ | ✅ |
| 项目不存在 | `PROJECT_NOT_FOUND` | 404 | ❌ | ✅ |
| 项目尚不能接任务（I-PRJ） | `PROJECT_NOT_READY` | 409 | ❌ | ✅ |
| 请求的分支不在基线里 | `BRANCH_NOT_FOUND` | 400 | ❌ | ✅ |

> **门口拒绝一律 `retryable: false`，这是门的性质而非逐条判断**：门给的答复是「这个请求本身不被接受」，原样再发一次必然同样被拒——要变的是请求或它的前置条件，不是重试次数。同 `UnknownRuntimeError.retryable = false` 的理由（04 §4）：每按必败的 [重试] 比没有按钮更糟。
>
> **⚠️ 上表里三条此前根本不产出 envelope（2026-08 修复）**：`UNKNOWN_PROVIDER` / `INVALID_IMAGE_REFERENCE` / `UNKNOWN_RUNTIME` 是裸 `BadRequestException(string)` 抛的，出线的是 Nest 默认 `{statusCode, message, error}`——**没有 `code`、没有 `retryable`**。前端 `toApiError` 判定「不是信封」后整体替换成 `{code:'UNKNOWN', message:'请求失败（HTTP 400）'}`，于是后端那句指名道姓的话（`unknown runtime 'shell'`）**从来没到过能据此行动的人眼前**。**给一个没人读的 body 加字段等于没加**——所以这三条先被改成真信封，字段才有意义。这也顺带证明了「缺席 = 保守读法」的设计是对的：漏改一处，前端读到 `undefined`，落回今天的行为（错，但不是谎）；若字段必填或默认 `true`，同一处遗漏就变成对着没人检查过的请求说「什么都没创建」。
>
> **DTO 校验失败走的是另一条路，但落到同一个信封（2026-08）**：上表六条是**创建门**里手写的拒绝；而 `POST /api/sandboxes` 的 `initialPrompt` 超过 8000 字符这类**字段级**违规，在 controller 之前就被全局 zod 管道拒了，根本到不了 `admit`。它现在同样产出信封——`VALIDATION_FAILED` / 400 / `retryable:false` / **`sideEffectFree:true`**，后者在这里是**构造上**成立的（管道跑在 controller 之前，没进 application service、没落库、没进调度），与 `atDoor` 同一套「位置换取标记」的道理。`message` 指名字段与规则（「请求参数 initialPrompt 长度超过上限 8000 字符」），`details[]` 给逐项 `{path, code, message}`。权威说明见 [backend/04 §4.2](../backend/04-Contract与Registry扩展体系.md)。
>
> **落地形态：位置换取，而不是逐点标注**。创建门收敛成 `SandboxApplicationService.admit` 一个方法——它不持 `uow`、不写库、不调度、不碰 `provider.create`，所以「零副作用」对**这里抛出的每一条**都是构造上成立的；`atDoor` 在出口处把这个结构事实翻成 `sideEffectFree`。创建门里**只有这一处**写这个字段（全库另有一处：controller 之前的 zod 校验管道，04 §4.2——同一套位置换取的道理）。于是**将来新增的门口校验只要写在这个方法里就自动标对**，作者根本不需要知道有这个字段——而逐点标注正是三条漏标的成因。守卫用例见 `packages/modules/sandbox/test/application/create-door.spec.ts`：它不测清单，测机制（往门里塞一条本仓从未见过的拒绝，要求它照样带标）。

---

## 7. 通用（wire）类型详细设计——两侧唯一权威定义

> 本节把散落在 13/23/27/§6/§3 的对外类型 **consolidate 成实际类型声明**（前几节讲"契约怎么流动/端点有哪些"，本节讲"类型本身长什么样"）。**这些类型的单一来源是后端 zod schema**（02 §3）：zod →（`createZodDto`）→ REST DTO / MCP inputSchema / openapi.json → 前端 `openapi.d.ts`。前端**永不手写**这些类型，一律从生成物取（14）。下面用 TS 声明表达**契约形状**（实现里是 zod，形状一致）。

### 7.1 枚举（字面量联合）——全库唯一取值来源

```ts
// —— sandbox 生命周期 ——
type SandboxStatus =                     // 13 §2.1 CHECK 12 值；推进顺序见注
  | 'pending' | 'scheduling' | 'preparing-workspace' | 'creating' | 'starting'
  | 'running' | 'idle' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'failed';
//   工作区先于实例：scheduling → preparing-workspace → creating → starting（审计 P0-1）
//   ⚠️ 'waiting-input' 不在此枚举——它是 running 的运行时子态，只走 WS + REST 派生字段（§7.4 / 03 §4.1）

type ReconciliationStatus = 'confirmed' | 'pending' | 'orphaned';   // 13 §2.1 对账

// —— project / 工作区 ——
type CloneStatus = 'cloning' | 'ready' | 'failed';   // 13 §2.2；空项目恒 ready；离开 failed 仅 retry-clone→cloning / convert-to-empty→ready
type ProjectSourceType = 'git' | 'empty';

// —— image ——
type ImageValidationStatus = 'pending' | 'valid' | 'warning' | 'invalid';  // 13 §2.4；warning 仍可选（P21-4 §9）
type RuntimeInstallStatus = 'not_installed' | 'installing' | 'installed' | 'failed';  // 13 runtime_installations

// —— credential ——
type CredentialKind = 'runtime' | 'git';                                   // 13 §2.5
type ObtainedViaRuntime = 'setup-token' | 'oauth-device' | 'api-key' | 'access-token-paste';
type ObtainedViaGit = 'git-ssh-key' | 'git-https-token';
type RuntimeAuthMode = 'account' | 'api-key';    // 二选一全局生效【模式】（active_auth_method，05 §4）；区别于鉴权【方式】 oauth-device/setup-token/api-key（BeginAuthRequest.method）
type CredentialStatus = 'none' | 'active' | 'expiring' | 'expired';  // 每 runtime 聚合态（UI 用，27 §runtime）
//   expiring = 剩 <7 天：只预警不拦截（前端据此决定拦截面板 vs 直达确认步）

// —— automation（v1.1）——
type AutomationRunStatus =                          // 13 automation_runs
  | 'pending' | 'running' | 'success' | 'failed' | 'timeout'
  | 'resource-exhausted' | 'skipped' | 'missed';
//   计数口径：仅 'failed'（含 'timeout'）计入连续失败；'skipped'/'missed' 不计（P21-7 §4 / 23 I-AUR）
type WebhookStatus = 'sent' | 'failed' | 'skipped';
type TriggerOn = 'failure' | 'success' | 'all';
type ScheduleKind = 'hourly' | 'daily' | 'weekly';

// —— terminal ——
type TerminalSessionStatus = 'attached' | 'detached' | 'closed';   // 13 §2.6

// —— 数值枚举 ——
type TimeoutMinutes = 30 | 60 | 120 | 240;          // 无头硬超时（默认 120）
type ArtifactRetentionDays = 3 | 7 | 30;            // 保留卷（默认 7）
```

### 7.2 通用值类型

> **可扩展类型原则（needs 6/7：sandbox/runtime/image 经 registry 可扩展）**——这决定一个标识该用**封闭联合**还是**开放 id**：
> - **封闭联合**（`'a' | 'b'`）：取值集合**由平台代码固定**、增删必须改代码（如 `SandboxStatus` 状态机、`CloneStatus`、错误分支）。加一个值 = 平台要处理它 = 本就该改代码。
> - **开放 id**（`type XxxId = string`）：取值集合**由 registry 在运行时决定**、第三方注册即生效、平台代码零改动（`ProviderId` / `runtime` id / `image` id）。**provider/runtime/image 必须是开放 id**——把它们写成 `'aio' | 'boxlite'` 这种封闭联合，等于每注册一个新 provider 都要改一次 wire 契约，直接违背"registry 可扩展"（04 §8：第三方注册的 runtime 自动出现、前端零改动）。
>
> ```ts
> type ProviderId = string;   // 已注册的 sandbox provider id；内建 'aio'（默认）/'boxlite'，但取值不封闭——registry 解析
> // runtime、image 同理已是 string；前端下拉的可选值来自 GET /api/providers / GET /api/runtimes / GET /api/images
> //   （运行时数据），不是编译期字面量
> // 内建集合仅作文档记载与默认值，不进类型约束——校验"是否已注册"在后端 registry，不在 TS 联合类型
> // 默认档同理不是前端常量：GET /api/providers 每项自带 isDefault，前端取那一项（§6.1）
> ```
>
> **反例存档（2026-08 已修复）**：前端曾写死 `SANDBOX_PROVIDERS = ['aio','boxlite'] as const` + `DEFAULT_SANDBOX_PROVIDER`，后端 registry 注册的第三方 provider 因此**在 UI 里根本看不见**——这正是本原则要防的那类漂移。现已删除，选项与默认档全部来自 `GET /api/providers`。

```ts
// 分页信封（列表类响应统一形状）
interface Paginated<T> { items: T[]; total: number; page: number; pageSize: number; }  // 用于 automation runs（GET .../runs）；其余列表端点返回裸数组 T[]

// 资源池快照（含磁盘——审计 P1-9）
interface ResourcePoolSnapshot {
  totalCores: number; usedCores: number;
  totalRamMb: number; usedRamMb: number;
  totalDiskMb: number; usedDiskMb: number;          // usedDiskMb = 活跃分配 disk_mb_reserved + 保留卷 size
}

// 镜像不可变坐标（digest 非空——23 I-IMG-6）
interface ImageRef { registry: string; repository: string; tag: string; digest: string; }

// provider 能力位（7 位全 required，权威定义 04 §2.5；wire schema 与 SPI 结构体有编译期对齐守卫，
// 加第 8 位而 wire 未跟即 typecheck 失败，故 GET /api/providers 永不会少报）
interface SandboxProviderCapabilities {
  spawnTty: boolean; volumeMount: boolean; updateResources: boolean;
  pauseResume: boolean; snapshot: boolean; watchEvents: boolean;
}
// 创建请求可"要求"的能力位子集（04 §5 创建前静态校验）——全部 optional，只有显式 true 才校验
type RequiredCapabilities = Partial<Omit<SandboxProviderCapabilities, 'watchEvents'>>;
//   ⚠️ 刻意**不含 `watchEvents`**：push/poll 对调用方完全封装（平台内部选订阅还是轮询 inspect），
//      "要求 watchEvents"对 API 调用方没有任何可观测意义，给了反而是个骗人的旋钮（04 §5）

// 掩码凭证（永不回明文，05 §4）
interface MaskedRuntimeCredential {
  id: string; kind: 'runtime'; runtimeId: string;
  mode: RuntimeAuthMode; obtainedVia: ObtainedViaRuntime;   // mode=生效模式 account|api-key（区别于鉴权方式 method）
  maskedIdentifier: string;        // 帐号 a***@gm / key sk-...ab12
  active: boolean;                 // 是否当前生效模式
  expiresAt?: string;              // ISO；API key 常无
  lastUsedAt?: string;
}
interface MaskedGitCredential {
  id: string; kind: 'git'; type: 'ssh-key' | 'https-token';
  maskedIdentifier: string;        // SSH=指纹 SHA256:… / token=尾号
  platform?: 'github' | 'gitlab' | 'gitee' | 'other';   // gitee 明确支持（国内自建/云托管 git 常见）
  allowedHosts: string[];          // host 白名单（非敏感、明示，I2 / 03 §7.3 C）；https-token ≥1、ssh-key 可空
  knownHosts?: string; lastUsedAt?: string; createdAt: string;
}

// 鉴权挑战（不落表——23 D-6；由 auth helper 产出，05 §3）
interface AuthChallenge {
  challengeRef: string;
  method: ObtainedViaRuntime;   // = RuntimeAuthMethod（04 §3）：本次挑战对应的鉴权方式，对齐 04 §3/23 §7.3（P2-6 补齐）
  kind: 'url' | 'device-code' | 'paste-prompt';
  //   三种"要用户在浏览器里做点什么"的形态，决定前端渲染哪种界面：
  //   'url'         = 只给一个链接，用户点开在浏览器完成 OAuth 即自动成功（无需回填）
  //   'device-code' = 给一个设备码 + 验证链接，用户在浏览器输码授权，前端轮询到成功（Codex device-auth，userCode/expiresAt 15min）
  //   'paste-prompt'= 用户在浏览器授权后拿到一段【授权码】，需要【粘贴回来】提交（Claude setup-token：CLI 打印链接→浏览器授权→复制 code→粘贴，见 05 §3）
  //                   —— "paste" 指"粘贴授权码"、"prompt" 指"需要一个回填输入框"；这类挑战靠 completeAuth 提交 pastedText 完成
  verificationUrl?: string; userCode?: string;      // device-code
  expiresAt?: string;                               // device-code 15min
  instructions: string;
}
type AuthStatus = 'pending' | 'success' | 'expired' | 'error';   // 轮询返回
```

### 7.3 各上下文 DTO（请求体 + 响应体）

> 字段名 **camelCase**（审计 P1-5，DB 的 snake_case 映射在 repository 层）。端点↔DTO 对照见 §6 与 27 §2–§9。

```ts
// ===== sandbox（Task）=====
interface CreateSandboxRequest {
  projectId: string;                 // 确认步项目行所选（27 §10.2）
  branch?: string;                   // ★ 本轮新增。缺省 = 基线当前分支。取值来自
                                     // GET /:id/branches；在**门口**校验（分支不存在 →
                                     // 零副作用拒绝，04 §5），不是等工作区准备到一半才失败。
                                     // 生效方式是工作区准备阶段一次**本地** checkout
                                     // ——完整克隆之后不碰网络（03 §7.2★）
  runtime: string;                   // runtime id
  image: string;                     // 镜像 id；缺省平台默认 AIO
  provider?: ProviderId;             // 已注册的 provider id，缺省 'aio'（见 §7.2.1 可扩展类型原则——不是封闭联合）
  initialPrompt?: string;            // 可选任务指令；≤8000 字符；不落前端持久化
  headless?: boolean;                // 缺省 false
  timeoutMinutes?: TimeoutMinutes;   // headless=true 缺省 120；headless=false 传即 400
  require?: RequiredCapabilities;    // 能力前置条件（§7.2）。所选 provider 该位为 false → 409
                                     // UNSUPPORTED_CAPABILITY，校验在**解析项目/落库/进调度之前**（03 §3 / 04 §5）
}
// 另有一条**无条件**规则（不由 require 表达）：所选 provider `spawnTty=false` 一律拒绝——
// 本平台每个 agent runtime 都要 TTY（终端页 + runtime 鉴权入口），不支持就承载不了任何 sandbox（04 §2.5）

// GET /api/providers 的数组项——**扁平数组 ProviderDto[]，没有外层信封**
interface ProviderDto {
  name: string;                                 // = ProviderId（§7.2 开放 id）
  capabilities: SandboxProviderCapabilities;    // 7 位全量下发，前端据此按能力显隐控件
  isDefault: boolean;                           // 请求未指定 provider 时 create 用的那一档——
                                                // 默认档挂在每一项上，响应没有顶层 default 字段
}
interface SandboxDto {
  id: string; projectId: string; runtime: string; image: string; provider: string;
  status: SandboxStatus;
  waitingInput: boolean;             // 派生字段（§7.4；网关内存态投影，非 status）
  name: string;                      // Task 视角命名。默认值由**后端**在创建时从 initialPrompt 派生
                                     // （首行前 20 个 UTF-8 码点 + 省略号；无指令则 'Codex · 2026-08-10 14:23'），
                                     // 规则权威 P21-1 §9；用户改名后不再自动覆盖
  headless: boolean; createdAt: string; lastActiveAt?: string;
  failureCode?: string;              // status='failed' 时的**机器码**（04 §4 闭集，兜底 INTERNAL）。
                                     // 存在的理由是 provision 异步：失败时调用方早已拿到 202，没有
                                     // 同步响应可承载它；WS 的 errorCode 是即时通道但错过即丢，
                                     // **刷新后仍能解释失败**只能靠这个字段（源 sandboxes.failure_code）
  failureMessage?: string;           // 自由文本细节，排障用；**不是给用户看的那句话**——
                                     // 人话由前端按 failureCode 查 P22 §1 出
  health?: SandboxHealth;            // 运行期健康度（03 §7.8）。★ **可选，且刻意不是第 13 个 status**：
                                     // 翻转的是 health.state，`status` 保持 running（SANDBOX_STATUSES
                                     // 那 12 个取值一个都没动）。缺席 = 平台这一刻没有观测（刚重启 /
                                     // 非 running·idle / provider 答不上）——⛔ **缺席不等于 unhealthy**，
                                     // 老客户端读不到它时行为与今天完全一致
  // 资源/配额不对外暴露（P22 §4.6）
  // ★ 刻意【不含 initialPrompt】（S5 裁决 D-14，TASK-LAUNCH-DECISIONS T-1）：
  //   ① 展示侧没有消费方——列表要的是默认任务名，那已由 name 承接；
  //   ② MCP 面是决定性理由：list_sandboxes / get_sandbox 与 REST 共用本 DTO，回显意味着
  //      一个被诱导的上层 agent 一次 list_sandboxes 就能读走全部历史任务指令（可能含仓库路径、
  //      内部系统名、业务上下文——正是前端红线要防的那类内容）；
  //   ③ 前端红线的原文是"不落前端持久化"，与"后端能不能回显"是两件事——但既然没有消费方，
  //      就不开这个口子。将来真需要（如"重新发起相同任务"），加字段比删字段容易。
  //   注：后端【落库】initialPrompt（sandboxes.initial_prompt，13 §2.1.1），这不违反前端红线。
}
// 运行期健康度（03 §7.8）。SPI 侧同形的那一份是 `HealthStatus`
// （packages/contracts/src/sandbox-provider.contract.ts），两仓由一条编译期断言钉住。
interface SandboxHealth {
  state: 'healthy' | 'unhealthy' | 'unknown' | 'starting';
  lastCheckedAt: string;
  message?: string;                  // 一行判据（哪个信号翻的），排障用，不是用户文案
  consecutiveFailures: number;       // ⚠️ DTO 上这个数是**平台**的抗抖动计数（跨采样，连续 3 次
                                     // 确认失败才翻）；provider 在 `SandboxRuntimeStatus.health`
                                     // 里报的那个是它**这一次**观测所知道的。同形不同源
}
interface DestroySandboxRequest { keepVolume?: boolean; }   // 勾选保留成果卷=true（默认 UI 勾选）
interface ExecRequest { command: string; }                 // POST /:id/exec（非交互命令）
interface ExecResult { exitCode: number; stdout: string; stderr: string; }
// —— 无头 Task（S6）——
interface RunAgentTaskRequest {                            // POST /:id/runtimes/:rt/tasks → 202
  prompt: string;                                          // ≤8000，与 initialPrompt 同一口径
  timeoutMinutes?: 30 | 60 | 120 | 240;                    // 缺省 30
  resumeFrom?: string;                                     // 上一轮的 sessionRef，给了就是接着上次聊
  extraArgs?: Array<'--verbose'>;                          // ⚠️ 白名单枚举，【不是】自由字符串数组
}
interface AgentTaskDto {                                   // 202 的响应体，也是 GET 的响应体
  id: string; sandboxId: string; runtime: string;
  status: 'running' | 'succeeded' | 'failed' | 'killed' | 'timed_out';
  exitCode?: number;                                       // 仅终态，且【可能缺席】（被信号杀掉没有退出码）
  sessionRef?: string;                                     // 下一轮填进 resumeFrom 即续接
  timeoutMinutes: 30 | 60 | 120 | 240;                     // 本次运行的硬超时预算；有它才能算倒计时
                                                           //（只有 startedAt 只能显示"已跑多久"）
  lastSeq: number;                                         // ⚠️【体检上界，不是恢复点】——fromSeq 是排他的，
                                                           //    拿它当 fromSeq 会一条都回放不到（§7.4）
  artifacts: Array<{ name: string; size: number; modifiedAt: string }>;  // 清单，不是字节
  errorCode?: string;                                      // 永远是码不是句子
  startedAt: string; finishedAt?: string;
}

// ===== project =====
interface CreateProjectRequest {
  name: string; sourceType: ProjectSourceType;
  repoUrl?: string;
  repoBranch?: string;   // 契约里一直有；本轮前端表单才把它接上（P21-6）
}
interface ProjectDto {
  id: string; name: string; sourceType: ProjectSourceType;
  cloneStatus: CloneStatus; cloneErrorCode?: string;
  taskCount: number; createdAt: string;
  // ★ 以下四项本轮新增。**推翻了此前那条"「来源」字段不对外展示（产品定案）——
  // repoUrl 不入 DTO"**：那条定案的场景是"用户不需要知道代码从哪来"，而现在产品
  // 要求项目只读条显示远端情况（P21-6 §N），并且要能看出"我的基线是什么时候的"。
  // 这四个值**库里一直有**（projects 表的 repo_url / repo_branch /
  // baseline_size_bytes / updated_at，13 §2.2），只是没进 DTO —— 不是没做，是卡住了。
  repoUrl?: string;            // 空项目为 undefined
  repoBranch?: string;         // 建项目时未指定则为 undefined（= 远端默认分支）
  baselineSizeBytes?: number;  // 完整克隆之后这个数字变重要（03 §7.2★ 磁盘）
  updatedAt: string;           // 最后一次 sync（或创建）的时刻
}
type ProjectBranches = string[];   // GET /:id/branches；读本地引用，空项目/cloning/failed 均为 []
// 无 body：POST /:id/retry-clone、POST /:id/convert-to-empty（仅 failed 态，非该态 409）
//          POST /:id/sync（仅 ready 态，非该态 409）

// ===== 保留卷（v1.1；表设计见 13 §2.2.2，不变量见 23 §6.2）=====
interface RetainedVolumeDto {
  id: string;                  // uuid v7 —— **就是 DELETE/下载 用的那个 id**
  projectId: string;
  sandboxId?: string;          // 来源 Task。⚠️ 弱引用：sandbox 记录归档后置 undefined，卷仍可管理
  source: 'manual-destroy' | 'automation-artifact';
  retainedAt: string;
  retainUntil: string;         // 到点由 VolumeReaper 清理（3/7/30 天）
  // ★ 两个大小都给，因为它们差一个数量级，只给一个必然误导（实测本仓 web 工作区：
  //   磁盘占 1.0 GB、下载包 14 MB —— 差 70 倍）。清理决策看前者，下载预期看后者。
  diskBytes: number;           // 宿主目录实占（reflink 之后可能远小于逻辑大小）
  downloadBytes: number;       // 打包后的 tar 字节数 —— 与 Content-Length 同一个数
  // ⛔ **不含 workspacePath**：宿主绝对路径对外等于泄露部署布局，而前端定位一条记录
  //    用 id 就够。它留在库里（UNIQUE，23 I-RV-3 靠它保证同一目录不被登记两次）。
}
// GET /api/retained-volumes?projectId=  → RetainedVolumeDto[]（不含已清理的，deletedAt 非空即只读）
// DELETE /api/retained-volumes/:id      → 204
// GET /api/retained-volumes/:id/archive → **tar 流**，见 §6.1 那三行注释

// ===== runtime / 凭证 =====
interface RuntimeDto {
  id: string; displayName: string; vendor: string;
  authMethods: Array<'oauth-device' | 'setup-token' | 'api-key'>;   // getAuthMethods 下发
  credentialStatus: CredentialStatus;
  maskedIdentifier?: string; expiresAt?: string; activeAuthMethod?: RuntimeAuthMode;
}
interface BeginAuthRequest { method: 'oauth-device' | 'setup-token'; }        // POST /api/runtimes/:rt/auth/begin → AuthChallenge
interface CompleteAuthRequest { challengeRef: string; pastedText?: string; }  // setup-token 粘贴
interface SubmitSecretRequest { method: 'api-key'; secret: string; }          // POST .../credentials/secret（不经 sandbox）
interface SetAuthModeRequest { method: RuntimeAuthMode; }                 // PUT .../auth-mode；目标未配置→409
//   body 字段名统一为 method（与 BeginAuthRequest/SubmitSecretRequest 一致，交叉评审 P1-1）——
//   前端发 mode、后端收 method 会 400。取值仍是 RuntimeAuthMode（account|api-key）=生效【模式】类别，
//   区别于 begin/submit 的 method（oauth-device|setup-token|api-key）鉴权【方式】；领域/响应里的 mode 字段保留（见 MaskedRuntimeCredential.mode）

// ===== git 凭证 =====  读 GET /api/credentials?kind=git（kind 必填）
interface CreateGitCredentialRequest {
  type: 'ssh-key' | 'https-token'; secret: string;
  platform?: 'github' | 'gitlab' | 'gitee' | 'other';   // gitee 明确支持（国内自建/云托管 git 常见）
  allowedHosts: string[];   // host 白名单；'https-token' 必须 ≥1（C 裁决 / I-CRD-8），'ssh-key' 可空（仅记录展示）
}
interface TestGitConnectionRequest { repoUrl?: string; }
interface TestGitConnectionResult { ok: boolean; errorCode?: string; message: string; }   // 不回 ref 名

// ===== image =====
interface ValidateImageRequest { ref: string; }                              // 注册前预检，不落库
interface ImageValidationResult { status: ImageValidationStatus; digest?: string; warnings: string[]; errors: string[]; supportedRuntimes: string[]; }
interface EnvVar { key: string; value?: string; secret: boolean; }           // secret 传空 value=保持不变
interface ImageConfig { env: EnvVar[]; cmdOverride?: string; }
interface PatchImageRequest { isActive?: boolean; imageConfig?: ImageConfig; }  // 两调用方各发各字段（F21-4）
interface ImageDto {
  id: string; name: string; ref: ImageRef;
  validationStatus: ImageValidationStatus; supportedRuntimes: string[];
  isActive: boolean; isBuiltin: boolean; lastValidatedAt?: string;
}

// ===== automation（v1.1）=====
interface AutomationScheduleConfig { minute?: number; time?: string; days?: number[]; }  // 不含时区
interface CreateAutomationRequest {
  name: string; description?: string; runtime: string; prompt: string;    // ≤8000 字符
  scheduleKind: ScheduleKind; scheduleConfig: AutomationScheduleConfig;
  timezone: string;                        // IANA 快照（23 I-AUT-9）；编辑时不隐式重传
  timeoutMinutes: TimeoutMinutes; artifactRetentionDays: ArtifactRetentionDays;
  webhookUrl?: string; triggerOn?: TriggerOn;
}
interface UpdateAutomationRequest extends Partial<CreateAutomationRequest> {}
//   ⚠️ **`timezone` 也是 optional，缺席 = 原样保留**（I-AUT-9）。写成必填会让前端每次改
//      prompt 都把「当前浏览器时区」再传一遍，用户换台机器就把凌晨任务挪走了。
interface AutomationDto {
  id: string; projectId: string;
  enabled: boolean; degraded: boolean; consecutiveFailures: number;
  lastTriggeredAt?: string; nextTriggerAt?: string;
  createdAt: string; updatedAt: string;
  /* + Create 字段全部回显：name / description? / runtime / prompt /
     scheduleKind / scheduleConfig / timezone / timeoutMinutes /
     artifactRetentionDays / webhookUrl? / triggerOn（必有，默认 'failure'） */
}
interface AutomationRunDto {
  id: string; automationId: string;
  sandboxId?: string;                 // 弱引用；skipped/missed 时缺席（13 §2.7.2）
  status: AutomationRunStatus;
  errorCode?: 'PREVIOUS_RUNNING' | 'AUTH_EXPIRED' | 'RESOURCE_EXHAUSTED';
  errorMessage?: string;
  retryCount: number; retryAt?: string;
  triggeredAt: string;                // ★ 每条 run 都有：调度器决定触发的那一刻
  startedAt?: string;                 // ★ optional —— 见下
  completedAt?: string;
  durationMs?: number;                // 库里存的是 duration_sec，线上给毫秒
  outputSummary?: string; webhookStatus?: WebhookStatus;
}
//   ★ **`startedAt` 从必填改成 optional，并新增必有的 `triggeredAt`（2026-08-31 落地时更正）。**
//     上一版写成 `startedAt: string` 必填，对不上 13 §2.7.2（`started_at` 可空、
//     `triggered_at` 非空）：`skipped` / `missed` 的 run 是**触发时刻直接落定**的
//     （I-AUR-1），它们根本没有「开始执行」这个时刻。必填只能靠拿 triggeredAt 顶上，
//     而那会让「排队等了多久」这个差值永远是 0。
interface PaginatedAutomationRuns extends Paginated<AutomationRunDto> {}   // GET .../runs
interface WebhookTestRequest { url: string; }
interface WebhookTestResult {
  ok: boolean;
  errorCode?: 'VALIDATION_FAILED' | 'HOST_NOT_ALLOWED' | 'TIMEOUT' | 'UPSTREAM_UNAVAILABLE';
  message: string;
}
//   ⚠️ 这个端点**恒 200**，成败在 `ok` 里（理由见 §6.5）。四个码全是既有码，不新造。

// ===== system =====（✅ 六个端点已落地，2026-08-28；权威源 api/packages/contracts/src/schemas/system.schema.ts）
interface InitStatusDto {
  initialized: boolean;
  initializedAt?: string;                       // 未初始化时缺席
  lastConnectivityCheck?: ConnectivityResult[]; // ⚠️ 附**上次**结果，本端点不重跑检测
  lastConnectivityCheckAt?: string;             // 没有它，前端无从判断那份结果是三秒前还是三周前的
}
interface ProxyConfig { httpProxy?: string; httpsProxy?: string; noProxy?: string; }
interface InitRequest { proxyConfig?: ProxyConfig; acknowledgeOffline?: boolean; }
//   ⚠️ `acknowledgeOffline` 不是可有可无的礼貌开关：模型 API 全挂且未带它 ⇒ **409**（零副作用）。
//      门的作用是保证「Agent 将不可用」这句话**被说出来过**（P21-8 §1 物理约束）；带上它照常放行。
interface SystemSettingsDto {                   // GET /api/system/settings
  initialized: boolean;
  proxyConfig?: ProxyConfig;
  publicBaseUrl?: string;
  accessPasscodeEnabled: boolean;               // ⛔ **永不回显 hash**，只出「启没启用」
  accessPasscodeUpdatedAt?: string;
  version: { platform: string; node: string };  // platform 来自 PLATFORM_VERSION，没注入就是 'dev'（不编版本号）
}
interface UpdateSystemSettingsRequest {         // PUT /api/system/settings —— ⚠️ **只存配置、不放行**
  proxyConfig?: ProxyConfig | null;             // null=清空，缺席=不改（少了前者，配错的代理就删不掉）
  publicBaseUrl?: string | null;
}
interface AccessPasscodeRequest { action: 'enable' | 'regenerate' | 'disable'; }   // ⏳ 端点尚未落地
interface AccessPasscodeResult { passcode?: string; }        // 启用/重生成一次性返回明文；此后只存 hash
type ResourceLevel = 'ok' | 'warn' | 'critical';   // 阈值在服务端算好再下发，前端不重算（P21-5 §5）
interface SystemResourcesDto {                     // GET /api/system/resources
  cpu: { cores: number; loadAvg1m: number; usedPercent: number; level: ResourceLevel };
  //   ⚠️ **`cpu.usedPercent` 是 `loadAvg1m / cores`，那是「饱和度」不是「CPU 时间占用率」。**
  //      两者相关但不相等：loadavg 数的是**可运行线程数**（Linux 上还含 D 态的不可中断 IO
  //      线程）。本机实测 loadavg 2.17/10 核 = 21.7%，而同一时刻真实占用只有 16%
  //      （top: 11.2% user + 4.8% sys）。⏳ 已知风险：本平台恰恰是重 IO 的（clone 大仓、
  //      拉 13GB 镜像），Linux 上 D 态线程会把 loadavg 顶高 ⇒ 有「CPU 空闲却报 critical」
  //      的假阳性路径。字段名与口径**本轮未改**（前端刚照它落地），单独记为待办。
  ram: { totalBytes: number; usedBytes: number; usedPercent: number; level: ResourceLevel };
  //   ⚠️ **`usedBytes` = 总量 − 「**可用**内存」，而「可用」**不是** `os.freemem()`。**
  //      `freemem()` 只算当前完全空闲的页：macOS 上把 inactive/speculative（都可回收）
  //      算成已用，Linux 上不含 buff/cache。实测一台真实占用 59% 的机器被它报成 96.3%
  //      ⇒ `level: critical` ⇒ 前端整页「资源耗尽，无法创建新 Task」（P21-5 §9E）。
  //      取数：Linux 读 `/proc/meminfo` 的 `MemAvailable`（内核给的权威答案）；
  //      darwin 读 `vm_stat` 的 `free + inactive + speculative`。
  //      ⚠️ 两个平台都取不到时 **`level` 钉在 `ok`** —— 测不准的读数绝不该驱动
  //      「能不能建 Task」这种硬判定（与 reflink 那条三态同一纪律）。
  disk: { path: string; totalBytes: number; usedBytes: number; availableBytes: number;
          usedPercent: number; level: ResourceLevel; reservedPercent: number };
  //   ⚠️ 量的是 **DATA_ROOT 所在的文件系统**（平台真正会写满的那一个），可用量用 `bavail`
  //      而不是 `bfree` —— 差值是 root 保留块，200GiB 盘上是 10GiB 的虚账（P1-9）
  retainedVolumes: { count: number; totalBytes: number; percentOfDisk: number;
                     level: ResourceLevel; oldestExpiresAt?: string; truncated: boolean };
  //   ⚠️ `truncated` 是一等字段：目录太多时统计会停，**截断了却说 45GB** 会让用户清完发现
  //      没腾出预期的空间，此后不再信这个数字
  activeTasks: number;
}
interface ProviderHealthDto {
  id: string; capabilities: SandboxProviderCapabilities; isDefault: boolean;
  healthy: boolean;                  // 最近 1h 失败率 ≤10%（P21-5 §3 的 ❌ 线）
  recentFailureRate?: number;        // ⚠️ `sampleSize: 0` 时**缺席** —— 0/0 不是 0%
  sampleSize: number;                // 窗口内该 provider 建过的沙箱数（失败率的分母）
  failureCount: number;
}
//   ⚠️ `capabilities` 原写作 `number`（位掩码），与 04 §2.5 的布尔结构体冲突——已按 04 更正。
//   ⚠️ **`healthy` 不是「刚探测过它活着」**：`SandboxProvider` 契约里没有健康探测方法
//      （04 §2.2 六必需 + 两可选里没有它），凭空 create/destroy 去探活既有副作用又慢。
//      它用平台已经拥有的事实 —— `sandboxes` 表里这个 provider 最近一小时的成败。
//      `sampleSize: 0` 是「这一小时没人用过它」，前端应照实说「无样本」而不是「正常」。
interface RuntimeHealthDto { id: string; displayName: string; vendor: string; authMethods: string[]; credentialConfigured: boolean; }
interface ImageSpecHealthDto { id: string; isDefault: boolean; }
interface SystemProvidersDto {      // GET /api/system/providers —— **是信封不是扁平数组**
  providers: ProviderHealthDto[]; runtimes: RuntimeHealthDto[]; imageSpecs: ImageSpecHealthDto[];
  healthWindowMs: number;           // 让前端照实说「最近 1h」而不是硬编码一个数字
}
interface ConnectivityResult {
  target: string;                   // `host` 或 `host:port`（非默认端口时带出来 —— 否则用户
                                    //   认不出说的是哪个 registry）。不含 path、不含凭证
  ok: boolean; latencyMs?: number; hint?: string;
  modelApi: boolean;                // ⚠️ **离线判定只看这一类**：镜像仓库不可达是「拉不到新镜像」，
                                    //    模型 API 不可达才是「Agent 不可用」。混作一谈会让一个只是
                                    //    内网镜像站没配好的部署被告知「Agent 将不可用」
}
// —— 诊断 SSE 的流帧类型**不在本节**：openapi 生成不出它，两仓各持一份手写副本
//    （`sse-protocol.ts`，B5 跨仓对账），形状见 02 §5.3 与 §6.7 的边界说明。
// —— 审计流（13 §2.8.2 / §6.6.1）——
type AuditCategory = 'sandbox' | 'project' | 'credential' | 'image' | 'system';
//   ⚠️ **闭集 ≠ 今天在写。** 五个类别现在**都有生产者**了（逐类的 `type` 清单见 13 §2.8.2
//      「各 category 的生产者清单」）：`image` / `system` 此前是空的——前端因此手抄了一份
//      `AUDIT_CATEGORY_EMIT_STATUS` 用来把「筛出来没有」与「压根没记」分开说，openapi 表达不了
//      这层差别。后端补上写入点而那份抄本没跟着改，页面上明明有数据、空态却还在说「尚未记录」。
type AuditSeverity = 'info' | 'warn' | 'error';
interface AuditEventDto {
  seq: number;                    // 单调游标；前端据此比较/翻页/判断断层（公开列，非 opaque）
  at: string;                     // ISO
  category: AuditCategory;
  type: string;                   // 'sandbox.provision.stage' 等；开放集合,前端不得穷举 switch
  severity: AuditSeverity;
  subjectType?: string; subjectId?: string;
  actor: string;                  // 取值全集 = TriggeredBy ∪ {'system'}：scheduler / reaper / user / health-check / provider-event / system
                                  //   ⚠️ 契约里由 `AUDIT_ACTORS` **派生**（= `TRIGGERED_BY` + `'system'`），不是手抄的一串字面量。
                                  //      本行此前写作 `user / system / reaper / mcp / automation`——后端**每次 provision 都在写**的
                                  //      `scheduler`、以及 projector 原样透传的 `health-check` / `provider-event` 一个都不在，
                                  //      而 `mcp` / `automation` 后端一处都不写（automation 是 v1.1；MCP caller 建沙箱走的是同一条
                                  //      `SandboxCreated`，actor 就是 `user`）。前端 `ACTOR_LABELS` 按这份清单翻中文，漂一个就在
                                  //      中文界面上漏一个英文标识符。列上**没有 CHECK**，所以 DTO 侧仍是 `string`（读那一侧不得因此报错）
  summary: string;                // 一行人话,直接上 UI
  detail?: Record<string, unknown>;   // 已在写入口脱敏(13 §2.8.2)
  durationMs?: number;
  outcome?: 'ok' | 'failed' | 'skipped';
  errorCode?: string;             // 与 §6.8 同一闭集
}
interface AuditListDto { items: AuditEventDto[]; hasMore: boolean; }
//   ⚠️ `items` 恒按 seq 降序；`hasMore` 在 since 方向表示「还有更新的没拉完」= 有断层,
//      在 before 方向表示「还有更老的」= 可继续滚。两个方向共用一个字段但含义不同,见 §6.6.1
```

### 7.4 WS 帧类型

> **本节是 WS 协议的唯一 canonical 定义**（§3 已删除，其语义表并入本节下方）。两个通道各有一套判别字段：**/terminal 帧用 `type`、/events 事件用 `event`**——这是刻意的（终端是字节流帧协议、事件是业务投影，两套无关的 union，判别字段名不同以防误 parse 到对方）。前后端各持一份 `ws-protocol.ts`，`SYNC WITH` 注释指向本节，改动经 X-Schema-Hash CI 比对（§1、14 §2.5）。

```ts
// —— /terminal 通道 ——（URL query 带 socketSessionKey，服务端生成，06 §6；判别键 type）
type TerminalClientFrame =
  | { type: 'input'; data: string }              // 用户键入（plain string，非 base64）
  | { type: 'resize'; cols: number; rows: number }
  | { type: 'ping' };
type TerminalServerFrame =
  | { type: 'data'; data: string }               // pty 输出（plain string；xterm 直接 write，无需解码；16ms 批量转发 + 前端 rAF 批处理）
  | { type: 'exit'; code: number }               // agent CLI 进程退出（137=OOM 等）——前端据此显示 exit code + 人话解释（08 §8）
  | { type: 'pong' }
  | { type: 'session'; socketSessionKey: string }; // 开会话首帧下发重连凭据

// —— /events 通道 —— 判别键 event
type SandboxEvent =
  | { event: 'sandbox.created'; sandboxId: string; projectId: string }
  | { event: 'sandbox.status_changed'; sandboxId: string; status: SandboxStatus; phase?: string; errorCode?: string }  // errorCode 仅 status:'failed' 时出现；是**码**不是句子（§3.1 第 2 条纪律）
  | { event: 'sandbox.removed'; sandboxId: string }
  | { event: 'sandbox.waiting_input'; sandboxId: string; waiting: boolean; sessionId?: string }  // 翻转时推；可丢失；不进 Outbox（06 §8）
  | { event: 'project.clone_progress'; projectId: string; phase: 'cloning' | 'slow' | 'done' | 'failed';
      stage?: 'enumerating'|'counting'|'compressing'|'receiving'|'resolving'|'checkout';
      percent?: number; objectsDone?: number; objectsTotal?: number;
      receivedBytes?: number; bytesPerSecond?: number; errorCode?: string }
  // ★ `phase` 是平台语义（cloning/slow/done/failed），`stage` 是 git 自己的阶段名——两者不是一回事，
  //   前者决定弹层形态，后者只喂明细行。⚠️ 原有的 `totalBytes?` 已删：git clone 不报总字节数，
  //   后端从未发过它，而前端有一条 `receivedBytes && totalBytes` 的分支在等它（03 §7.2★）。
  | { event: 'runtime-auth.status_changed'; runtime: string }
  | { event: 'runtime.install_progress'; sandboxId: string; runtime: string; status: RuntimeInstallStatus; versionDetected?: string; errorCode?: string }  // 装 CLI 进度（03 §4.3 ③）；进 Outbox；status 复用 §7.1 的 RuntimeInstallStatus
  | { event: 'sandbox.instance_progress'; sandboxId: string; phase: 'starting' | 'ready'; imageStaged?: boolean };  // 起实例那一步的两个**边界**（03 §4.3 ①）；**不进 Outbox**
  // ★ 为什么是第 8 条而不是塞进 `sandbox.status_changed`：与 install_progress 同一条论证，
  //   且更硬 —— `provider.start()` 期间 status 恒为 `starting`，实测 **190529ms**（13GB 镜像
  //   本机首次使用，微 VM 是懒的：create 只要 ~4ms，第一次 exec 才真拉+铺 rootfs）。
  //   折进去会发出一串「状态没变的状态变更」，`phase?` 字段不是豁免，只是把违反藏起来。
  // ★ **没有百分比、没有 ETA、没有 elapsedMs**，三个缺席都是刻意的：
  //   · `start()` 就是一个 await，只有「开始」和「结束」，没有 40%/80% 这种回调可读
  //     —— 编一个就是第二个 `totalBytes` 幽灵字段；
  //   · 「已等待多久」前端自己算（它知道自己何时收到 `starting`），为它加字段等于让唯一的
  //     读者去跟一个它本来就有的值对账，还要跨仓同步一次。
  // ★ `imageStaged` 才是后端**独有**的事实：本机有没有这份镜像的位（`SandboxProvider.imageStaged`，
  //   04 §11「minor = 新增可选方法」）。**缺席 ≠ false**：缺席是「provider 说不出」，
  //   false 是「确实没有」，只有后者能当「你要等几分钟」的理由。
  // ★ 不进 Outbox（与 install_progress 相反，理由是这一条特有的）：丢一帧 `starting` 只让
  //   这一次等待退回中性文案，丢一帧 `ready` 会被随后必到的 `status_changed` 盖掉；
  //   而 install_progress 后面没有别的东西跟上，丢了就永远钉在陈旧文案上。

// —— /tasks 通道（S6 无头 Task 输出流）—— 判别键 type（是流，不是投影）
type TaskClientFrame =
  // ⚠️ fromSeq 是【排他】的："到 N 为止我已经有了，给我 N 之后的"。不给 = 从头。
  //    因此 AgentTaskDto.lastSeq 不能当 fromSeq 用——那会一条都回放不到。
  | { type: 'subscribe'; taskId: string; fromSeq?: number }
  | { type: 'unsubscribe'; taskId: string }
  | { type: 'ping' };                                      // 可选；心跳靠 socket.io engine 层

type TaskServerFrame =
  | { type: 'event'; taskId: string; seq: number; event: RuntimeEvent }   // seq 稠密单调，跳号=bug
  // firstSeq = 本次回放【实际发出的第一条】的 seq（一条没发时为 seq+1）。
  // 它是判断"开头被截断"的唯一手段：只看跳号只能发现中间的洞，发现不了缺头。
  | { type: 'caught_up'; taskId: string; firstSeq: number; seq: number }
  // 终态。⚠️ 订阅一个【已经终态】的任务，回放完之后也必须收到这一帧——否则前端得
  //   从 REST DTO 绕回去还原退出码，同一个事实两个来源。
  | { type: 'exit'; taskId: string; status: TaskStatus; exitCode?: number }
  | { type: 'error'; taskId: string; code: string }        // 永远是码
  | { type: 'pong' };

// 一条 RuntimeEvent 的载荷【按成员钉死】，不是 unknown——否则消费方只能猜字段名，
// 而改名不会有任何编译期报错（04 §3）。
type RuntimeEvent =
  | { type: 'session-started'; timestamp: string; data: { ref: string } }
  | { type: 'agent-message';   timestamp: string; data: { text: string } }   // agent 正文
  | { type: 'stdout-chunk';    timestamp: string; data: { text: string } }   // 无结构化模式时的原始字节
  // ⚠️ 载荷按 status 再判别一次：name 只在 started 半边（必填），consumer 按 id 配对。
  //    claude 的 tool_result 只带 tool_use_id，发空串等于"必填但有时是假的"。
  | { type: 'tool-call';       timestamp: string; data:
        | { status: 'started';   id: string; name: string; input?: unknown }
        // ⚠️ exitCode 只放真实退出码（codex 有）；isError 放"工具自己说它失败了"（claude 有）。
        //    两者不冗余，也不互相冒充——合成一个 exitCode:1 会和实测的 1 无法区分。
        //    判失败：isError === true || (exitCode !== undefined && exitCode !== 0)
        | { status: 'completed'; id: string; exitCode?: number; isError?: boolean; output?: string } }
  // ⚠️ 载荷【空】：退出码不在这里，在 exit 帧上（那是作业的事实，不是这一轮的）
  | { type: 'task-complete';   timestamp: string; data: Record<string, never> }
  | { type: 'error';           timestamp: string; data: { message: string } }
  | { type: 'auth-required';   timestamp: string; data: { method?: string } };
```

**决策记录（2026-08，交叉评审 P0-1）**：`data` 用 **plain string 非 base64**（xterm 直接 write，省一次编解码）；**保留 `exit` 帧**（04 的 `ProcessStream.onExit` 需要一条上报通道，删了前端无法得知进程退出）；终端帧判别键统一 `type`。此前 §3 与本节冲突（§3 用 `payload`/base64/无 exit/`SandboxSummary` 嵌套/clone 6 值）已删 §3、以本节为准。

### 7.5 错误 envelope

见 §6.8（`ErrorEnvelope`：`code / message / retryable / sideEffectFree? / traceId / details?[]`）——所有非 2xx 一律此形状，REST 与 MCP 同构。`sideEffectFree` 缺席表示「未表态」，按「可能有副作用」读（§6.8）。

### 7.6 类型总量与派生链

| 类别 | 数量 | 单源 |
|---|---|---|
| 枚举 | 19（含 AuthStatus） | 后端 zod `z.enum` / 字面量联合 |
| 通用值类型 | 10（含 `SandboxProviderCapabilities`——S6 起 **7 位** / `RequiredCapabilities`） | zod schema |
| DTO（请求+响应） | ~42（含 `ProviderDto` / `AgentTaskDto`） | `createZodDto()`（02 §3） |
| WS 帧/事件 | 3 通道 union（**8** 事件 + 3+3 终端帧 + 3+5 任务帧 + 7 成员 `RuntimeEvent`） | 手工同步 `ws-protocol.ts`（14 §2.5，带 X-Schema-Hash 握手；`/terminal` 与 `/tasks` 各自一个 hash） |
| 错误 envelope | 1 | §6.8 |

**一份类型的五处消费**（02 §3）：`zod schema` → ① `createZodDto()` REST DTO → ② `@nestjs/swagger` 反射 → `openapi.json` → ③ 前端 `openapi-typescript` → `openapi.d.ts`；zod schema 又直接作 ④ `@Tool({parameters})` MCP inputSchema、⑤ 运行时 `.parse()` 入参校验。**改一处 zod，五处同步**——CI 漂移检测（§2.1）+ X-Schema-Hash（14 §2.4）双保险。后端实现形态见 [28 类型与领域类型详细设计](../backend/28-类型与领域类型详细设计.md)，前端消费形态见 [前端 07/28]。

> **provider / capabilities 三个类型走的正是这条链，前端一个字都不手写**：`ProviderDto` / `SandboxProviderCapabilities` / `RequiredCapabilities` 在前端分别是生成物 `components['schemas']['ProviderResponseDto']`、该 DTO 的 `['capabilities']`、`components['schemas']['CreateSandboxDto']['require']` 的别名（前端 28 §1）。因此**后端加一位能力位 → `generate:api` → 前端编译期报红**，而不是运行时静默少一位。同理 ④：MCP tool `create_sandbox` 的 `require` 参数与 REST 共用同一份 schema，**无需单独实现即自动获得**（27 §11.1）。
