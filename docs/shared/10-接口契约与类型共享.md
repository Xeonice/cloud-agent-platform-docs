# 10 - 前后端接口契约与类型共享（独立仓库前提）

> 状态：✅ 可评审（后端侧确认 @nestjs/swagger 11.4.x 暴露 /openapi.json，双侧 CI 均做 openapi.json diff 校验，见文档 02 §8 与 09 §2.3；**§6 端点全量清单**与 §3 WS 事件已按产品定稿 P20/P21/P22 同步）
> 关联文档：[02 MCP 与 OpenAPI 双协议](../backend/02-MCP与OpenAPI双协议.md) · [07 前端目录结构](../frontend/07-前端目录结构与视图逻辑分离.md) · [03 调度中心](../backend/03-Sandbox调度中心.md) · [05 鉴权流转](../backend/05-Runtime鉴权流转.md) · [06 TTY 链路](../backend/06-TTY终端链路.md)

## 1. 结论先行

| 契约类型 | 方案 | 同步方式 |
|---|---|---|
| REST | `openapi-typescript`（生成纯类型）+ `openapi-fetch`（类型安全 client） | **生成后提交** + CI 漂移检测门禁 |
| WebSocket | 手写 `ws-protocol.ts` 双端同步 + **运行时 zod/valibot 校验兜底** | 手动同步 + CI hash 比对感知变更（文档 14 §2.5 方案 B）；改动频繁时升级为独立协议小包（方案 A） |
| 不采用 | 发布 npm 类型包 | 作为规模扩大后的平滑演进路径预留 |

## 2. REST 契约链路

```
NestJS (@nestjs/swagger)
      │  暴露 /openapi.json
      ▼
前端仓库: pnpm generate:api
      │  openapi-typescript https://backend/openapi.json
      │    -o src/types/generated/openapi.d.ts
      ▼
services/api/client.ts
      │  openapi-fetch 基于该类型创建强类型 client
      ▼
业务代码: client.GET('/api/sandboxes/{id}')  ← 天然类型推导，无需手写 DTO
          （后端设 setGlobalPrefix('api')，openapi paths 含 /api 前缀，见文档 02 §8）
```

选型理由（2026-08 现状）：

- `openapi-typescript`：约 11k star / 50 万周下载，生成**纯类型**（`.d.ts`），零运行时开销；被 TanStack Query 官方文档列为推荐范式之一。
- `openapi-fetch`：与上者配套，无代码生成的运行时薄封装。
- 备选 **Orval**（约 7k star / 35 万周下载）：可直接生成 React Query hooks + MSW mock。需要"开箱即用 hooks + mock"时再评估切换；当前推荐 openapi-fetch 保持数据层显式。

### 2.1 CI 契约漂移检测（关键门禁）

防止"后端契约变化被前端悄悄吃掉"：

```yaml
# static-checks job 内的一步（PR 硬门禁——与文档 09 §1.3、16 §6 的流水线一致）
- run: pnpm generate:api              # 重新从后端 /openapi.json 生成
- run: git diff --exit-code src/types/generated/
  # 有差异 → CI fail → 人工确认并提交更新
```

契约变更因此会出现在 PR diff 中被 review，比 npm 包版本号更新更透明。

### 2.2 为什么不发 npm 类型包

- 双仓库物理隔离，发包意味着版本号对齐、私有 registry、联调时 `npm link`/`file:` hack，对"简易管理入口"级项目工程成本偏重。
- "生成后提交"同样强类型，且改动可 review。
- **演进路径**：若未来多个前端消费同一后端契约，再升级为发布 `@org/api-types`（仅含生成 `.d.ts`，无运行时代码），与现方案不冲突。

## 3. WebSocket 契约

WS 没有 OpenAPI 级别的成熟工具链（AsyncAPI 生态尚不无缝），采用**最低工程成本 + 运行时兜底**：

1. **帧/事件类型的唯一 canonical 定义在 [§7.4](#74-ws-帧类型)**（`TerminalClientFrame`/`TerminalServerFrame` + `SandboxEvent`）——本节此前重复给过一份已删除（交叉评审 P0-1：与 §7.4 冲突过，判别键/字段名/base64/exit/clone phase 都不一致）。后端仓库以 discriminated union 定义、双端各持一份 `ws-protocol.ts`。
2. 双端文件顶部加 `// SYNC WITH shared/10 §7.4 — 修改需双端确认` 注释。
3. **前端对收到的每帧做 zod/valibot `parse`**——类型意外漂移时开发环境立刻抛错，而不是 UI 静默 undefined。
4. **变更感知**：后端 CI 对协议文件算 hash 写入 release asset，前端定时 job 比对，不一致即失败提醒人工同步（文档 14 §2.5 方案 B）；协议改动趋于频繁时升级为独立 npm 协议小包 + Renovate 自动开 PR（方案 A，仅含类型与 zod schema、无运行时代码——与本文档"REST 不发包"的结论不冲突：REST 有 codegen 这条更优路径，WS 没有）。

### 3.1 事件语义表（前端 patch 行为的权威）

| 事件 | 触发时机 | 前端处置 | 后端落点 |
|---|---|---|---|
| `sandbox.created` | 创建请求受理、pending 记录落库后 | 列表插入（或对齐乐观项） | 03 §4 |
| `sandbox.status_changed` | **每一次**状态机转移（含新增的 `preparing-workspace`） | patch 列表项状态 + 驱动阶段进度卡 | 03 §4，事务内双写后经 Outbox 投递（13 §3） |
| `sandbox.removed` | destroyed 终态 | 移除列表项 | 03 §4 |
| **`sandbox.waiting_input`** | `running` 子态**翻转时**（不周期性重发） | 列表项切 🔵 等待输入 图标；**仅展示，不改主状态** | 06 §8 网关内存态；判定与误报纪律见 03 §4.1 |
| **`project.clone_progress`** | 异步 clone 期间节流 **1s** 一帧；`phase:'slow'` 在超 10min 时补发一帧 | 创建弹层进度态；`slow` 出提示、`failed` 就地红字按 `errorCode` 分支引导 | 03 §7.2 |
| `runtime-auth.status_changed` | 凭证过期/吊销/重授权成功 | invalidate `['runtime-auth']` + 横幅 | 05 §5 |

两条纪律：

- **`sandbox.waiting_input` 与 `project.clone_progress` 都是"可丢失"事件**——前者丢了下次翻转会重发（且有 REST 派生字段 `waitingInput` 兜底），后者丢了不影响最终 `clone_status`（REST 可查）。因此**都不进 domain_events Outbox**，直接由网关/编排器推送；进 Outbox 的只有状态类事件（需要可靠投递与回放）。
- 高频事件必须节流：clone 进度 1s、waiting_input 只在翻转推——WS 通道与终端数据流共用连接预算，事件洪水会挤压终端体验。

## 4. 与 sandbox 列表更新的配合

- WS 事件通道推送 `sandbox.*` 事件 → 前端 `queryClient.setQueryData` patch 缓存（推送为主）。
- TanStack Query 保留 30–60s 低频轮询兜底（WS 断线期间防陈旧）。

## 5. 风险与备选

| 风险 | 等级 | 缓解 |
|---|---|---|
| 生成后提交模式下前端未及时重新生成 | 中 | CI 漂移检测作为强制门禁（§2.1），不依赖自觉 |
| WS 手动同步漂移 | 中 | 运行时 schema 校验兜底 + artifact 拉取增强 |
| 后端 OpenAPI 注解不全导致生成类型缺失 | 中 | 后端引入 `eslint-plugin-nestjs-typed` 校验 @ApiProperty 与字段类型对齐（文档 14 §8） |

## 6. REST 端点全量清单（前后端唯一权威副本）

> 生成物（`openapi.d.ts`）是类型的权威，本表是**路径与语义**的权威——前端写 service 方法、后端建 controller、Playwright 建 mock（12 §4.2）都以此对表。定义细节见各后端文档，此处只做全量索引。所有路径含全局前缀 `/api`。

### 6.1 sandbox（Task）

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | `/api/sandboxes?projectId=&status=` | 响应含派生字段 `waitingInput`（取数经 terminal 只读端口，P1-12） | 02 §5.1 |
| GET | `/api/sandboxes/:id` | | 02 §5.1 |
| POST | `/api/sandboxes` | **202** + pending 记录；`timeoutMinutes` 缺省=120（headless）/ 传了即 400（交互式），P1-13 | 02 §5.1 |
| POST | `/api/sandboxes/:id/start` · `/stop` | | 02 §5.1 |
| DELETE | `/api/sandboxes/:id` | **`{ keepVolume?: boolean }`**（body 或 `?keepVolume=`，等价） | 02 §5.1 / 03 §7.7 |
| POST | `/api/sandboxes/:id/exec` | | 02 §5.2 |
| POST | `/api/sandboxes/:id/runtimes/:rt/tasks` | 无头任务 | 02 §5.2 |

### 6.2 project

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | `/api/projects` | 含各项目 Task 数聚合 + `cloneStatus` | 02 §5.1 |
| POST | `/api/projects` | git **202 异步**，进度经 WS | 03 §7.2 |
| GET | `/api/projects/:id` | 含 clone 失败的 `errorCode` | 03 §7.2 |
| POST | **`/api/projects/:id/retry-clone`** | 重试克隆（`failed`→`cloning` 显式重置）；非 failed 态 → 409 | 02 §5.1 / 03 §7.2 |
| POST | **`/api/projects/:id/convert-to-empty`** | 改为空项目（`failed`→`ready`，转 `sourceType='empty'`、删半成品基线目录、**保留 id/名称/已关联 Task**）；非 failed 态 → 409 | 02 §5.1 / 03 §7.2 |
| DELETE | `/api/projects/:id` | 级联；cloning 态=取消克隆 | 03 §7.2 |
| GET | `/api/retained-volumes?projectId=` · DELETE `/api/retained-volumes/:id` | 已保留卷（统一资源前缀，P2-5） | 03 §7.7 |

### 6.3 runtime / 凭证（**全部无 sandbox 维度**——05 §2 决策 A）

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | **`/api/runtimes`** | 列表 + 卡片元数据 + **凭证状态聚合** | 02 §5.1 |
| GET | `/api/runtimes/:rt/credentials/status` | 单 runtime 状态（永不回明文） | 05 §3 |
| POST | `/api/runtimes/:rt/auth/begin` | 在 auth helper 内起登录 | 05 §3 |
| GET | `/api/runtimes/:rt/auth/status?challengeRef=` | device-code 轮询 | 05 §3 |
| POST | `/api/runtimes/:rt/auth/complete` | setup-token 粘贴 | 05 §3 |
| POST | `/api/runtimes/:rt/credentials/secret` | API key 直存 | 05 §3.1 |
| PUT | `/api/runtimes/:rt/auth-mode` | 模式二选一；目标未配置 → 409 | 05 §4 |
| DELETE | `/api/runtimes/:rt/credentials/:credentialId` | 吊销 | 05 §4 |
| GET | **`/api/credentials?kind=git`** | Git 凭证掩码列表（SSH 只回指纹 / token 只回尾号）。**`kind` 必填，MVP 只接受 `git`**——runtime 凭证的读取归 `GET /api/runtimes`，避免两条可达路径 | 02 §5.1 |
| POST | `/api/credentials/git` | `{ type, secret, platform? }`；同协议已有则按"更换"语义；passphrase 私钥拒绝 | 02 §5.1 / 05 §3.2 |
| POST | `/api/credentials/git/test` | `ls-remote` 测试（15s），**不回 ref 名** | 03 §7.4 |
| DELETE | `/api/credentials/git/:id` | 吊销；**无联动清除**（Git 凭证不注入 sandbox） | 05 §3.2 |

> ⚠️ **历史路径 `POST /api/sandboxes/:id/runtimes/:rt/auth/*` 已废弃**，任何仍按"选一个 running sandbox 作登录宿主"实现的前端代码需一并改（决策 A）。

### 6.4 image

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET / POST | `/api/images` | 列表 / 注册 | 04 §7 |
| POST | **`/api/images/validate`** | **注册前预检**，不落库（前端在用的就是它，P1-3） | 02 §5.1 |
| POST | `/api/images/:id/validate` | 已注册镜像重新验证，写回 `validationStatus` | 04 §7 |
| DELETE | **`/api/images/:id`** | 硬删除；有引用或预置镜像 → 409（P1-6） | 02 §5.1 |
| PATCH | **`/api/images/:id { isActive?, imageConfig? }`** | 修改 manifest 可变字段的**唯一入口**：启用/禁用 + 运行参数 env/cmdOverride；env 违规一律 400 + 逐项 `path`（`ENV_NAME_INVALID` / `ENV_NAME_RESERVED` / `ENV_LIMIT_EXCEEDED` / `ENV_DUPLICATE_KEY`） | 02 §5.1 · 校验权威 05 §4.1 |

### 6.5 automation（v1.1）

| 方法 | 路径 | 定义处 |
|---|---|---|
| GET / POST | `/api/projects/:id/automations` | P21-7 §8 |
| GET / PUT / DELETE | `/api/automations/:id` | P21-7 §8 |
| POST | `/api/automations/:id/enable` · `/disable` | 03 §8.4 |
| GET | `/api/automations/:id/runs`（分页）· `/api/automations/runs/:runId` | 03 §8 |
| GET | `/api/automations/runs/:runId/logs?offset=&limit=` | 03 §8.6 |
| POST | `/api/automations/webhook-test { url }` | 03 §8.5 |

### 6.6 system

| 方法 | 路径 | 备注 | 定义处 |
|---|---|---|---|
| GET | **`/api/system/init-status`** | 冷启动首屏据此决定是否进初始化向导；附上次出网检测结果 | 02 §5.1 |
| POST | **`/api/system/init`** | `{ proxyConfig?, acknowledgeOffline? }` → 写 `initialized=true`；**一次性操作，已初始化返 409**（不是幂等，P2-4）。出网检测复用 `/diagnose` | 02 §5.1 / P21-8 §2 |
| GET / PUT | `/api/system/settings` | 运行期读改代理等；**永不回显口令 hash** | 13 §2 / 11 §3.1 |
| PUT | **`/api/system/access-passcode`**（**MVP**，P0-3） | `{ action: enable\|regenerate\|disable }`；启用/重生成时**一次性返回 16 位明文**，此后只存 hash；不影响已通过 session | 02 §5.1 / 11 §3.1 |
| POST | **`/api/system/diagnose`** | **SSE `text/event-stream`**，单项超时 5s | 02 §5.3 |
| GET | `/api/system/resources` | 系统状态页看板（含**磁盘水位**与保留卷占用，P1-9） | P21-5 |
| GET | **`/api/system/providers`** | 已注册 provider/runtime/imageSpec + capabilities + 最近 testkit 结果（统一名，P1-6） | 02 §5.1 · 04 §10.1 |
| GET | **`/api/health`** | 存活探针；唯一豁免访问口令的端点 | 11 §3.1 |
| POST / GET | `/api/system/backup` · `/api/system/version` | 备份导出 · 版本检查 | **v1.5 占位** |

### 6.7 WS 通道

| 通道 | 用途 |
|---|---|
| `/events` | `SandboxWsEvent` 推送（§3） |
| `/terminal?socketSessionKey=` | PTY 帧（§7.4 `TerminalClientFrame`/`TerminalServerFrame`）。**query 参数 camelCase**（P1-5），对应 DB 列 `socket_session_key`，映射在 gateway 层；**由服务端生成**（P2-9），前端只负责存回带（06 §6）。tmux re-attach 由后端负责 |

**SSE 与 codegen 的边界**：`openapi-typescript` 只能生成 `/api/system/diagnose` 的响应 content-type 声明，**流帧的类型必须手写**并与 WS 协议文件同放（同一份 `// SYNC WITH ...` 纪律，§3）——诊断帧结构见 02 §5.3。

### 6.8 统一错误响应 envelope（审计 P1-4）

**所有非 2xx 响应一律是这个形状**，无例外。**codegen 可得性**：`ErrorEnvelope` 必须作为具名 component 进 `openapi.json` 且被所有 error response 引用（`@nestjs/swagger` + nestjs-zod `patchNestJsSwagger()` 才能把 zod 的 enum/discriminatedUnion 如实反射）——否则前端 `components['schemas']['ErrorEnvelope']` 取不到、退化成手写（交叉评审 P1-4）。

```ts
interface ErrorEnvelope {
  code: string;          // 稳定错误码（02 §6.1；未映射的一律 'INTERNAL'，绝不缺省）
  message: string;       // 人类可读，可直接展示；已脱敏（不含明文/URL userinfo）
  retryable: boolean;    // 前端据此决定渲染不渲染 [重试]——不要用 HTTP 状态码猜
  traceId: string;       // 服务端日志关联，用户报障时报这个
  details?: Array<{ path?: string; code?: string; message: string }>;
                         // 逐项错误：env 校验（ENV_NAME_RESERVED 等）、镜像 validate 的 errors[]、DTO 字段校验
}
```

| 规则 | 说明 |
|---|---|
| **`code` 永不缺省** | 兜底也要给 `'INTERNAL'`——裸 500 会让前端连「操作失败（错误码 XXX）」都显示不出来（02 §6.2） |
| **`retryable` 是一等字段** | 它来自 `SandboxProviderError.retryable`（04 §4），不是从状态码推断的 |
| **`details` 只在有逐项信息时出现** | 空数组不如不给；`path` 用点号路径（`env[3].key`） |
| **MCP 侧同构** | tool 错误的文本载荷里带同一个 JSON，字段一致，便于 LLM 调用方解析（02 §6.1） |
| **CI 断言** | 25 每个错误码用例除断言 `code` 外，还断言 envelope 五字段完整性 |

---

## 7. 通用（wire）类型详细设计——两侧唯一权威定义

> 本节把散落在 13/23/27/§6/§3 的对外类型 **consolidate 成实际类型声明**（前几节讲"契约怎么流动/端点有哪些"，本节讲"类型本身长什么样"）。**这些类型的单一来源是后端 zod schema**（02 §3）：zod →（`createZodDto`）→ REST DTO / MCP inputSchema / openapi.json → 前端 `openapi.d.ts`。前端**永不手写**这些类型，一律从生成物取（14）。下面用 TS 声明表达**契约形状**（实现里是 zod，形状一致）。

### 7.1 枚举（字面量联合）——全库唯一取值来源

```ts
// —— sandbox 生命周期 ——
type SandboxStatus =                     // 13 §2.1 CHECK 12 值；推进顺序见注
  | 'pending' | 'scheduling' | 'preparing-workspace' | 'creating' | 'starting'
  | 'running' | 'idle' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'failed';
//   工作区先于实例：scheduling → preparing-workspace → creating → starting（审计 P0-1）
//   ⚠️ 'waiting-input' 不在此枚举——它是 running 的运行时子态，只走 WS + REST 派生字段（§7.4 / 03 §4.1）

type ReconciliationStatus = 'confirmed' | 'pending' | 'orphaned';   // 13 §2.1 对账

// —— project / 工作区 ——
type CloneStatus = 'cloning' | 'ready' | 'failed';   // 13 §2.2；空项目恒 ready；离开 failed 仅 retry-clone→cloning / convert-to-empty→ready
type ProjectSourceType = 'git' | 'empty';

// —— image ——
type ImageValidationStatus = 'pending' | 'valid' | 'warning' | 'invalid';  // 13 §2.4；warning 仍可选（P21-4 §9）
type RuntimeInstallStatus = 'not_installed' | 'installing' | 'installed' | 'failed';  // 13 runtime_installations

// —— credential ——
type CredentialKind = 'runtime' | 'git';                                   // 13 §2.5
type ObtainedViaRuntime = 'setup-token' | 'oauth-device' | 'api-key' | 'access-token-paste';
type ObtainedViaGit = 'git-ssh-key' | 'git-https-token';
type RuntimeAuthMode = 'account' | 'api-key';    // 二选一全局生效【模式】（active_auth_method，05 §4）；区别于鉴权【方式】 oauth-device/setup-token/api-key（BeginAuthRequest.method）
type CredentialStatus = 'none' | 'active' | 'expiring' | 'expired';  // 每 runtime 聚合态（UI 用，27 §runtime）
//   expiring = 剩 <7 天：只预警不拦截（前端据此决定拦截面板 vs 直达确认步）

// —— automation（v1.1）——
type AutomationRunStatus =                          // 13 automation_runs
  | 'pending' | 'running' | 'success' | 'failed' | 'timeout'
  | 'resource-exhausted' | 'skipped' | 'missed';
//   计数口径：仅 'failed'（含 'timeout'）计入连续失败；'skipped'/'missed' 不计（P21-7 §4 / 23 I-AUR）
type WebhookStatus = 'sent' | 'failed' | 'skipped';
type TriggerOn = 'failure' | 'success' | 'all';
type ScheduleKind = 'hourly' | 'daily' | 'weekly';

// —— terminal ——
type TerminalSessionStatus = 'attached' | 'detached' | 'closed';   // 13 §2.6

// —— 数值枚举 ——
type TimeoutMinutes = 30 | 60 | 120 | 240;          // 无头硬超时（默认 120）
type ArtifactRetentionDays = 3 | 7 | 30;            // 保留卷（默认 7）
```

### 7.2 通用值类型

> **可扩展类型原则（needs 6/7：sandbox/runtime/image 经 registry 可扩展）**——这决定一个标识该用**封闭联合**还是**开放 id**：
> - **封闭联合**（`'a' | 'b'`）：取值集合**由平台代码固定**、增删必须改代码（如 `SandboxStatus` 状态机、`CloneStatus`、错误分支）。加一个值 = 平台要处理它 = 本就该改代码。
> - **开放 id**（`type XxxId = string`）：取值集合**由 registry 在运行时决定**、第三方注册即生效、平台代码零改动（`ProviderId` / `runtime` id / `image` id）。**provider/runtime/image 必须是开放 id**——把它们写成 `'aio' | 'boxlite'` 这种封闭联合，等于每注册一个新 provider 都要改一次 wire 契约，直接违背"registry 可扩展"（04 §8：第三方注册的 runtime 自动出现、前端零改动）。
>
> ```ts
> type ProviderId = string;   // 已注册的 sandbox provider id；内建 'aio'（默认）/'boxlite'，但取值不封闭——registry 解析
> // runtime、image 同理已是 string；前端下拉的可选值来自 GET /api/runtimes / GET /api/images（运行时数据），不是编译期字面量
> // 内建集合仅作文档记载与默认值，不进类型约束——校验"是否已注册"在后端 registry，不在 TS 联合类型
> ```

```ts
// 分页信封（列表类响应统一形状）
interface Paginated<T> { items: T[]; total: number; page: number; pageSize: number; }  // 用于 automation runs（GET .../runs）；其余列表端点返回裸数组 T[]

// 资源池快照（含磁盘——审计 P1-9）
interface ResourcePoolSnapshot {
  totalCores: number; usedCores: number;
  totalRamMb: number; usedRamMb: number;
  totalDiskMb: number; usedDiskMb: number;          // usedDiskMb = 活跃分配 disk_mb_reserved + 保留卷 size
}

// 镜像不可变坐标（digest 非空——23 I-IMG-6）
interface ImageRef { registry: string; repository: string; tag: string; digest: string; }

// 掩码凭证（永不回明文，05 §4）
interface MaskedRuntimeCredential {
  id: string; kind: 'runtime'; runtimeId: string;
  mode: RuntimeAuthMode; obtainedVia: ObtainedViaRuntime;   // mode=生效模式 account|api-key（区别于鉴权方式 method）
  maskedIdentifier: string;        // 帐号 a***@gm / key sk-...ab12
  active: boolean;                 // 是否当前生效模式
  expiresAt?: string;              // ISO；API key 常无
  lastUsedAt?: string;
}
interface MaskedGitCredential {
  id: string; kind: 'git'; type: 'ssh-key' | 'https-token';
  maskedIdentifier: string;        // SSH=指纹 SHA256:… / token=尾号
  platform?: 'github' | 'gitlab' | 'gitee' | 'other';   // gitee 明确支持（国内自建/云托管 git 常见）
  knownHosts?: string; lastUsedAt?: string; createdAt: string;
}

// 鉴权挑战（不落表——23 D-6；由 auth helper 产出，05 §3）
interface AuthChallenge {
  challengeRef: string;
  kind: 'url' | 'device-code' | 'paste-prompt';
  //   三种"要用户在浏览器里做点什么"的形态，决定前端渲染哪种界面：
  //   'url'         = 只给一个链接，用户点开在浏览器完成 OAuth 即自动成功（无需回填）
  //   'device-code' = 给一个设备码 + 验证链接，用户在浏览器输码授权，前端轮询到成功（Codex device-auth，userCode/expiresAt 15min）
  //   'paste-prompt'= 用户在浏览器授权后拿到一段【授权码】，需要【粘贴回来】提交（Claude setup-token：CLI 打印链接→浏览器授权→复制 code→粘贴，见 05 §3）
  //                   —— "paste" 指"粘贴授权码"、"prompt" 指"需要一个回填输入框"；这类挑战靠 completeAuth 提交 pastedText 完成
  verificationUrl?: string; userCode?: string;      // device-code
  expiresAt?: string;                               // device-code 15min
  instructions: string;
}
type AuthStatus = 'pending' | 'success' | 'expired' | 'error';   // 轮询返回
```

### 7.3 各上下文 DTO（请求体 + 响应体）

> 字段名 **camelCase**（审计 P1-5，DB 的 snake_case 映射在 repository 层）。端点↔DTO 对照见 §6 与 27 §2–§9。

```ts
// ===== sandbox（Task）=====
interface CreateSandboxRequest {
  projectId: string;                 // 确认步项目行所选（27 §10.2）
  runtime: string;                   // runtime id
  image: string;                     // 镜像 id；缺省平台默认 AIO
  provider?: ProviderId;             // 已注册的 provider id，缺省 'aio'（见 §7.2.1 可扩展类型原则——不是封闭联合）
  initialPrompt?: string;            // 可选任务指令；≤8000 字符；不落前端持久化
  headless?: boolean;                // 缺省 false
  timeoutMinutes?: TimeoutMinutes;   // headless=true 缺省 120；headless=false 传即 400
}
interface SandboxDto {
  id: string; projectId: string; runtime: string; image: string; provider: string;
  status: SandboxStatus;
  waitingInput: boolean;             // 派生字段（§7.4；网关内存态投影，非 status）
  name: string;                      // Task 视角命名
  headless: boolean; createdAt: string; lastActiveAt?: string;
  // 资源/配额不对外暴露（P22 §4.6）
}
interface DestroySandboxRequest { keepVolume?: boolean; }   // 勾选保留成果卷=true（默认 UI 勾选）
interface ExecRequest { command: string; }                 // POST /:id/exec（非交互命令）
interface ExecResult { exitCode: number; stdout: string; stderr: string; }
interface RunTaskRequest { prompt: string; }               // POST /:id/runtimes/:rt/tasks（无头）
interface RunTaskResult { runId: string; }

// ===== project =====
interface CreateProjectRequest { name: string; sourceType: ProjectSourceType; repoUrl?: string; }  // 与响应 ProjectDto.sourceType 对称
interface ProjectDto {
  id: string; name: string; sourceType: ProjectSourceType;
  cloneStatus: CloneStatus; cloneErrorCode?: string;
  taskCount: number; createdAt: string;
  // 「来源」字段不对外展示（产品定案）——repoUrl 不入 DTO
}
// 无 body：POST /:id/retry-clone、POST /:id/convert-to-empty（仅 failed 态，非该态 409）

// ===== runtime / 凭证 =====
interface RuntimeDto {
  id: string; displayName: string; vendor: string;
  authMethods: Array<'oauth-device' | 'setup-token' | 'api-key'>;   // getAuthMethods 下发
  credentialStatus: CredentialStatus;
  maskedIdentifier?: string; expiresAt?: string; activeAuthMethod?: RuntimeAuthMode;
}
interface BeginAuthRequest { method: 'oauth-device' | 'setup-token'; }        // POST /api/runtimes/:rt/auth/begin → AuthChallenge
interface CompleteAuthRequest { challengeRef: string; pastedText?: string; }  // setup-token 粘贴
interface SubmitSecretRequest { method: 'api-key'; secret: string; }          // POST .../credentials/secret（不经 sandbox）
interface SetAuthModeRequest { mode: RuntimeAuthMode; }                   // PUT .../auth-mode；目标未配置→409

// ===== git 凭证 =====  读 GET /api/credentials?kind=git（kind 必填）
interface CreateGitCredentialRequest { type: 'ssh-key' | 'https-token'; secret: string; platform?: 'github' | 'gitlab' | 'gitee' | 'other';   // gitee 明确支持（国内自建/云托管 git 常见） }
interface TestGitConnectionRequest { repoUrl?: string; }
interface TestGitConnectionResult { ok: boolean; errorCode?: string; message: string; }   // 不回 ref 名

// ===== image =====
interface ValidateImageRequest { ref: string; }                              // 注册前预检，不落库
interface ImageValidationResult { status: ImageValidationStatus; digest?: string; warnings: string[]; errors: string[]; supportedRuntimes: string[]; }
interface EnvVar { key: string; value?: string; secret: boolean; }           // secret 传空 value=保持不变
interface ImageConfig { env: EnvVar[]; cmdOverride?: string; }
interface PatchImageRequest { isActive?: boolean; imageConfig?: ImageConfig; }  // 两调用方各发各字段（F21-4）
interface ImageDto {
  id: string; name: string; ref: ImageRef;
  validationStatus: ImageValidationStatus; supportedRuntimes: string[];
  isActive: boolean; isBuiltin: boolean; lastValidatedAt?: string;
}

// ===== automation（v1.1）=====
interface AutomationScheduleConfig { minute?: number; time?: string; days?: number[]; }  // 不含时区
interface CreateAutomationRequest {
  name: string; description?: string; runtime: string; prompt: string;    // ≤8000 字符
  scheduleKind: ScheduleKind; scheduleConfig: AutomationScheduleConfig;
  timezone: string;                        // IANA 快照（23 I-AUT-9）；编辑时不隐式重传
  timeoutMinutes: TimeoutMinutes; artifactRetentionDays: ArtifactRetentionDays;
  webhookUrl?: string; triggerOn?: TriggerOn;
}
interface AutomationDto { id: string; projectId: string; enabled: boolean; degraded: boolean; consecutiveFailures: number; nextTriggerAt?: string; /* + Create 字段回显 */ }
interface AutomationRunDto { id: string; automationId: string; status: AutomationRunStatus; retryCount: number; retryAt?: string; startedAt: string; durationMs?: number; outputSummary?: string; webhookStatus?: WebhookStatus; }

// ===== system =====
interface InitStatusDto { initialized: boolean; lastConnectivityCheck?: ConnectivityResult[]; }
interface InitRequest { proxyConfig?: { httpProxy?: string; httpsProxy?: string; noProxy?: string }; acknowledgeOffline?: boolean; }
interface AccessPasscodeRequest { action: 'enable' | 'regenerate' | 'disable'; }
interface AccessPasscodeResult { passcode?: string; }        // 启用/重生成一次性返回明文；此后只存 hash
interface ProviderHealthDto { id: string; capabilities: number; healthy: boolean; recentFailureRate?: number; }
interface ConnectivityResult { target: string; ok: boolean; latencyMs?: number; hint?: string; }
```

### 7.4 WS 帧类型

> **本节是 WS 协议的唯一 canonical 定义**（§3 已删除，其语义表并入本节下方）。两个通道各有一套判别字段：**/terminal 帧用 `type`、/events 事件用 `event`**——这是刻意的（终端是字节流帧协议、事件是业务投影，两套无关的 union，判别字段名不同以防误 parse 到对方）。前后端各持一份 `ws-protocol.ts`，`SYNC WITH` 注释指向本节，改动经 X-Schema-Hash CI 比对（§1、14 §2.5）。

```ts
// —— /terminal 通道 ——（URL query 带 socketSessionKey，服务端生成，06 §6；判别键 type）
type TerminalClientFrame =
  | { type: 'input'; data: string }              // 用户键入（plain string，非 base64）
  | { type: 'resize'; cols: number; rows: number }
  | { type: 'ping' };
type TerminalServerFrame =
  | { type: 'data'; data: string }               // pty 输出（plain string；xterm 直接 write，无需解码；16ms 批量转发 + 前端 rAF 批处理）
  | { type: 'exit'; code: number }               // agent CLI 进程退出（137=OOM 等）——前端据此显示 exit code + 人话解释（08 §8）
  | { type: 'pong' }
  | { type: 'session'; socketSessionKey: string }; // 开会话首帧下发重连凭据

// —— /events 通道 —— 判别键 event
type SandboxEvent =
  | { event: 'sandbox.created'; sandboxId: string; projectId: string }
  | { event: 'sandbox.status_changed'; sandboxId: string; status: SandboxStatus; phase?: string }
  | { event: 'sandbox.removed'; sandboxId: string }
  | { event: 'sandbox.waiting_input'; sandboxId: string; waiting: boolean; sessionId?: string }  // 翻转时推；可丢失；不进 Outbox（06 §8）
  | { event: 'project.clone_progress'; projectId: string; phase: 'cloning' | 'slow' | 'done' | 'failed'; receivedBytes?: number; totalBytes?: number; percent?: number; errorCode?: string }  // git 内部 counting/receiving/resolving 统一投影为 'cloning'，字节进度单独带（03 §7.2）
  | { event: 'runtime-auth.status_changed'; runtime: string };
```

**决策记录（2026-08，交叉评审 P0-1）**：`data` 用 **plain string 非 base64**（xterm 直接 write，省一次编解码）；**保留 `exit` 帧**（04 的 `ProcessStream.onExit` 需要一条上报通道，删了前端无法得知进程退出）；终端帧判别键统一 `type`。此前 §3 与本节冲突（§3 用 `payload`/base64/无 exit/`SandboxSummary` 嵌套/clone 6 值）已删 §3、以本节为准。

### 7.5 错误 envelope

见 §6.8（`ErrorEnvelope`：`code / message / retryable / traceId / details?[]`）——所有非 2xx 一律此形状，REST 与 MCP 同构。

### 7.6 类型总量与派生链

| 类别 | 数量 | 单源 |
|---|---|---|
| 枚举 | 19（含 AuthStatus） | 后端 zod `z.enum` / 字面量联合 |
| 通用值类型 | 8 | zod schema |
| DTO（请求+响应） | ~40 | `createZodDto()`（02 §3） |
| WS 帧/事件 | 2 通道 union（6 事件 + 3+3 帧） | 手工同步 `ws-protocol.ts`（14 §2.5，带 X-Schema-Hash 握手） |
| 错误 envelope | 1 | §6.8 |

**一份类型的五处消费**（02 §3）：`zod schema` → ① `createZodDto()` REST DTO → ② `@nestjs/swagger` 反射 → `openapi.json` → ③ 前端 `openapi-typescript` → `openapi.d.ts`；zod schema 又直接作 ④ `@Tool({parameters})` MCP inputSchema、⑤ 运行时 `.parse()` 入参校验。**改一处 zod，五处同步**——CI 漂移检测（§2.1）+ X-Schema-Hash（14 §2.4）双保险。后端实现形态见 [28 类型与领域类型详细设计](../backend/28-类型与领域类型详细设计.md)，前端消费形态见 [前端 07/28]。
