{
  "name": "cloud-agent-platform-docs",
  "version": "0.0.0",
  "private": true,
  "description": "云 Agent 管理平台 · 文档主仓（docs/ + api/web 两个 submodule）。只承载文档一致性门禁，不发布、不构建产物。",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "docs:check": "node scripts/docs-check.mjs"
  }
}
